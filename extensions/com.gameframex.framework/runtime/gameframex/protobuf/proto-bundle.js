/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
"use strict";

var $protobuf = require("protobufjs/minimal");

// Common aliases
var $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
var $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

$root.Basic = (function() {

    /**
     * Namespace Basic.
     * @exports Basic
     * @namespace
     */
    var Basic = {};

    Basic.ReqHeartBeat = (function() {

        /**
         * Properties of a ReqHeartBeat.
         * @memberof Basic
         * @interface IReqHeartBeat
         * @property {number|Long|null} [Timestamp] ReqHeartBeat Timestamp
         */

        /**
         * Constructs a new ReqHeartBeat.
         * @memberof Basic
         * @classdesc Represents a ReqHeartBeat.
         * @implements IReqHeartBeat
         * @constructor
         * @param {Basic.IReqHeartBeat=} [properties] Properties to set
         */
        function ReqHeartBeat(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqHeartBeat Timestamp.
         * @member {number|Long} Timestamp
         * @memberof Basic.ReqHeartBeat
         * @instance
         */
        ReqHeartBeat.prototype.Timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqHeartBeat instance using the specified properties.
         * @function create
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {Basic.IReqHeartBeat=} [properties] Properties to set
         * @returns {Basic.ReqHeartBeat} ReqHeartBeat instance
         */
        ReqHeartBeat.create = function create(properties) {
            return new ReqHeartBeat(properties);
        };

        /**
         * Encodes the specified ReqHeartBeat message. Does not implicitly {@link Basic.ReqHeartBeat.verify|verify} messages.
         * @function encode
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {Basic.IReqHeartBeat} message ReqHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqHeartBeat.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Timestamp != null && Object.hasOwnProperty.call(message, "Timestamp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.Timestamp);
            return writer;
        };

        /**
         * Encodes the specified ReqHeartBeat message, length delimited. Does not implicitly {@link Basic.ReqHeartBeat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {Basic.IReqHeartBeat} message ReqHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqHeartBeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqHeartBeat message from the specified reader or buffer.
         * @function decode
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Basic.ReqHeartBeat} ReqHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqHeartBeat.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Basic.ReqHeartBeat();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Timestamp = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqHeartBeat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Basic.ReqHeartBeat} ReqHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqHeartBeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqHeartBeat message.
         * @function verify
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqHeartBeat.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Timestamp != null && Object.hasOwnProperty.call(message, "Timestamp"))
                if (!$util.isInteger(message.Timestamp) && !(message.Timestamp && $util.isInteger(message.Timestamp.low) && $util.isInteger(message.Timestamp.high)))
                    return "Timestamp: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqHeartBeat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Basic.ReqHeartBeat} ReqHeartBeat
         */
        ReqHeartBeat.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Basic.ReqHeartBeat)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Basic.ReqHeartBeat: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Basic.ReqHeartBeat();
            if (object.Timestamp != null)
                if ($util.Long)
                    message.Timestamp = $util.Long.fromValue(object.Timestamp, false);
                else if (typeof object.Timestamp === "string")
                    message.Timestamp = parseInt(object.Timestamp, 10);
                else if (typeof object.Timestamp === "number")
                    message.Timestamp = object.Timestamp;
                else if (typeof object.Timestamp === "object")
                    message.Timestamp = new $util.LongBits(object.Timestamp.low >>> 0, object.Timestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqHeartBeat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {Basic.ReqHeartBeat} message ReqHeartBeat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqHeartBeat.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.Timestamp != null && Object.hasOwnProperty.call(message, "Timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Timestamp = typeof message.Timestamp === "number" ? BigInt(message.Timestamp) : $util.Long.fromBits(message.Timestamp.low >>> 0, message.Timestamp.high >>> 0, false).toBigInt();
                else if (typeof message.Timestamp === "number")
                    object.Timestamp = options.longs === String ? String(message.Timestamp) : message.Timestamp;
                else
                    object.Timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.Timestamp) : options.longs === Number ? new $util.LongBits(message.Timestamp.low >>> 0, message.Timestamp.high >>> 0).toNumber() : message.Timestamp;
            return object;
        };

        /**
         * Converts this ReqHeartBeat to JSON.
         * @function toJSON
         * @memberof Basic.ReqHeartBeat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqHeartBeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqHeartBeat
         * @function getTypeUrl
         * @memberof Basic.ReqHeartBeat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqHeartBeat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Basic.ReqHeartBeat";
        };

        return ReqHeartBeat;
    })();

    Basic.NotifyHeartBeat = (function() {

        /**
         * Properties of a NotifyHeartBeat.
         * @memberof Basic
         * @interface INotifyHeartBeat
         * @property {number|Long|null} [Timestamp] NotifyHeartBeat Timestamp
         */

        /**
         * Constructs a new NotifyHeartBeat.
         * @memberof Basic
         * @classdesc Represents a NotifyHeartBeat.
         * @implements INotifyHeartBeat
         * @constructor
         * @param {Basic.INotifyHeartBeat=} [properties] Properties to set
         */
        function NotifyHeartBeat(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyHeartBeat Timestamp.
         * @member {number|Long} Timestamp
         * @memberof Basic.NotifyHeartBeat
         * @instance
         */
        NotifyHeartBeat.prototype.Timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new NotifyHeartBeat instance using the specified properties.
         * @function create
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {Basic.INotifyHeartBeat=} [properties] Properties to set
         * @returns {Basic.NotifyHeartBeat} NotifyHeartBeat instance
         */
        NotifyHeartBeat.create = function create(properties) {
            return new NotifyHeartBeat(properties);
        };

        /**
         * Encodes the specified NotifyHeartBeat message. Does not implicitly {@link Basic.NotifyHeartBeat.verify|verify} messages.
         * @function encode
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {Basic.INotifyHeartBeat} message NotifyHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyHeartBeat.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Timestamp != null && Object.hasOwnProperty.call(message, "Timestamp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.Timestamp);
            return writer;
        };

        /**
         * Encodes the specified NotifyHeartBeat message, length delimited. Does not implicitly {@link Basic.NotifyHeartBeat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {Basic.INotifyHeartBeat} message NotifyHeartBeat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyHeartBeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyHeartBeat message from the specified reader or buffer.
         * @function decode
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Basic.NotifyHeartBeat} NotifyHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyHeartBeat.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Basic.NotifyHeartBeat();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Timestamp = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyHeartBeat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Basic.NotifyHeartBeat} NotifyHeartBeat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyHeartBeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyHeartBeat message.
         * @function verify
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyHeartBeat.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Timestamp != null && Object.hasOwnProperty.call(message, "Timestamp"))
                if (!$util.isInteger(message.Timestamp) && !(message.Timestamp && $util.isInteger(message.Timestamp.low) && $util.isInteger(message.Timestamp.high)))
                    return "Timestamp: integer|Long expected";
            return null;
        };

        /**
         * Creates a NotifyHeartBeat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Basic.NotifyHeartBeat} NotifyHeartBeat
         */
        NotifyHeartBeat.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Basic.NotifyHeartBeat)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Basic.NotifyHeartBeat: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Basic.NotifyHeartBeat();
            if (object.Timestamp != null)
                if ($util.Long)
                    message.Timestamp = $util.Long.fromValue(object.Timestamp, false);
                else if (typeof object.Timestamp === "string")
                    message.Timestamp = parseInt(object.Timestamp, 10);
                else if (typeof object.Timestamp === "number")
                    message.Timestamp = object.Timestamp;
                else if (typeof object.Timestamp === "object")
                    message.Timestamp = new $util.LongBits(object.Timestamp.low >>> 0, object.Timestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a NotifyHeartBeat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {Basic.NotifyHeartBeat} message NotifyHeartBeat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyHeartBeat.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.Timestamp != null && Object.hasOwnProperty.call(message, "Timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Timestamp = typeof message.Timestamp === "number" ? BigInt(message.Timestamp) : $util.Long.fromBits(message.Timestamp.low >>> 0, message.Timestamp.high >>> 0, false).toBigInt();
                else if (typeof message.Timestamp === "number")
                    object.Timestamp = options.longs === String ? String(message.Timestamp) : message.Timestamp;
                else
                    object.Timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.Timestamp) : options.longs === Number ? new $util.LongBits(message.Timestamp.low >>> 0, message.Timestamp.high >>> 0).toNumber() : message.Timestamp;
            return object;
        };

        /**
         * Converts this NotifyHeartBeat to JSON.
         * @function toJSON
         * @memberof Basic.NotifyHeartBeat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyHeartBeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyHeartBeat
         * @function getTypeUrl
         * @memberof Basic.NotifyHeartBeat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyHeartBeat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Basic.NotifyHeartBeat";
        };

        return NotifyHeartBeat;
    })();

    Basic.NotifyServerFullyLoaded = (function() {

        /**
         * Properties of a NotifyServerFullyLoaded.
         * @memberof Basic
         * @interface INotifyServerFullyLoaded
         */

        /**
         * Constructs a new NotifyServerFullyLoaded.
         * @memberof Basic
         * @classdesc Represents a NotifyServerFullyLoaded.
         * @implements INotifyServerFullyLoaded
         * @constructor
         * @param {Basic.INotifyServerFullyLoaded=} [properties] Properties to set
         */
        function NotifyServerFullyLoaded(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new NotifyServerFullyLoaded instance using the specified properties.
         * @function create
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {Basic.INotifyServerFullyLoaded=} [properties] Properties to set
         * @returns {Basic.NotifyServerFullyLoaded} NotifyServerFullyLoaded instance
         */
        NotifyServerFullyLoaded.create = function create(properties) {
            return new NotifyServerFullyLoaded(properties);
        };

        /**
         * Encodes the specified NotifyServerFullyLoaded message. Does not implicitly {@link Basic.NotifyServerFullyLoaded.verify|verify} messages.
         * @function encode
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {Basic.INotifyServerFullyLoaded} message NotifyServerFullyLoaded message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyServerFullyLoaded.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            return writer;
        };

        /**
         * Encodes the specified NotifyServerFullyLoaded message, length delimited. Does not implicitly {@link Basic.NotifyServerFullyLoaded.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {Basic.INotifyServerFullyLoaded} message NotifyServerFullyLoaded message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyServerFullyLoaded.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyServerFullyLoaded message from the specified reader or buffer.
         * @function decode
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Basic.NotifyServerFullyLoaded} NotifyServerFullyLoaded
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyServerFullyLoaded.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Basic.NotifyServerFullyLoaded();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyServerFullyLoaded message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Basic.NotifyServerFullyLoaded} NotifyServerFullyLoaded
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyServerFullyLoaded.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyServerFullyLoaded message.
         * @function verify
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyServerFullyLoaded.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            return null;
        };

        /**
         * Creates a NotifyServerFullyLoaded message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Basic.NotifyServerFullyLoaded} NotifyServerFullyLoaded
         */
        NotifyServerFullyLoaded.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Basic.NotifyServerFullyLoaded)
                return object;
            return new $root.Basic.NotifyServerFullyLoaded();
        };

        /**
         * Creates a plain object from a NotifyServerFullyLoaded message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {Basic.NotifyServerFullyLoaded} message NotifyServerFullyLoaded
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyServerFullyLoaded.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this NotifyServerFullyLoaded to JSON.
         * @function toJSON
         * @memberof Basic.NotifyServerFullyLoaded
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyServerFullyLoaded.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyServerFullyLoaded
         * @function getTypeUrl
         * @memberof Basic.NotifyServerFullyLoaded
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyServerFullyLoaded.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Basic.NotifyServerFullyLoaded";
        };

        return NotifyServerFullyLoaded;
    })();

    return Basic;
})();

$root.Common = (function() {

    /**
     * Namespace Common.
     * @exports Common
     * @namespace
     */
    var Common = {};

    /**
     * ResultCode enum.
     * @name Common.ResultCode
     * @enum {number}
     * @property {number} Success=0 Success value
     * @property {number} Failed=1 Failed value
     */
    Common.ResultCode = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Success"] = 0;
        values[valuesById[1] = "Failed"] = 1;
        return values;
    })();

    /**
     * OperationStatusCode enum.
     * @name Common.OperationStatusCode
     * @enum {number}
     * @property {number} Ok=0 Ok value
     * @property {number} ConfigErr=1 ConfigErr value
     * @property {number} ParamErr=2 ParamErr value
     * @property {number} CostNotEnough=3 CostNotEnough value
     * @property {number} Forbidden=4 Forbidden value
     * @property {number} NotFound=5 NotFound value
     * @property {number} HasExist=6 HasExist value
     * @property {number} AccountCannotBeNull=7 AccountCannotBeNull value
     * @property {number} Unprocessable=8 Unprocessable value
     * @property {number} UnknownPlatform=9 UnknownPlatform value
     * @property {number} Notice=10 Notice value
     * @property {number} FuncNotOpen=11 FuncNotOpen value
     * @property {number} Other=12 Other value
     * @property {number} InternalServerError=13 InternalServerError value
     * @property {number} ServerFullyLoaded=14 ServerFullyLoaded value
     * @property {number} Unsupported=15 Unsupported value
     * @property {number} InvalidReward=16 InvalidReward value
     * @property {number} PartialSuccess=17 PartialSuccess value
     */
    Common.OperationStatusCode = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Ok"] = 0;
        values[valuesById[1] = "ConfigErr"] = 1;
        values[valuesById[2] = "ParamErr"] = 2;
        values[valuesById[3] = "CostNotEnough"] = 3;
        values[valuesById[4] = "Forbidden"] = 4;
        values[valuesById[5] = "NotFound"] = 5;
        values[valuesById[6] = "HasExist"] = 6;
        values[valuesById[7] = "AccountCannotBeNull"] = 7;
        values[valuesById[8] = "Unprocessable"] = 8;
        values[valuesById[9] = "UnknownPlatform"] = 9;
        values[valuesById[10] = "Notice"] = 10;
        values[valuesById[11] = "FuncNotOpen"] = 11;
        values[valuesById[12] = "Other"] = 12;
        values[valuesById[13] = "InternalServerError"] = 13;
        values[valuesById[14] = "ServerFullyLoaded"] = 14;
        values[valuesById[15] = "Unsupported"] = 15;
        values[valuesById[16] = "InvalidReward"] = 16;
        values[valuesById[17] = "PartialSuccess"] = 17;
        return values;
    })();

    return Common;
})();

$root.Bag = (function() {

    /**
     * Namespace Bag.
     * @exports Bag
     * @namespace
     */
    var Bag = {};

    Bag.BagItem = (function() {

        /**
         * Properties of a BagItem.
         * @memberof Bag
         * @interface IBagItem
         * @property {number|null} [ItemId] BagItem ItemId
         * @property {number|Long|null} [Count] BagItem Count
         */

        /**
         * Constructs a new BagItem.
         * @memberof Bag
         * @classdesc Represents a BagItem.
         * @implements IBagItem
         * @constructor
         * @param {Bag.IBagItem=} [properties] Properties to set
         */
        function BagItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BagItem ItemId.
         * @member {number} ItemId
         * @memberof Bag.BagItem
         * @instance
         */
        BagItem.prototype.ItemId = 0;

        /**
         * BagItem Count.
         * @member {number|Long} Count
         * @memberof Bag.BagItem
         * @instance
         */
        BagItem.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new BagItem instance using the specified properties.
         * @function create
         * @memberof Bag.BagItem
         * @static
         * @param {Bag.IBagItem=} [properties] Properties to set
         * @returns {Bag.BagItem} BagItem instance
         */
        BagItem.create = function create(properties) {
            return new BagItem(properties);
        };

        /**
         * Encodes the specified BagItem message. Does not implicitly {@link Bag.BagItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.BagItem
         * @static
         * @param {Bag.IBagItem} message BagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BagItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Count);
            return writer;
        };

        /**
         * Encodes the specified BagItem message, length delimited. Does not implicitly {@link Bag.BagItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.BagItem
         * @static
         * @param {Bag.IBagItem} message BagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BagItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BagItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.BagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.BagItem} BagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BagItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.BagItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a BagItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.BagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.BagItem} BagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BagItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BagItem message.
         * @function verify
         * @memberof Bag.BagItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BagItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            return null;
        };

        /**
         * Creates a BagItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.BagItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.BagItem} BagItem
         */
        BagItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.BagItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.BagItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.BagItem();
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a BagItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.BagItem
         * @static
         * @param {Bag.BagItem} message BagItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BagItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            return object;
        };

        /**
         * Converts this BagItem to JSON.
         * @function toJSON
         * @memberof Bag.BagItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BagItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BagItem
         * @function getTypeUrl
         * @memberof Bag.BagItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BagItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.BagItem";
        };

        return BagItem;
    })();

    Bag.ReqBagInfo = (function() {

        /**
         * Properties of a ReqBagInfo.
         * @memberof Bag
         * @interface IReqBagInfo
         */

        /**
         * Constructs a new ReqBagInfo.
         * @memberof Bag
         * @classdesc Represents a ReqBagInfo.
         * @implements IReqBagInfo
         * @constructor
         * @param {Bag.IReqBagInfo=} [properties] Properties to set
         */
        function ReqBagInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new ReqBagInfo instance using the specified properties.
         * @function create
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {Bag.IReqBagInfo=} [properties] Properties to set
         * @returns {Bag.ReqBagInfo} ReqBagInfo instance
         */
        ReqBagInfo.create = function create(properties) {
            return new ReqBagInfo(properties);
        };

        /**
         * Encodes the specified ReqBagInfo message. Does not implicitly {@link Bag.ReqBagInfo.verify|verify} messages.
         * @function encode
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {Bag.IReqBagInfo} message ReqBagInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqBagInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            return writer;
        };

        /**
         * Encodes the specified ReqBagInfo message, length delimited. Does not implicitly {@link Bag.ReqBagInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {Bag.IReqBagInfo} message ReqBagInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqBagInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqBagInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.ReqBagInfo} ReqBagInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqBagInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.ReqBagInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqBagInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.ReqBagInfo} ReqBagInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqBagInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqBagInfo message.
         * @function verify
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqBagInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            return null;
        };

        /**
         * Creates a ReqBagInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.ReqBagInfo} ReqBagInfo
         */
        ReqBagInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.ReqBagInfo)
                return object;
            return new $root.Bag.ReqBagInfo();
        };

        /**
         * Creates a plain object from a ReqBagInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {Bag.ReqBagInfo} message ReqBagInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqBagInfo.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this ReqBagInfo to JSON.
         * @function toJSON
         * @memberof Bag.ReqBagInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqBagInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqBagInfo
         * @function getTypeUrl
         * @memberof Bag.ReqBagInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqBagInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.ReqBagInfo";
        };

        return ReqBagInfo;
    })();

    Bag.RespBagInfo = (function() {

        /**
         * Properties of a RespBagInfo.
         * @memberof Bag
         * @interface IRespBagInfo
         * @property {Object.<string,number|Long>|null} [ItemDic] RespBagInfo ItemDic
         */

        /**
         * Constructs a new RespBagInfo.
         * @memberof Bag
         * @classdesc Represents a RespBagInfo.
         * @implements IRespBagInfo
         * @constructor
         * @param {Bag.IRespBagInfo=} [properties] Properties to set
         */
        function RespBagInfo(properties) {
            this.ItemDic = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespBagInfo ItemDic.
         * @member {Object.<string,number|Long>} ItemDic
         * @memberof Bag.RespBagInfo
         * @instance
         */
        RespBagInfo.prototype.ItemDic = $util.emptyObject;

        /**
         * Creates a new RespBagInfo instance using the specified properties.
         * @function create
         * @memberof Bag.RespBagInfo
         * @static
         * @param {Bag.IRespBagInfo=} [properties] Properties to set
         * @returns {Bag.RespBagInfo} RespBagInfo instance
         */
        RespBagInfo.create = function create(properties) {
            return new RespBagInfo(properties);
        };

        /**
         * Encodes the specified RespBagInfo message. Does not implicitly {@link Bag.RespBagInfo.verify|verify} messages.
         * @function encode
         * @memberof Bag.RespBagInfo
         * @static
         * @param {Bag.IRespBagInfo} message RespBagInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespBagInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic"))
                for (var keys = Object.keys(message.ItemDic), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.ItemDic[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespBagInfo message, length delimited. Does not implicitly {@link Bag.RespBagInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.RespBagInfo
         * @static
         * @param {Bag.IRespBagInfo} message RespBagInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespBagInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespBagInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.RespBagInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.RespBagInfo} RespBagInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespBagInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message, key, value;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.RespBagInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.ItemDic === $util.emptyObject)
                            message.ItemDic = {};
                        var end2 = reader.uint32() + reader.pos;
                        if (end2 > reader.len)
                            throw RangeError("index out of range");
                        reader.len = end2;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int64();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (reader.pos !== end2)
                            throw RangeError("index out of range");
                        reader.len = end;
                        message.ItemDic[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespBagInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.RespBagInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.RespBagInfo} RespBagInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespBagInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespBagInfo message.
         * @function verify
         * @memberof Bag.RespBagInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespBagInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic")) {
                if (!$util.isObject(message.ItemDic))
                    return "ItemDic: object expected";
                var key = Object.keys(message.ItemDic);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "ItemDic: integer key{k:int32} expected";
                    if (!$util.isInteger(message.ItemDic[key[i]]) && !(message.ItemDic[key[i]] && $util.isInteger(message.ItemDic[key[i]].low) && $util.isInteger(message.ItemDic[key[i]].high)))
                        return "ItemDic: integer|Long{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates a RespBagInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.RespBagInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.RespBagInfo} RespBagInfo
         */
        RespBagInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.RespBagInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.RespBagInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.RespBagInfo();
            if (object.ItemDic) {
                if (!$util.isObject(object.ItemDic))
                    throw TypeError(".Bag.RespBagInfo.ItemDic: object expected");
                message.ItemDic = {};
                for (var keys = Object.keys(object.ItemDic), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ItemDic, keys[i]);
                    if ($util.Long)
                        message.ItemDic[keys[i]] = $util.Long.fromValue(object.ItemDic[keys[i]], false);
                    else if (typeof object.ItemDic[keys[i]] === "string")
                        message.ItemDic[keys[i]] = parseInt(object.ItemDic[keys[i]], 10);
                    else if (typeof object.ItemDic[keys[i]] === "number")
                        message.ItemDic[keys[i]] = object.ItemDic[keys[i]];
                    else if (typeof object.ItemDic[keys[i]] === "object")
                        message.ItemDic[keys[i]] = new $util.LongBits(object.ItemDic[keys[i]].low >>> 0, object.ItemDic[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespBagInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.RespBagInfo
         * @static
         * @param {Bag.RespBagInfo} message RespBagInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespBagInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.ItemDic = {};
            var keys2;
            if (message.ItemDic && (keys2 = Object.keys(message.ItemDic)).length) {
                object.ItemDic = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ItemDic, keys2[j]);
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ItemDic[keys2[j]] = typeof message.ItemDic[keys2[j]] === "number" ? BigInt(message.ItemDic[keys2[j]]) : $util.Long.fromBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.ItemDic[keys2[j]] === "number")
                        object.ItemDic[keys2[j]] = options.longs === String ? String(message.ItemDic[keys2[j]]) : message.ItemDic[keys2[j]];
                    else
                        object.ItemDic[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.ItemDic[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0).toNumber() : message.ItemDic[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this RespBagInfo to JSON.
         * @function toJSON
         * @memberof Bag.RespBagInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespBagInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespBagInfo
         * @function getTypeUrl
         * @memberof Bag.RespBagInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespBagInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.RespBagInfo";
        };

        return RespBagInfo;
    })();

    Bag.NotifyBagItem = (function() {

        /**
         * Properties of a NotifyBagItem.
         * @memberof Bag
         * @interface INotifyBagItem
         * @property {number|null} [ItemId] NotifyBagItem ItemId
         * @property {number|Long|null} [Count] NotifyBagItem Count
         * @property {number|Long|null} [Value] NotifyBagItem Value
         */

        /**
         * Constructs a new NotifyBagItem.
         * @memberof Bag
         * @classdesc Represents a NotifyBagItem.
         * @implements INotifyBagItem
         * @constructor
         * @param {Bag.INotifyBagItem=} [properties] Properties to set
         */
        function NotifyBagItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyBagItem ItemId.
         * @member {number} ItemId
         * @memberof Bag.NotifyBagItem
         * @instance
         */
        NotifyBagItem.prototype.ItemId = 0;

        /**
         * NotifyBagItem Count.
         * @member {number|Long} Count
         * @memberof Bag.NotifyBagItem
         * @instance
         */
        NotifyBagItem.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NotifyBagItem Value.
         * @member {number|Long} Value
         * @memberof Bag.NotifyBagItem
         * @instance
         */
        NotifyBagItem.prototype.Value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new NotifyBagItem instance using the specified properties.
         * @function create
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {Bag.INotifyBagItem=} [properties] Properties to set
         * @returns {Bag.NotifyBagItem} NotifyBagItem instance
         */
        NotifyBagItem.create = function create(properties) {
            return new NotifyBagItem(properties);
        };

        /**
         * Encodes the specified NotifyBagItem message. Does not implicitly {@link Bag.NotifyBagItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {Bag.INotifyBagItem} message NotifyBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyBagItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Count);
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.Value);
            return writer;
        };

        /**
         * Encodes the specified NotifyBagItem message, length delimited. Does not implicitly {@link Bag.NotifyBagItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {Bag.INotifyBagItem} message NotifyBagItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyBagItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyBagItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.NotifyBagItem} NotifyBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyBagItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.NotifyBagItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int64();
                        break;
                    }
                case 3: {
                        message.Value = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyBagItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.NotifyBagItem} NotifyBagItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyBagItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyBagItem message.
         * @function verify
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyBagItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                if (!$util.isInteger(message.Value) && !(message.Value && $util.isInteger(message.Value.low) && $util.isInteger(message.Value.high)))
                    return "Value: integer|Long expected";
            return null;
        };

        /**
         * Creates a NotifyBagItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.NotifyBagItem} NotifyBagItem
         */
        NotifyBagItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.NotifyBagItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.NotifyBagItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.NotifyBagItem();
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            if (object.Value != null)
                if ($util.Long)
                    message.Value = $util.Long.fromValue(object.Value, false);
                else if (typeof object.Value === "string")
                    message.Value = parseInt(object.Value, 10);
                else if (typeof object.Value === "number")
                    message.Value = object.Value;
                else if (typeof object.Value === "object")
                    message.Value = new $util.LongBits(object.Value.low >>> 0, object.Value.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a NotifyBagItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {Bag.NotifyBagItem} message NotifyBagItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyBagItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Value = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Value = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Value = typeof message.Value === "number" ? BigInt(message.Value) : $util.Long.fromBits(message.Value.low >>> 0, message.Value.high >>> 0, false).toBigInt();
                else if (typeof message.Value === "number")
                    object.Value = options.longs === String ? String(message.Value) : message.Value;
                else
                    object.Value = options.longs === String ? $util.Long.prototype.toString.call(message.Value) : options.longs === Number ? new $util.LongBits(message.Value.low >>> 0, message.Value.high >>> 0).toNumber() : message.Value;
            return object;
        };

        /**
         * Converts this NotifyBagItem to JSON.
         * @function toJSON
         * @memberof Bag.NotifyBagItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyBagItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyBagItem
         * @function getTypeUrl
         * @memberof Bag.NotifyBagItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyBagItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.NotifyBagItem";
        };

        return NotifyBagItem;
    })();

    Bag.NotifyBagInfoChanged = (function() {

        /**
         * Properties of a NotifyBagInfoChanged.
         * @memberof Bag
         * @interface INotifyBagInfoChanged
         * @property {Object.<string,Bag.INotifyBagItem>|null} [ItemDic] NotifyBagInfoChanged ItemDic
         */

        /**
         * Constructs a new NotifyBagInfoChanged.
         * @memberof Bag
         * @classdesc Represents a NotifyBagInfoChanged.
         * @implements INotifyBagInfoChanged
         * @constructor
         * @param {Bag.INotifyBagInfoChanged=} [properties] Properties to set
         */
        function NotifyBagInfoChanged(properties) {
            this.ItemDic = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyBagInfoChanged ItemDic.
         * @member {Object.<string,Bag.INotifyBagItem>} ItemDic
         * @memberof Bag.NotifyBagInfoChanged
         * @instance
         */
        NotifyBagInfoChanged.prototype.ItemDic = $util.emptyObject;

        /**
         * Creates a new NotifyBagInfoChanged instance using the specified properties.
         * @function create
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {Bag.INotifyBagInfoChanged=} [properties] Properties to set
         * @returns {Bag.NotifyBagInfoChanged} NotifyBagInfoChanged instance
         */
        NotifyBagInfoChanged.create = function create(properties) {
            return new NotifyBagInfoChanged(properties);
        };

        /**
         * Encodes the specified NotifyBagInfoChanged message. Does not implicitly {@link Bag.NotifyBagInfoChanged.verify|verify} messages.
         * @function encode
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {Bag.INotifyBagInfoChanged} message NotifyBagInfoChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyBagInfoChanged.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic"))
                for (var keys = Object.keys(message.ItemDic), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                    $root.Bag.NotifyBagItem.encode(message.ItemDic[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified NotifyBagInfoChanged message, length delimited. Does not implicitly {@link Bag.NotifyBagInfoChanged.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {Bag.INotifyBagInfoChanged} message NotifyBagInfoChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyBagInfoChanged.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyBagInfoChanged message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.NotifyBagInfoChanged} NotifyBagInfoChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyBagInfoChanged.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message, key, value;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.NotifyBagInfoChanged();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.ItemDic === $util.emptyObject)
                            message.ItemDic = {};
                        var end2 = reader.uint32() + reader.pos;
                        if (end2 > reader.len)
                            throw RangeError("index out of range");
                        reader.len = end2;
                        key = 0;
                        value = null;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = $root.Bag.NotifyBagItem.decode(reader, reader.uint32(), undefined, long + 1);
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (reader.pos !== end2)
                            throw RangeError("index out of range");
                        reader.len = end;
                        message.ItemDic[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyBagInfoChanged message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.NotifyBagInfoChanged} NotifyBagInfoChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyBagInfoChanged.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyBagInfoChanged message.
         * @function verify
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyBagInfoChanged.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic")) {
                if (!$util.isObject(message.ItemDic))
                    return "ItemDic: object expected";
                var key = Object.keys(message.ItemDic);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "ItemDic: integer key{k:int32} expected";
                    {
                        var error = $root.Bag.NotifyBagItem.verify(message.ItemDic[key[i]], long + 1);
                        if (error)
                            return "ItemDic." + error;
                    }
                }
            }
            return null;
        };

        /**
         * Creates a NotifyBagInfoChanged message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.NotifyBagInfoChanged} NotifyBagInfoChanged
         */
        NotifyBagInfoChanged.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.NotifyBagInfoChanged)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.NotifyBagInfoChanged: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.NotifyBagInfoChanged();
            if (object.ItemDic) {
                if (!$util.isObject(object.ItemDic))
                    throw TypeError(".Bag.NotifyBagInfoChanged.ItemDic: object expected");
                message.ItemDic = {};
                for (var keys = Object.keys(object.ItemDic), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ItemDic, keys[i]);
                    if (!$util.isObject(object.ItemDic[keys[i]]))
                        throw TypeError(".Bag.NotifyBagInfoChanged.ItemDic: object expected");
                    message.ItemDic[keys[i]] = $root.Bag.NotifyBagItem.fromObject(object.ItemDic[keys[i]], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NotifyBagInfoChanged message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {Bag.NotifyBagInfoChanged} message NotifyBagInfoChanged
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyBagInfoChanged.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.ItemDic = {};
            var keys2;
            if (message.ItemDic && (keys2 = Object.keys(message.ItemDic)).length) {
                object.ItemDic = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ItemDic, keys2[j]);
                    object.ItemDic[keys2[j]] = $root.Bag.NotifyBagItem.toObject(message.ItemDic[keys2[j]], options, q + 1);
                }
            }
            return object;
        };

        /**
         * Converts this NotifyBagInfoChanged to JSON.
         * @function toJSON
         * @memberof Bag.NotifyBagInfoChanged
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyBagInfoChanged.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyBagInfoChanged
         * @function getTypeUrl
         * @memberof Bag.NotifyBagInfoChanged
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyBagInfoChanged.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.NotifyBagInfoChanged";
        };

        return NotifyBagInfoChanged;
    })();

    Bag.ReqComposePet = (function() {

        /**
         * Properties of a ReqComposePet.
         * @memberof Bag
         * @interface IReqComposePet
         * @property {number|null} [FragmentId] ReqComposePet FragmentId
         */

        /**
         * Constructs a new ReqComposePet.
         * @memberof Bag
         * @classdesc Represents a ReqComposePet.
         * @implements IReqComposePet
         * @constructor
         * @param {Bag.IReqComposePet=} [properties] Properties to set
         */
        function ReqComposePet(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqComposePet FragmentId.
         * @member {number} FragmentId
         * @memberof Bag.ReqComposePet
         * @instance
         */
        ReqComposePet.prototype.FragmentId = 0;

        /**
         * Creates a new ReqComposePet instance using the specified properties.
         * @function create
         * @memberof Bag.ReqComposePet
         * @static
         * @param {Bag.IReqComposePet=} [properties] Properties to set
         * @returns {Bag.ReqComposePet} ReqComposePet instance
         */
        ReqComposePet.create = function create(properties) {
            return new ReqComposePet(properties);
        };

        /**
         * Encodes the specified ReqComposePet message. Does not implicitly {@link Bag.ReqComposePet.verify|verify} messages.
         * @function encode
         * @memberof Bag.ReqComposePet
         * @static
         * @param {Bag.IReqComposePet} message ReqComposePet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqComposePet.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.FragmentId != null && Object.hasOwnProperty.call(message, "FragmentId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.FragmentId);
            return writer;
        };

        /**
         * Encodes the specified ReqComposePet message, length delimited. Does not implicitly {@link Bag.ReqComposePet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.ReqComposePet
         * @static
         * @param {Bag.IReqComposePet} message ReqComposePet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqComposePet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqComposePet message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.ReqComposePet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.ReqComposePet} ReqComposePet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqComposePet.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.ReqComposePet();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.FragmentId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqComposePet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.ReqComposePet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.ReqComposePet} ReqComposePet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqComposePet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqComposePet message.
         * @function verify
         * @memberof Bag.ReqComposePet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqComposePet.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.FragmentId != null && Object.hasOwnProperty.call(message, "FragmentId"))
                if (!$util.isInteger(message.FragmentId))
                    return "FragmentId: integer expected";
            return null;
        };

        /**
         * Creates a ReqComposePet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.ReqComposePet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.ReqComposePet} ReqComposePet
         */
        ReqComposePet.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.ReqComposePet)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.ReqComposePet: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.ReqComposePet();
            if (object.FragmentId != null)
                message.FragmentId = object.FragmentId | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReqComposePet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.ReqComposePet
         * @static
         * @param {Bag.ReqComposePet} message ReqComposePet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqComposePet.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.FragmentId = 0;
            if (message.FragmentId != null && Object.hasOwnProperty.call(message, "FragmentId"))
                object.FragmentId = message.FragmentId;
            return object;
        };

        /**
         * Converts this ReqComposePet to JSON.
         * @function toJSON
         * @memberof Bag.ReqComposePet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqComposePet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqComposePet
         * @function getTypeUrl
         * @memberof Bag.ReqComposePet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqComposePet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.ReqComposePet";
        };

        return ReqComposePet;
    })();

    Bag.RespComposePet = (function() {

        /**
         * Properties of a RespComposePet.
         * @memberof Bag
         * @interface IRespComposePet
         * @property {number|null} [PetId] RespComposePet PetId
         */

        /**
         * Constructs a new RespComposePet.
         * @memberof Bag
         * @classdesc Represents a RespComposePet.
         * @implements IRespComposePet
         * @constructor
         * @param {Bag.IRespComposePet=} [properties] Properties to set
         */
        function RespComposePet(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespComposePet PetId.
         * @member {number} PetId
         * @memberof Bag.RespComposePet
         * @instance
         */
        RespComposePet.prototype.PetId = 0;

        /**
         * Creates a new RespComposePet instance using the specified properties.
         * @function create
         * @memberof Bag.RespComposePet
         * @static
         * @param {Bag.IRespComposePet=} [properties] Properties to set
         * @returns {Bag.RespComposePet} RespComposePet instance
         */
        RespComposePet.create = function create(properties) {
            return new RespComposePet(properties);
        };

        /**
         * Encodes the specified RespComposePet message. Does not implicitly {@link Bag.RespComposePet.verify|verify} messages.
         * @function encode
         * @memberof Bag.RespComposePet
         * @static
         * @param {Bag.IRespComposePet} message RespComposePet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespComposePet.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.PetId != null && Object.hasOwnProperty.call(message, "PetId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.PetId);
            return writer;
        };

        /**
         * Encodes the specified RespComposePet message, length delimited. Does not implicitly {@link Bag.RespComposePet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.RespComposePet
         * @static
         * @param {Bag.IRespComposePet} message RespComposePet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespComposePet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespComposePet message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.RespComposePet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.RespComposePet} RespComposePet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespComposePet.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.RespComposePet();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PetId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespComposePet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.RespComposePet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.RespComposePet} RespComposePet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespComposePet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespComposePet message.
         * @function verify
         * @memberof Bag.RespComposePet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespComposePet.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.PetId != null && Object.hasOwnProperty.call(message, "PetId"))
                if (!$util.isInteger(message.PetId))
                    return "PetId: integer expected";
            return null;
        };

        /**
         * Creates a RespComposePet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.RespComposePet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.RespComposePet} RespComposePet
         */
        RespComposePet.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.RespComposePet)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.RespComposePet: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.RespComposePet();
            if (object.PetId != null)
                message.PetId = object.PetId | 0;
            return message;
        };

        /**
         * Creates a plain object from a RespComposePet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.RespComposePet
         * @static
         * @param {Bag.RespComposePet} message RespComposePet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespComposePet.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.PetId = 0;
            if (message.PetId != null && Object.hasOwnProperty.call(message, "PetId"))
                object.PetId = message.PetId;
            return object;
        };

        /**
         * Converts this RespComposePet to JSON.
         * @function toJSON
         * @memberof Bag.RespComposePet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespComposePet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespComposePet
         * @function getTypeUrl
         * @memberof Bag.RespComposePet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespComposePet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.RespComposePet";
        };

        return RespComposePet;
    })();

    Bag.ReqUseItem = (function() {

        /**
         * Properties of a ReqUseItem.
         * @memberof Bag
         * @interface IReqUseItem
         * @property {number|null} [ItemId] ReqUseItem ItemId
         * @property {number|Long|null} [Count] ReqUseItem Count
         */

        /**
         * Constructs a new ReqUseItem.
         * @memberof Bag
         * @classdesc Represents a ReqUseItem.
         * @implements IReqUseItem
         * @constructor
         * @param {Bag.IReqUseItem=} [properties] Properties to set
         */
        function ReqUseItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqUseItem ItemId.
         * @member {number} ItemId
         * @memberof Bag.ReqUseItem
         * @instance
         */
        ReqUseItem.prototype.ItemId = 0;

        /**
         * ReqUseItem Count.
         * @member {number|Long} Count
         * @memberof Bag.ReqUseItem
         * @instance
         */
        ReqUseItem.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqUseItem instance using the specified properties.
         * @function create
         * @memberof Bag.ReqUseItem
         * @static
         * @param {Bag.IReqUseItem=} [properties] Properties to set
         * @returns {Bag.ReqUseItem} ReqUseItem instance
         */
        ReqUseItem.create = function create(properties) {
            return new ReqUseItem(properties);
        };

        /**
         * Encodes the specified ReqUseItem message. Does not implicitly {@link Bag.ReqUseItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.ReqUseItem
         * @static
         * @param {Bag.IReqUseItem} message ReqUseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqUseItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Count);
            return writer;
        };

        /**
         * Encodes the specified ReqUseItem message, length delimited. Does not implicitly {@link Bag.ReqUseItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.ReqUseItem
         * @static
         * @param {Bag.IReqUseItem} message ReqUseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqUseItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqUseItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.ReqUseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.ReqUseItem} ReqUseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqUseItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.ReqUseItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqUseItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.ReqUseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.ReqUseItem} ReqUseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqUseItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqUseItem message.
         * @function verify
         * @memberof Bag.ReqUseItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqUseItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqUseItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.ReqUseItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.ReqUseItem} ReqUseItem
         */
        ReqUseItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.ReqUseItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.ReqUseItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.ReqUseItem();
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqUseItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.ReqUseItem
         * @static
         * @param {Bag.ReqUseItem} message ReqUseItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqUseItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            return object;
        };

        /**
         * Converts this ReqUseItem to JSON.
         * @function toJSON
         * @memberof Bag.ReqUseItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqUseItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqUseItem
         * @function getTypeUrl
         * @memberof Bag.ReqUseItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqUseItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.ReqUseItem";
        };

        return ReqUseItem;
    })();

    Bag.RespUseItem = (function() {

        /**
         * Properties of a RespUseItem.
         * @memberof Bag
         * @interface IRespUseItem
         * @property {number|null} [ItemId] RespUseItem ItemId
         * @property {number|Long|null} [Count] RespUseItem Count
         */

        /**
         * Constructs a new RespUseItem.
         * @memberof Bag
         * @classdesc Represents a RespUseItem.
         * @implements IRespUseItem
         * @constructor
         * @param {Bag.IRespUseItem=} [properties] Properties to set
         */
        function RespUseItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespUseItem ItemId.
         * @member {number} ItemId
         * @memberof Bag.RespUseItem
         * @instance
         */
        RespUseItem.prototype.ItemId = 0;

        /**
         * RespUseItem Count.
         * @member {number|Long} Count
         * @memberof Bag.RespUseItem
         * @instance
         */
        RespUseItem.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RespUseItem instance using the specified properties.
         * @function create
         * @memberof Bag.RespUseItem
         * @static
         * @param {Bag.IRespUseItem=} [properties] Properties to set
         * @returns {Bag.RespUseItem} RespUseItem instance
         */
        RespUseItem.create = function create(properties) {
            return new RespUseItem(properties);
        };

        /**
         * Encodes the specified RespUseItem message. Does not implicitly {@link Bag.RespUseItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.RespUseItem
         * @static
         * @param {Bag.IRespUseItem} message RespUseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespUseItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Count);
            return writer;
        };

        /**
         * Encodes the specified RespUseItem message, length delimited. Does not implicitly {@link Bag.RespUseItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.RespUseItem
         * @static
         * @param {Bag.IRespUseItem} message RespUseItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespUseItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespUseItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.RespUseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.RespUseItem} RespUseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespUseItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.RespUseItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespUseItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.RespUseItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.RespUseItem} RespUseItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespUseItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespUseItem message.
         * @function verify
         * @memberof Bag.RespUseItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespUseItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            return null;
        };

        /**
         * Creates a RespUseItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.RespUseItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.RespUseItem} RespUseItem
         */
        RespUseItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.RespUseItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.RespUseItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.RespUseItem();
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RespUseItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.RespUseItem
         * @static
         * @param {Bag.RespUseItem} message RespUseItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespUseItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            return object;
        };

        /**
         * Converts this RespUseItem to JSON.
         * @function toJSON
         * @memberof Bag.RespUseItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespUseItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespUseItem
         * @function getTypeUrl
         * @memberof Bag.RespUseItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespUseItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.RespUseItem";
        };

        return RespUseItem;
    })();

    Bag.ReqDiscardItem = (function() {

        /**
         * Properties of a ReqDiscardItem.
         * @memberof Bag
         * @interface IReqDiscardItem
         * @property {number|null} [ItemId] ReqDiscardItem ItemId
         * @property {number|Long|null} [Count] ReqDiscardItem Count
         */

        /**
         * Constructs a new ReqDiscardItem.
         * @memberof Bag
         * @classdesc Represents a ReqDiscardItem.
         * @implements IReqDiscardItem
         * @constructor
         * @param {Bag.IReqDiscardItem=} [properties] Properties to set
         */
        function ReqDiscardItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqDiscardItem ItemId.
         * @member {number} ItemId
         * @memberof Bag.ReqDiscardItem
         * @instance
         */
        ReqDiscardItem.prototype.ItemId = 0;

        /**
         * ReqDiscardItem Count.
         * @member {number|Long} Count
         * @memberof Bag.ReqDiscardItem
         * @instance
         */
        ReqDiscardItem.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqDiscardItem instance using the specified properties.
         * @function create
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {Bag.IReqDiscardItem=} [properties] Properties to set
         * @returns {Bag.ReqDiscardItem} ReqDiscardItem instance
         */
        ReqDiscardItem.create = function create(properties) {
            return new ReqDiscardItem(properties);
        };

        /**
         * Encodes the specified ReqDiscardItem message. Does not implicitly {@link Bag.ReqDiscardItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {Bag.IReqDiscardItem} message ReqDiscardItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqDiscardItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Count);
            return writer;
        };

        /**
         * Encodes the specified ReqDiscardItem message, length delimited. Does not implicitly {@link Bag.ReqDiscardItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {Bag.IReqDiscardItem} message ReqDiscardItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqDiscardItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqDiscardItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.ReqDiscardItem} ReqDiscardItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqDiscardItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.ReqDiscardItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqDiscardItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.ReqDiscardItem} ReqDiscardItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqDiscardItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqDiscardItem message.
         * @function verify
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqDiscardItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqDiscardItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.ReqDiscardItem} ReqDiscardItem
         */
        ReqDiscardItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.ReqDiscardItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.ReqDiscardItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.ReqDiscardItem();
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqDiscardItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {Bag.ReqDiscardItem} message ReqDiscardItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqDiscardItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            return object;
        };

        /**
         * Converts this ReqDiscardItem to JSON.
         * @function toJSON
         * @memberof Bag.ReqDiscardItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqDiscardItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqDiscardItem
         * @function getTypeUrl
         * @memberof Bag.ReqDiscardItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqDiscardItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.ReqDiscardItem";
        };

        return ReqDiscardItem;
    })();

    Bag.RespDiscardItem = (function() {

        /**
         * Properties of a RespDiscardItem.
         * @memberof Bag
         * @interface IRespDiscardItem
         * @property {number|null} [ItemId] RespDiscardItem ItemId
         * @property {number|Long|null} [Count] RespDiscardItem Count
         */

        /**
         * Constructs a new RespDiscardItem.
         * @memberof Bag
         * @classdesc Represents a RespDiscardItem.
         * @implements IRespDiscardItem
         * @constructor
         * @param {Bag.IRespDiscardItem=} [properties] Properties to set
         */
        function RespDiscardItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespDiscardItem ItemId.
         * @member {number} ItemId
         * @memberof Bag.RespDiscardItem
         * @instance
         */
        RespDiscardItem.prototype.ItemId = 0;

        /**
         * RespDiscardItem Count.
         * @member {number|Long} Count
         * @memberof Bag.RespDiscardItem
         * @instance
         */
        RespDiscardItem.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RespDiscardItem instance using the specified properties.
         * @function create
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {Bag.IRespDiscardItem=} [properties] Properties to set
         * @returns {Bag.RespDiscardItem} RespDiscardItem instance
         */
        RespDiscardItem.create = function create(properties) {
            return new RespDiscardItem(properties);
        };

        /**
         * Encodes the specified RespDiscardItem message. Does not implicitly {@link Bag.RespDiscardItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {Bag.IRespDiscardItem} message RespDiscardItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespDiscardItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Count);
            return writer;
        };

        /**
         * Encodes the specified RespDiscardItem message, length delimited. Does not implicitly {@link Bag.RespDiscardItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {Bag.IRespDiscardItem} message RespDiscardItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespDiscardItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespDiscardItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.RespDiscardItem} RespDiscardItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespDiscardItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.RespDiscardItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespDiscardItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.RespDiscardItem} RespDiscardItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespDiscardItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespDiscardItem message.
         * @function verify
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespDiscardItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            return null;
        };

        /**
         * Creates a RespDiscardItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.RespDiscardItem} RespDiscardItem
         */
        RespDiscardItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.RespDiscardItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.RespDiscardItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.RespDiscardItem();
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RespDiscardItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {Bag.RespDiscardItem} message RespDiscardItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespDiscardItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            return object;
        };

        /**
         * Converts this RespDiscardItem to JSON.
         * @function toJSON
         * @memberof Bag.RespDiscardItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespDiscardItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespDiscardItem
         * @function getTypeUrl
         * @memberof Bag.RespDiscardItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespDiscardItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.RespDiscardItem";
        };

        return RespDiscardItem;
    })();

    Bag.ReqSellItem = (function() {

        /**
         * Properties of a ReqSellItem.
         * @memberof Bag
         * @interface IReqSellItem
         * @property {number|null} [ItemId] ReqSellItem ItemId
         */

        /**
         * Constructs a new ReqSellItem.
         * @memberof Bag
         * @classdesc Represents a ReqSellItem.
         * @implements IReqSellItem
         * @constructor
         * @param {Bag.IReqSellItem=} [properties] Properties to set
         */
        function ReqSellItem(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqSellItem ItemId.
         * @member {number} ItemId
         * @memberof Bag.ReqSellItem
         * @instance
         */
        ReqSellItem.prototype.ItemId = 0;

        /**
         * Creates a new ReqSellItem instance using the specified properties.
         * @function create
         * @memberof Bag.ReqSellItem
         * @static
         * @param {Bag.IReqSellItem=} [properties] Properties to set
         * @returns {Bag.ReqSellItem} ReqSellItem instance
         */
        ReqSellItem.create = function create(properties) {
            return new ReqSellItem(properties);
        };

        /**
         * Encodes the specified ReqSellItem message. Does not implicitly {@link Bag.ReqSellItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.ReqSellItem
         * @static
         * @param {Bag.IReqSellItem} message ReqSellItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqSellItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ItemId);
            return writer;
        };

        /**
         * Encodes the specified ReqSellItem message, length delimited. Does not implicitly {@link Bag.ReqSellItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.ReqSellItem
         * @static
         * @param {Bag.IReqSellItem} message ReqSellItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqSellItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqSellItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.ReqSellItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.ReqSellItem} ReqSellItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqSellItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.ReqSellItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ItemId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqSellItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.ReqSellItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.ReqSellItem} ReqSellItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqSellItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqSellItem message.
         * @function verify
         * @memberof Bag.ReqSellItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqSellItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            return null;
        };

        /**
         * Creates a ReqSellItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.ReqSellItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.ReqSellItem} ReqSellItem
         */
        ReqSellItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.ReqSellItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.ReqSellItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.ReqSellItem();
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReqSellItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.ReqSellItem
         * @static
         * @param {Bag.ReqSellItem} message ReqSellItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqSellItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.ItemId = 0;
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            return object;
        };

        /**
         * Converts this ReqSellItem to JSON.
         * @function toJSON
         * @memberof Bag.ReqSellItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqSellItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqSellItem
         * @function getTypeUrl
         * @memberof Bag.ReqSellItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqSellItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.ReqSellItem";
        };

        return ReqSellItem;
    })();

    Bag.RespSellItem = (function() {

        /**
         * Properties of a RespSellItem.
         * @memberof Bag
         * @interface IRespSellItem
         * @property {Object.<string,number|Long>|null} [ItemDic] RespSellItem ItemDic
         */

        /**
         * Constructs a new RespSellItem.
         * @memberof Bag
         * @classdesc Represents a RespSellItem.
         * @implements IRespSellItem
         * @constructor
         * @param {Bag.IRespSellItem=} [properties] Properties to set
         */
        function RespSellItem(properties) {
            this.ItemDic = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespSellItem ItemDic.
         * @member {Object.<string,number|Long>} ItemDic
         * @memberof Bag.RespSellItem
         * @instance
         */
        RespSellItem.prototype.ItemDic = $util.emptyObject;

        /**
         * Creates a new RespSellItem instance using the specified properties.
         * @function create
         * @memberof Bag.RespSellItem
         * @static
         * @param {Bag.IRespSellItem=} [properties] Properties to set
         * @returns {Bag.RespSellItem} RespSellItem instance
         */
        RespSellItem.create = function create(properties) {
            return new RespSellItem(properties);
        };

        /**
         * Encodes the specified RespSellItem message. Does not implicitly {@link Bag.RespSellItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.RespSellItem
         * @static
         * @param {Bag.IRespSellItem} message RespSellItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespSellItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic"))
                for (var keys = Object.keys(message.ItemDic), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int64(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.ItemDic[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespSellItem message, length delimited. Does not implicitly {@link Bag.RespSellItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.RespSellItem
         * @static
         * @param {Bag.IRespSellItem} message RespSellItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespSellItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespSellItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.RespSellItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.RespSellItem} RespSellItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespSellItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message, key, value;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.RespSellItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.ItemDic === $util.emptyObject)
                            message.ItemDic = {};
                        var end2 = reader.uint32() + reader.pos;
                        if (end2 > reader.len)
                            throw RangeError("index out of range");
                        reader.len = end2;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int64();
                                break;
                            case 2:
                                value = reader.int64();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (reader.pos !== end2)
                            throw RangeError("index out of range");
                        reader.len = end;
                        message.ItemDic[typeof key === "object" ? $util.longToHash(key) : key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespSellItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.RespSellItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.RespSellItem} RespSellItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespSellItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespSellItem message.
         * @function verify
         * @memberof Bag.RespSellItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespSellItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic")) {
                if (!$util.isObject(message.ItemDic))
                    return "ItemDic: object expected";
                var key = Object.keys(message.ItemDic);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key64Re.test(key[i]))
                        return "ItemDic: integer|Long key{k:int64} expected";
                    if (!$util.isInteger(message.ItemDic[key[i]]) && !(message.ItemDic[key[i]] && $util.isInteger(message.ItemDic[key[i]].low) && $util.isInteger(message.ItemDic[key[i]].high)))
                        return "ItemDic: integer|Long{k:int64} expected";
                }
            }
            return null;
        };

        /**
         * Creates a RespSellItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.RespSellItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.RespSellItem} RespSellItem
         */
        RespSellItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.RespSellItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.RespSellItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.RespSellItem();
            if (object.ItemDic) {
                if (!$util.isObject(object.ItemDic))
                    throw TypeError(".Bag.RespSellItem.ItemDic: object expected");
                message.ItemDic = {};
                for (var keys = Object.keys(object.ItemDic), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ItemDic, keys[i]);
                    if ($util.Long)
                        message.ItemDic[keys[i]] = $util.Long.fromValue(object.ItemDic[keys[i]], false);
                    else if (typeof object.ItemDic[keys[i]] === "string")
                        message.ItemDic[keys[i]] = parseInt(object.ItemDic[keys[i]], 10);
                    else if (typeof object.ItemDic[keys[i]] === "number")
                        message.ItemDic[keys[i]] = object.ItemDic[keys[i]];
                    else if (typeof object.ItemDic[keys[i]] === "object")
                        message.ItemDic[keys[i]] = new $util.LongBits(object.ItemDic[keys[i]].low >>> 0, object.ItemDic[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespSellItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.RespSellItem
         * @static
         * @param {Bag.RespSellItem} message RespSellItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespSellItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.ItemDic = {};
            var keys2;
            if (message.ItemDic && (keys2 = Object.keys(message.ItemDic)).length) {
                object.ItemDic = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ItemDic, keys2[j]);
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ItemDic[keys2[j]] = typeof message.ItemDic[keys2[j]] === "number" ? BigInt(message.ItemDic[keys2[j]]) : $util.Long.fromBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.ItemDic[keys2[j]] === "number")
                        object.ItemDic[keys2[j]] = options.longs === String ? String(message.ItemDic[keys2[j]]) : message.ItemDic[keys2[j]];
                    else
                        object.ItemDic[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.ItemDic[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0).toNumber() : message.ItemDic[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this RespSellItem to JSON.
         * @function toJSON
         * @memberof Bag.RespSellItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespSellItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespSellItem
         * @function getTypeUrl
         * @memberof Bag.RespSellItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespSellItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.RespSellItem";
        };

        return RespSellItem;
    })();

    Bag.ReqAddItem = (function() {

        /**
         * Properties of a ReqAddItem.
         * @memberof Bag
         * @interface IReqAddItem
         * @property {Object.<string,number|Long>|null} [ItemDic] ReqAddItem ItemDic
         */

        /**
         * Constructs a new ReqAddItem.
         * @memberof Bag
         * @classdesc Represents a ReqAddItem.
         * @implements IReqAddItem
         * @constructor
         * @param {Bag.IReqAddItem=} [properties] Properties to set
         */
        function ReqAddItem(properties) {
            this.ItemDic = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqAddItem ItemDic.
         * @member {Object.<string,number|Long>} ItemDic
         * @memberof Bag.ReqAddItem
         * @instance
         */
        ReqAddItem.prototype.ItemDic = $util.emptyObject;

        /**
         * Creates a new ReqAddItem instance using the specified properties.
         * @function create
         * @memberof Bag.ReqAddItem
         * @static
         * @param {Bag.IReqAddItem=} [properties] Properties to set
         * @returns {Bag.ReqAddItem} ReqAddItem instance
         */
        ReqAddItem.create = function create(properties) {
            return new ReqAddItem(properties);
        };

        /**
         * Encodes the specified ReqAddItem message. Does not implicitly {@link Bag.ReqAddItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.ReqAddItem
         * @static
         * @param {Bag.IReqAddItem} message ReqAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqAddItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic"))
                for (var keys = Object.keys(message.ItemDic), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.ItemDic[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ReqAddItem message, length delimited. Does not implicitly {@link Bag.ReqAddItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.ReqAddItem
         * @static
         * @param {Bag.IReqAddItem} message ReqAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqAddItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqAddItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.ReqAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.ReqAddItem} ReqAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqAddItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message, key, value;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.ReqAddItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.ItemDic === $util.emptyObject)
                            message.ItemDic = {};
                        var end2 = reader.uint32() + reader.pos;
                        if (end2 > reader.len)
                            throw RangeError("index out of range");
                        reader.len = end2;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int64();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (reader.pos !== end2)
                            throw RangeError("index out of range");
                        reader.len = end;
                        message.ItemDic[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqAddItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.ReqAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.ReqAddItem} ReqAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqAddItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqAddItem message.
         * @function verify
         * @memberof Bag.ReqAddItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqAddItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic")) {
                if (!$util.isObject(message.ItemDic))
                    return "ItemDic: object expected";
                var key = Object.keys(message.ItemDic);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "ItemDic: integer key{k:int32} expected";
                    if (!$util.isInteger(message.ItemDic[key[i]]) && !(message.ItemDic[key[i]] && $util.isInteger(message.ItemDic[key[i]].low) && $util.isInteger(message.ItemDic[key[i]].high)))
                        return "ItemDic: integer|Long{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates a ReqAddItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.ReqAddItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.ReqAddItem} ReqAddItem
         */
        ReqAddItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.ReqAddItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.ReqAddItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.ReqAddItem();
            if (object.ItemDic) {
                if (!$util.isObject(object.ItemDic))
                    throw TypeError(".Bag.ReqAddItem.ItemDic: object expected");
                message.ItemDic = {};
                for (var keys = Object.keys(object.ItemDic), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ItemDic, keys[i]);
                    if ($util.Long)
                        message.ItemDic[keys[i]] = $util.Long.fromValue(object.ItemDic[keys[i]], false);
                    else if (typeof object.ItemDic[keys[i]] === "string")
                        message.ItemDic[keys[i]] = parseInt(object.ItemDic[keys[i]], 10);
                    else if (typeof object.ItemDic[keys[i]] === "number")
                        message.ItemDic[keys[i]] = object.ItemDic[keys[i]];
                    else if (typeof object.ItemDic[keys[i]] === "object")
                        message.ItemDic[keys[i]] = new $util.LongBits(object.ItemDic[keys[i]].low >>> 0, object.ItemDic[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ReqAddItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.ReqAddItem
         * @static
         * @param {Bag.ReqAddItem} message ReqAddItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqAddItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.ItemDic = {};
            var keys2;
            if (message.ItemDic && (keys2 = Object.keys(message.ItemDic)).length) {
                object.ItemDic = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ItemDic, keys2[j]);
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ItemDic[keys2[j]] = typeof message.ItemDic[keys2[j]] === "number" ? BigInt(message.ItemDic[keys2[j]]) : $util.Long.fromBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.ItemDic[keys2[j]] === "number")
                        object.ItemDic[keys2[j]] = options.longs === String ? String(message.ItemDic[keys2[j]]) : message.ItemDic[keys2[j]];
                    else
                        object.ItemDic[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.ItemDic[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0).toNumber() : message.ItemDic[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this ReqAddItem to JSON.
         * @function toJSON
         * @memberof Bag.ReqAddItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqAddItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqAddItem
         * @function getTypeUrl
         * @memberof Bag.ReqAddItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqAddItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.ReqAddItem";
        };

        return ReqAddItem;
    })();

    Bag.RespAddItem = (function() {

        /**
         * Properties of a RespAddItem.
         * @memberof Bag
         * @interface IRespAddItem
         * @property {Object.<string,number|Long>|null} [ItemDic] RespAddItem ItemDic
         */

        /**
         * Constructs a new RespAddItem.
         * @memberof Bag
         * @classdesc Represents a RespAddItem.
         * @implements IRespAddItem
         * @constructor
         * @param {Bag.IRespAddItem=} [properties] Properties to set
         */
        function RespAddItem(properties) {
            this.ItemDic = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespAddItem ItemDic.
         * @member {Object.<string,number|Long>} ItemDic
         * @memberof Bag.RespAddItem
         * @instance
         */
        RespAddItem.prototype.ItemDic = $util.emptyObject;

        /**
         * Creates a new RespAddItem instance using the specified properties.
         * @function create
         * @memberof Bag.RespAddItem
         * @static
         * @param {Bag.IRespAddItem=} [properties] Properties to set
         * @returns {Bag.RespAddItem} RespAddItem instance
         */
        RespAddItem.create = function create(properties) {
            return new RespAddItem(properties);
        };

        /**
         * Encodes the specified RespAddItem message. Does not implicitly {@link Bag.RespAddItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.RespAddItem
         * @static
         * @param {Bag.IRespAddItem} message RespAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespAddItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic"))
                for (var keys = Object.keys(message.ItemDic), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.ItemDic[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespAddItem message, length delimited. Does not implicitly {@link Bag.RespAddItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.RespAddItem
         * @static
         * @param {Bag.IRespAddItem} message RespAddItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespAddItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespAddItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.RespAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.RespAddItem} RespAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespAddItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message, key, value;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.RespAddItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.ItemDic === $util.emptyObject)
                            message.ItemDic = {};
                        var end2 = reader.uint32() + reader.pos;
                        if (end2 > reader.len)
                            throw RangeError("index out of range");
                        reader.len = end2;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int64();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (reader.pos !== end2)
                            throw RangeError("index out of range");
                        reader.len = end;
                        message.ItemDic[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespAddItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.RespAddItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.RespAddItem} RespAddItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespAddItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespAddItem message.
         * @function verify
         * @memberof Bag.RespAddItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespAddItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic")) {
                if (!$util.isObject(message.ItemDic))
                    return "ItemDic: object expected";
                var key = Object.keys(message.ItemDic);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "ItemDic: integer key{k:int32} expected";
                    if (!$util.isInteger(message.ItemDic[key[i]]) && !(message.ItemDic[key[i]] && $util.isInteger(message.ItemDic[key[i]].low) && $util.isInteger(message.ItemDic[key[i]].high)))
                        return "ItemDic: integer|Long{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates a RespAddItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.RespAddItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.RespAddItem} RespAddItem
         */
        RespAddItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.RespAddItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.RespAddItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.RespAddItem();
            if (object.ItemDic) {
                if (!$util.isObject(object.ItemDic))
                    throw TypeError(".Bag.RespAddItem.ItemDic: object expected");
                message.ItemDic = {};
                for (var keys = Object.keys(object.ItemDic), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ItemDic, keys[i]);
                    if ($util.Long)
                        message.ItemDic[keys[i]] = $util.Long.fromValue(object.ItemDic[keys[i]], false);
                    else if (typeof object.ItemDic[keys[i]] === "string")
                        message.ItemDic[keys[i]] = parseInt(object.ItemDic[keys[i]], 10);
                    else if (typeof object.ItemDic[keys[i]] === "number")
                        message.ItemDic[keys[i]] = object.ItemDic[keys[i]];
                    else if (typeof object.ItemDic[keys[i]] === "object")
                        message.ItemDic[keys[i]] = new $util.LongBits(object.ItemDic[keys[i]].low >>> 0, object.ItemDic[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespAddItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.RespAddItem
         * @static
         * @param {Bag.RespAddItem} message RespAddItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespAddItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.ItemDic = {};
            var keys2;
            if (message.ItemDic && (keys2 = Object.keys(message.ItemDic)).length) {
                object.ItemDic = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ItemDic, keys2[j]);
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ItemDic[keys2[j]] = typeof message.ItemDic[keys2[j]] === "number" ? BigInt(message.ItemDic[keys2[j]]) : $util.Long.fromBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.ItemDic[keys2[j]] === "number")
                        object.ItemDic[keys2[j]] = options.longs === String ? String(message.ItemDic[keys2[j]]) : message.ItemDic[keys2[j]];
                    else
                        object.ItemDic[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.ItemDic[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0).toNumber() : message.ItemDic[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this RespAddItem to JSON.
         * @function toJSON
         * @memberof Bag.RespAddItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespAddItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespAddItem
         * @function getTypeUrl
         * @memberof Bag.RespAddItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespAddItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.RespAddItem";
        };

        return RespAddItem;
    })();

    Bag.ReqRemoveItem = (function() {

        /**
         * Properties of a ReqRemoveItem.
         * @memberof Bag
         * @interface IReqRemoveItem
         * @property {Object.<string,number|Long>|null} [ItemDic] ReqRemoveItem ItemDic
         */

        /**
         * Constructs a new ReqRemoveItem.
         * @memberof Bag
         * @classdesc Represents a ReqRemoveItem.
         * @implements IReqRemoveItem
         * @constructor
         * @param {Bag.IReqRemoveItem=} [properties] Properties to set
         */
        function ReqRemoveItem(properties) {
            this.ItemDic = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqRemoveItem ItemDic.
         * @member {Object.<string,number|Long>} ItemDic
         * @memberof Bag.ReqRemoveItem
         * @instance
         */
        ReqRemoveItem.prototype.ItemDic = $util.emptyObject;

        /**
         * Creates a new ReqRemoveItem instance using the specified properties.
         * @function create
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {Bag.IReqRemoveItem=} [properties] Properties to set
         * @returns {Bag.ReqRemoveItem} ReqRemoveItem instance
         */
        ReqRemoveItem.create = function create(properties) {
            return new ReqRemoveItem(properties);
        };

        /**
         * Encodes the specified ReqRemoveItem message. Does not implicitly {@link Bag.ReqRemoveItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {Bag.IReqRemoveItem} message ReqRemoveItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRemoveItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic"))
                for (var keys = Object.keys(message.ItemDic), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.ItemDic[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ReqRemoveItem message, length delimited. Does not implicitly {@link Bag.ReqRemoveItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {Bag.IReqRemoveItem} message ReqRemoveItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRemoveItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqRemoveItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.ReqRemoveItem} ReqRemoveItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRemoveItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message, key, value;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.ReqRemoveItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.ItemDic === $util.emptyObject)
                            message.ItemDic = {};
                        var end2 = reader.uint32() + reader.pos;
                        if (end2 > reader.len)
                            throw RangeError("index out of range");
                        reader.len = end2;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int64();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (reader.pos !== end2)
                            throw RangeError("index out of range");
                        reader.len = end;
                        message.ItemDic[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqRemoveItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.ReqRemoveItem} ReqRemoveItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRemoveItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqRemoveItem message.
         * @function verify
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqRemoveItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic")) {
                if (!$util.isObject(message.ItemDic))
                    return "ItemDic: object expected";
                var key = Object.keys(message.ItemDic);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "ItemDic: integer key{k:int32} expected";
                    if (!$util.isInteger(message.ItemDic[key[i]]) && !(message.ItemDic[key[i]] && $util.isInteger(message.ItemDic[key[i]].low) && $util.isInteger(message.ItemDic[key[i]].high)))
                        return "ItemDic: integer|Long{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates a ReqRemoveItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.ReqRemoveItem} ReqRemoveItem
         */
        ReqRemoveItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.ReqRemoveItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.ReqRemoveItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.ReqRemoveItem();
            if (object.ItemDic) {
                if (!$util.isObject(object.ItemDic))
                    throw TypeError(".Bag.ReqRemoveItem.ItemDic: object expected");
                message.ItemDic = {};
                for (var keys = Object.keys(object.ItemDic), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ItemDic, keys[i]);
                    if ($util.Long)
                        message.ItemDic[keys[i]] = $util.Long.fromValue(object.ItemDic[keys[i]], false);
                    else if (typeof object.ItemDic[keys[i]] === "string")
                        message.ItemDic[keys[i]] = parseInt(object.ItemDic[keys[i]], 10);
                    else if (typeof object.ItemDic[keys[i]] === "number")
                        message.ItemDic[keys[i]] = object.ItemDic[keys[i]];
                    else if (typeof object.ItemDic[keys[i]] === "object")
                        message.ItemDic[keys[i]] = new $util.LongBits(object.ItemDic[keys[i]].low >>> 0, object.ItemDic[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ReqRemoveItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {Bag.ReqRemoveItem} message ReqRemoveItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqRemoveItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.ItemDic = {};
            var keys2;
            if (message.ItemDic && (keys2 = Object.keys(message.ItemDic)).length) {
                object.ItemDic = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ItemDic, keys2[j]);
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ItemDic[keys2[j]] = typeof message.ItemDic[keys2[j]] === "number" ? BigInt(message.ItemDic[keys2[j]]) : $util.Long.fromBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.ItemDic[keys2[j]] === "number")
                        object.ItemDic[keys2[j]] = options.longs === String ? String(message.ItemDic[keys2[j]]) : message.ItemDic[keys2[j]];
                    else
                        object.ItemDic[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.ItemDic[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0).toNumber() : message.ItemDic[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this ReqRemoveItem to JSON.
         * @function toJSON
         * @memberof Bag.ReqRemoveItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqRemoveItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqRemoveItem
         * @function getTypeUrl
         * @memberof Bag.ReqRemoveItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqRemoveItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.ReqRemoveItem";
        };

        return ReqRemoveItem;
    })();

    Bag.RespRemoveItem = (function() {

        /**
         * Properties of a RespRemoveItem.
         * @memberof Bag
         * @interface IRespRemoveItem
         * @property {Object.<string,number|Long>|null} [ItemDic] RespRemoveItem ItemDic
         */

        /**
         * Constructs a new RespRemoveItem.
         * @memberof Bag
         * @classdesc Represents a RespRemoveItem.
         * @implements IRespRemoveItem
         * @constructor
         * @param {Bag.IRespRemoveItem=} [properties] Properties to set
         */
        function RespRemoveItem(properties) {
            this.ItemDic = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespRemoveItem ItemDic.
         * @member {Object.<string,number|Long>} ItemDic
         * @memberof Bag.RespRemoveItem
         * @instance
         */
        RespRemoveItem.prototype.ItemDic = $util.emptyObject;

        /**
         * Creates a new RespRemoveItem instance using the specified properties.
         * @function create
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {Bag.IRespRemoveItem=} [properties] Properties to set
         * @returns {Bag.RespRemoveItem} RespRemoveItem instance
         */
        RespRemoveItem.create = function create(properties) {
            return new RespRemoveItem(properties);
        };

        /**
         * Encodes the specified RespRemoveItem message. Does not implicitly {@link Bag.RespRemoveItem.verify|verify} messages.
         * @function encode
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {Bag.IRespRemoveItem} message RespRemoveItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRemoveItem.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic"))
                for (var keys = Object.keys(message.ItemDic), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 0 =*/16).int64(message.ItemDic[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespRemoveItem message, length delimited. Does not implicitly {@link Bag.RespRemoveItem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {Bag.IRespRemoveItem} message RespRemoveItem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRemoveItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespRemoveItem message from the specified reader or buffer.
         * @function decode
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Bag.RespRemoveItem} RespRemoveItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRemoveItem.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message, key, value;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Bag.RespRemoveItem();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.ItemDic === $util.emptyObject)
                            message.ItemDic = {};
                        var end2 = reader.uint32() + reader.pos;
                        if (end2 > reader.len)
                            throw RangeError("index out of range");
                        reader.len = end2;
                        key = 0;
                        value = 0;
                        while (reader.pos < end2) {
                            var tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.int64();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (reader.pos !== end2)
                            throw RangeError("index out of range");
                        reader.len = end;
                        message.ItemDic[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespRemoveItem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Bag.RespRemoveItem} RespRemoveItem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRemoveItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespRemoveItem message.
         * @function verify
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespRemoveItem.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ItemDic != null && Object.hasOwnProperty.call(message, "ItemDic")) {
                if (!$util.isObject(message.ItemDic))
                    return "ItemDic: object expected";
                var key = Object.keys(message.ItemDic);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "ItemDic: integer key{k:int32} expected";
                    if (!$util.isInteger(message.ItemDic[key[i]]) && !(message.ItemDic[key[i]] && $util.isInteger(message.ItemDic[key[i]].low) && $util.isInteger(message.ItemDic[key[i]].high)))
                        return "ItemDic: integer|Long{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates a RespRemoveItem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Bag.RespRemoveItem} RespRemoveItem
         */
        RespRemoveItem.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Bag.RespRemoveItem)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Bag.RespRemoveItem: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Bag.RespRemoveItem();
            if (object.ItemDic) {
                if (!$util.isObject(object.ItemDic))
                    throw TypeError(".Bag.RespRemoveItem.ItemDic: object expected");
                message.ItemDic = {};
                for (var keys = Object.keys(object.ItemDic), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.ItemDic, keys[i]);
                    if ($util.Long)
                        message.ItemDic[keys[i]] = $util.Long.fromValue(object.ItemDic[keys[i]], false);
                    else if (typeof object.ItemDic[keys[i]] === "string")
                        message.ItemDic[keys[i]] = parseInt(object.ItemDic[keys[i]], 10);
                    else if (typeof object.ItemDic[keys[i]] === "number")
                        message.ItemDic[keys[i]] = object.ItemDic[keys[i]];
                    else if (typeof object.ItemDic[keys[i]] === "object")
                        message.ItemDic[keys[i]] = new $util.LongBits(object.ItemDic[keys[i]].low >>> 0, object.ItemDic[keys[i]].high >>> 0).toNumber();
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespRemoveItem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {Bag.RespRemoveItem} message RespRemoveItem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespRemoveItem.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.ItemDic = {};
            var keys2;
            if (message.ItemDic && (keys2 = Object.keys(message.ItemDic)).length) {
                object.ItemDic = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.ItemDic, keys2[j]);
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ItemDic[keys2[j]] = typeof message.ItemDic[keys2[j]] === "number" ? BigInt(message.ItemDic[keys2[j]]) : $util.Long.fromBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0, false).toBigInt();
                    else if (typeof message.ItemDic[keys2[j]] === "number")
                        object.ItemDic[keys2[j]] = options.longs === String ? String(message.ItemDic[keys2[j]]) : message.ItemDic[keys2[j]];
                    else
                        object.ItemDic[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.ItemDic[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.ItemDic[keys2[j]].low >>> 0, message.ItemDic[keys2[j]].high >>> 0).toNumber() : message.ItemDic[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this RespRemoveItem to JSON.
         * @function toJSON
         * @memberof Bag.RespRemoveItem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespRemoveItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespRemoveItem
         * @function getTypeUrl
         * @memberof Bag.RespRemoveItem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespRemoveItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Bag.RespRemoveItem";
        };

        return RespRemoveItem;
    })();

    return Bag;
})();

$root.Social = (function() {

    /**
     * Namespace Social.
     * @exports Social
     * @namespace
     */
    var Social = {};

    Social.FriendInfo = (function() {

        /**
         * Properties of a FriendInfo.
         * @memberof Social
         * @interface IFriendInfo
         * @property {number|Long|null} [PlayerId] FriendInfo PlayerId
         * @property {string|null} [PlayerName] FriendInfo PlayerName
         */

        /**
         * Constructs a new FriendInfo.
         * @memberof Social
         * @classdesc Represents a FriendInfo.
         * @implements IFriendInfo
         * @constructor
         * @param {Social.IFriendInfo=} [properties] Properties to set
         */
        function FriendInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FriendInfo PlayerId.
         * @member {number|Long} PlayerId
         * @memberof Social.FriendInfo
         * @instance
         */
        FriendInfo.prototype.PlayerId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * FriendInfo PlayerName.
         * @member {string} PlayerName
         * @memberof Social.FriendInfo
         * @instance
         */
        FriendInfo.prototype.PlayerName = "";

        /**
         * Creates a new FriendInfo instance using the specified properties.
         * @function create
         * @memberof Social.FriendInfo
         * @static
         * @param {Social.IFriendInfo=} [properties] Properties to set
         * @returns {Social.FriendInfo} FriendInfo instance
         */
        FriendInfo.create = function create(properties) {
            return new FriendInfo(properties);
        };

        /**
         * Encodes the specified FriendInfo message. Does not implicitly {@link Social.FriendInfo.verify|verify} messages.
         * @function encode
         * @memberof Social.FriendInfo
         * @static
         * @param {Social.IFriendInfo} message FriendInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FriendInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.PlayerId);
            if (message.PlayerName != null && Object.hasOwnProperty.call(message, "PlayerName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.PlayerName);
            return writer;
        };

        /**
         * Encodes the specified FriendInfo message, length delimited. Does not implicitly {@link Social.FriendInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.FriendInfo
         * @static
         * @param {Social.IFriendInfo} message FriendInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FriendInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FriendInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Social.FriendInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.FriendInfo} FriendInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FriendInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.FriendInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlayerId = reader.int64();
                        break;
                    }
                case 2: {
                        message.PlayerName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a FriendInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.FriendInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.FriendInfo} FriendInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FriendInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FriendInfo message.
         * @function verify
         * @memberof Social.FriendInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FriendInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                if (!$util.isInteger(message.PlayerId) && !(message.PlayerId && $util.isInteger(message.PlayerId.low) && $util.isInteger(message.PlayerId.high)))
                    return "PlayerId: integer|Long expected";
            if (message.PlayerName != null && Object.hasOwnProperty.call(message, "PlayerName"))
                if (!$util.isString(message.PlayerName))
                    return "PlayerName: string expected";
            return null;
        };

        /**
         * Creates a FriendInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.FriendInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.FriendInfo} FriendInfo
         */
        FriendInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.FriendInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Social.FriendInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Social.FriendInfo();
            if (object.PlayerId != null)
                if ($util.Long)
                    message.PlayerId = $util.Long.fromValue(object.PlayerId, false);
                else if (typeof object.PlayerId === "string")
                    message.PlayerId = parseInt(object.PlayerId, 10);
                else if (typeof object.PlayerId === "number")
                    message.PlayerId = object.PlayerId;
                else if (typeof object.PlayerId === "object")
                    message.PlayerId = new $util.LongBits(object.PlayerId.low >>> 0, object.PlayerId.high >>> 0).toNumber();
            if (object.PlayerName != null)
                message.PlayerName = String(object.PlayerName);
            return message;
        };

        /**
         * Creates a plain object from a FriendInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.FriendInfo
         * @static
         * @param {Social.FriendInfo} message FriendInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FriendInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.PlayerId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.PlayerId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.PlayerName = "";
            }
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.PlayerId = typeof message.PlayerId === "number" ? BigInt(message.PlayerId) : $util.Long.fromBits(message.PlayerId.low >>> 0, message.PlayerId.high >>> 0, false).toBigInt();
                else if (typeof message.PlayerId === "number")
                    object.PlayerId = options.longs === String ? String(message.PlayerId) : message.PlayerId;
                else
                    object.PlayerId = options.longs === String ? $util.Long.prototype.toString.call(message.PlayerId) : options.longs === Number ? new $util.LongBits(message.PlayerId.low >>> 0, message.PlayerId.high >>> 0).toNumber() : message.PlayerId;
            if (message.PlayerName != null && Object.hasOwnProperty.call(message, "PlayerName"))
                object.PlayerName = message.PlayerName;
            return object;
        };

        /**
         * Converts this FriendInfo to JSON.
         * @function toJSON
         * @memberof Social.FriendInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FriendInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FriendInfo
         * @function getTypeUrl
         * @memberof Social.FriendInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FriendInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.FriendInfo";
        };

        return FriendInfo;
    })();

    Social.ReqSocialInfo = (function() {

        /**
         * Properties of a ReqSocialInfo.
         * @memberof Social
         * @interface IReqSocialInfo
         */

        /**
         * Constructs a new ReqSocialInfo.
         * @memberof Social
         * @classdesc Represents a ReqSocialInfo.
         * @implements IReqSocialInfo
         * @constructor
         * @param {Social.IReqSocialInfo=} [properties] Properties to set
         */
        function ReqSocialInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new ReqSocialInfo instance using the specified properties.
         * @function create
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {Social.IReqSocialInfo=} [properties] Properties to set
         * @returns {Social.ReqSocialInfo} ReqSocialInfo instance
         */
        ReqSocialInfo.create = function create(properties) {
            return new ReqSocialInfo(properties);
        };

        /**
         * Encodes the specified ReqSocialInfo message. Does not implicitly {@link Social.ReqSocialInfo.verify|verify} messages.
         * @function encode
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {Social.IReqSocialInfo} message ReqSocialInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqSocialInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            return writer;
        };

        /**
         * Encodes the specified ReqSocialInfo message, length delimited. Does not implicitly {@link Social.ReqSocialInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {Social.IReqSocialInfo} message ReqSocialInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqSocialInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqSocialInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.ReqSocialInfo} ReqSocialInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqSocialInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.ReqSocialInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqSocialInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.ReqSocialInfo} ReqSocialInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqSocialInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqSocialInfo message.
         * @function verify
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqSocialInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            return null;
        };

        /**
         * Creates a ReqSocialInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.ReqSocialInfo} ReqSocialInfo
         */
        ReqSocialInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.ReqSocialInfo)
                return object;
            return new $root.Social.ReqSocialInfo();
        };

        /**
         * Creates a plain object from a ReqSocialInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {Social.ReqSocialInfo} message ReqSocialInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqSocialInfo.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this ReqSocialInfo to JSON.
         * @function toJSON
         * @memberof Social.ReqSocialInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqSocialInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqSocialInfo
         * @function getTypeUrl
         * @memberof Social.ReqSocialInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqSocialInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.ReqSocialInfo";
        };

        return ReqSocialInfo;
    })();

    Social.RespSocialInfo = (function() {

        /**
         * Properties of a RespSocialInfo.
         * @memberof Social
         * @interface IRespSocialInfo
         */

        /**
         * Constructs a new RespSocialInfo.
         * @memberof Social
         * @classdesc Represents a RespSocialInfo.
         * @implements IRespSocialInfo
         * @constructor
         * @param {Social.IRespSocialInfo=} [properties] Properties to set
         */
        function RespSocialInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new RespSocialInfo instance using the specified properties.
         * @function create
         * @memberof Social.RespSocialInfo
         * @static
         * @param {Social.IRespSocialInfo=} [properties] Properties to set
         * @returns {Social.RespSocialInfo} RespSocialInfo instance
         */
        RespSocialInfo.create = function create(properties) {
            return new RespSocialInfo(properties);
        };

        /**
         * Encodes the specified RespSocialInfo message. Does not implicitly {@link Social.RespSocialInfo.verify|verify} messages.
         * @function encode
         * @memberof Social.RespSocialInfo
         * @static
         * @param {Social.IRespSocialInfo} message RespSocialInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespSocialInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            return writer;
        };

        /**
         * Encodes the specified RespSocialInfo message, length delimited. Does not implicitly {@link Social.RespSocialInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.RespSocialInfo
         * @static
         * @param {Social.IRespSocialInfo} message RespSocialInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespSocialInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespSocialInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Social.RespSocialInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.RespSocialInfo} RespSocialInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespSocialInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.RespSocialInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespSocialInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.RespSocialInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.RespSocialInfo} RespSocialInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespSocialInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespSocialInfo message.
         * @function verify
         * @memberof Social.RespSocialInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespSocialInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            return null;
        };

        /**
         * Creates a RespSocialInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.RespSocialInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.RespSocialInfo} RespSocialInfo
         */
        RespSocialInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.RespSocialInfo)
                return object;
            return new $root.Social.RespSocialInfo();
        };

        /**
         * Creates a plain object from a RespSocialInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.RespSocialInfo
         * @static
         * @param {Social.RespSocialInfo} message RespSocialInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespSocialInfo.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this RespSocialInfo to JSON.
         * @function toJSON
         * @memberof Social.RespSocialInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespSocialInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespSocialInfo
         * @function getTypeUrl
         * @memberof Social.RespSocialInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespSocialInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.RespSocialInfo";
        };

        return RespSocialInfo;
    })();

    Social.ReqDeleteFriend = (function() {

        /**
         * Properties of a ReqDeleteFriend.
         * @memberof Social
         * @interface IReqDeleteFriend
         * @property {number|Long|null} [PlayerId] ReqDeleteFriend PlayerId
         */

        /**
         * Constructs a new ReqDeleteFriend.
         * @memberof Social
         * @classdesc Represents a ReqDeleteFriend.
         * @implements IReqDeleteFriend
         * @constructor
         * @param {Social.IReqDeleteFriend=} [properties] Properties to set
         */
        function ReqDeleteFriend(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqDeleteFriend PlayerId.
         * @member {number|Long} PlayerId
         * @memberof Social.ReqDeleteFriend
         * @instance
         */
        ReqDeleteFriend.prototype.PlayerId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqDeleteFriend instance using the specified properties.
         * @function create
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {Social.IReqDeleteFriend=} [properties] Properties to set
         * @returns {Social.ReqDeleteFriend} ReqDeleteFriend instance
         */
        ReqDeleteFriend.create = function create(properties) {
            return new ReqDeleteFriend(properties);
        };

        /**
         * Encodes the specified ReqDeleteFriend message. Does not implicitly {@link Social.ReqDeleteFriend.verify|verify} messages.
         * @function encode
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {Social.IReqDeleteFriend} message ReqDeleteFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqDeleteFriend.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.PlayerId);
            return writer;
        };

        /**
         * Encodes the specified ReqDeleteFriend message, length delimited. Does not implicitly {@link Social.ReqDeleteFriend.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {Social.IReqDeleteFriend} message ReqDeleteFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqDeleteFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqDeleteFriend message from the specified reader or buffer.
         * @function decode
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.ReqDeleteFriend} ReqDeleteFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqDeleteFriend.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.ReqDeleteFriend();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlayerId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqDeleteFriend message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.ReqDeleteFriend} ReqDeleteFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqDeleteFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqDeleteFriend message.
         * @function verify
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqDeleteFriend.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                if (!$util.isInteger(message.PlayerId) && !(message.PlayerId && $util.isInteger(message.PlayerId.low) && $util.isInteger(message.PlayerId.high)))
                    return "PlayerId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqDeleteFriend message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.ReqDeleteFriend} ReqDeleteFriend
         */
        ReqDeleteFriend.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.ReqDeleteFriend)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Social.ReqDeleteFriend: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Social.ReqDeleteFriend();
            if (object.PlayerId != null)
                if ($util.Long)
                    message.PlayerId = $util.Long.fromValue(object.PlayerId, false);
                else if (typeof object.PlayerId === "string")
                    message.PlayerId = parseInt(object.PlayerId, 10);
                else if (typeof object.PlayerId === "number")
                    message.PlayerId = object.PlayerId;
                else if (typeof object.PlayerId === "object")
                    message.PlayerId = new $util.LongBits(object.PlayerId.low >>> 0, object.PlayerId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqDeleteFriend message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {Social.ReqDeleteFriend} message ReqDeleteFriend
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqDeleteFriend.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.PlayerId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.PlayerId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.PlayerId = typeof message.PlayerId === "number" ? BigInt(message.PlayerId) : $util.Long.fromBits(message.PlayerId.low >>> 0, message.PlayerId.high >>> 0, false).toBigInt();
                else if (typeof message.PlayerId === "number")
                    object.PlayerId = options.longs === String ? String(message.PlayerId) : message.PlayerId;
                else
                    object.PlayerId = options.longs === String ? $util.Long.prototype.toString.call(message.PlayerId) : options.longs === Number ? new $util.LongBits(message.PlayerId.low >>> 0, message.PlayerId.high >>> 0).toNumber() : message.PlayerId;
            return object;
        };

        /**
         * Converts this ReqDeleteFriend to JSON.
         * @function toJSON
         * @memberof Social.ReqDeleteFriend
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqDeleteFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqDeleteFriend
         * @function getTypeUrl
         * @memberof Social.ReqDeleteFriend
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqDeleteFriend.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.ReqDeleteFriend";
        };

        return ReqDeleteFriend;
    })();

    Social.RespDeleteFriend = (function() {

        /**
         * Properties of a RespDeleteFriend.
         * @memberof Social
         * @interface IRespDeleteFriend
         * @property {boolean|null} [Success] RespDeleteFriend Success
         */

        /**
         * Constructs a new RespDeleteFriend.
         * @memberof Social
         * @classdesc Represents a RespDeleteFriend.
         * @implements IRespDeleteFriend
         * @constructor
         * @param {Social.IRespDeleteFriend=} [properties] Properties to set
         */
        function RespDeleteFriend(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespDeleteFriend Success.
         * @member {boolean} Success
         * @memberof Social.RespDeleteFriend
         * @instance
         */
        RespDeleteFriend.prototype.Success = false;

        /**
         * Creates a new RespDeleteFriend instance using the specified properties.
         * @function create
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {Social.IRespDeleteFriend=} [properties] Properties to set
         * @returns {Social.RespDeleteFriend} RespDeleteFriend instance
         */
        RespDeleteFriend.create = function create(properties) {
            return new RespDeleteFriend(properties);
        };

        /**
         * Encodes the specified RespDeleteFriend message. Does not implicitly {@link Social.RespDeleteFriend.verify|verify} messages.
         * @function encode
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {Social.IRespDeleteFriend} message RespDeleteFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespDeleteFriend.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.Success);
            return writer;
        };

        /**
         * Encodes the specified RespDeleteFriend message, length delimited. Does not implicitly {@link Social.RespDeleteFriend.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {Social.IRespDeleteFriend} message RespDeleteFriend message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespDeleteFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespDeleteFriend message from the specified reader or buffer.
         * @function decode
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.RespDeleteFriend} RespDeleteFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespDeleteFriend.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.RespDeleteFriend();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Success = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespDeleteFriend message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.RespDeleteFriend} RespDeleteFriend
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespDeleteFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespDeleteFriend message.
         * @function verify
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespDeleteFriend.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                if (typeof message.Success !== "boolean")
                    return "Success: boolean expected";
            return null;
        };

        /**
         * Creates a RespDeleteFriend message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.RespDeleteFriend} RespDeleteFriend
         */
        RespDeleteFriend.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.RespDeleteFriend)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Social.RespDeleteFriend: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Social.RespDeleteFriend();
            if (object.Success != null)
                message.Success = Boolean(object.Success);
            return message;
        };

        /**
         * Creates a plain object from a RespDeleteFriend message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {Social.RespDeleteFriend} message RespDeleteFriend
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespDeleteFriend.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.Success = false;
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                object.Success = message.Success;
            return object;
        };

        /**
         * Converts this RespDeleteFriend to JSON.
         * @function toJSON
         * @memberof Social.RespDeleteFriend
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespDeleteFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespDeleteFriend
         * @function getTypeUrl
         * @memberof Social.RespDeleteFriend
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespDeleteFriend.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.RespDeleteFriend";
        };

        return RespDeleteFriend;
    })();

    Social.ReqFriendList = (function() {

        /**
         * Properties of a ReqFriendList.
         * @memberof Social
         * @interface IReqFriendList
         */

        /**
         * Constructs a new ReqFriendList.
         * @memberof Social
         * @classdesc Represents a ReqFriendList.
         * @implements IReqFriendList
         * @constructor
         * @param {Social.IReqFriendList=} [properties] Properties to set
         */
        function ReqFriendList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new ReqFriendList instance using the specified properties.
         * @function create
         * @memberof Social.ReqFriendList
         * @static
         * @param {Social.IReqFriendList=} [properties] Properties to set
         * @returns {Social.ReqFriendList} ReqFriendList instance
         */
        ReqFriendList.create = function create(properties) {
            return new ReqFriendList(properties);
        };

        /**
         * Encodes the specified ReqFriendList message. Does not implicitly {@link Social.ReqFriendList.verify|verify} messages.
         * @function encode
         * @memberof Social.ReqFriendList
         * @static
         * @param {Social.IReqFriendList} message ReqFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqFriendList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            return writer;
        };

        /**
         * Encodes the specified ReqFriendList message, length delimited. Does not implicitly {@link Social.ReqFriendList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.ReqFriendList
         * @static
         * @param {Social.IReqFriendList} message ReqFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqFriendList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqFriendList message from the specified reader or buffer.
         * @function decode
         * @memberof Social.ReqFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.ReqFriendList} ReqFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqFriendList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.ReqFriendList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqFriendList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.ReqFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.ReqFriendList} ReqFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqFriendList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqFriendList message.
         * @function verify
         * @memberof Social.ReqFriendList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqFriendList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            return null;
        };

        /**
         * Creates a ReqFriendList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.ReqFriendList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.ReqFriendList} ReqFriendList
         */
        ReqFriendList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.ReqFriendList)
                return object;
            return new $root.Social.ReqFriendList();
        };

        /**
         * Creates a plain object from a ReqFriendList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.ReqFriendList
         * @static
         * @param {Social.ReqFriendList} message ReqFriendList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqFriendList.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this ReqFriendList to JSON.
         * @function toJSON
         * @memberof Social.ReqFriendList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqFriendList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqFriendList
         * @function getTypeUrl
         * @memberof Social.ReqFriendList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqFriendList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.ReqFriendList";
        };

        return ReqFriendList;
    })();

    Social.RespFriendList = (function() {

        /**
         * Properties of a RespFriendList.
         * @memberof Social
         * @interface IRespFriendList
         * @property {Array.<Social.IFriendInfo>|null} [Friends] RespFriendList Friends
         */

        /**
         * Constructs a new RespFriendList.
         * @memberof Social
         * @classdesc Represents a RespFriendList.
         * @implements IRespFriendList
         * @constructor
         * @param {Social.IRespFriendList=} [properties] Properties to set
         */
        function RespFriendList(properties) {
            this.Friends = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespFriendList Friends.
         * @member {Array.<Social.IFriendInfo>} Friends
         * @memberof Social.RespFriendList
         * @instance
         */
        RespFriendList.prototype.Friends = $util.emptyArray;

        /**
         * Creates a new RespFriendList instance using the specified properties.
         * @function create
         * @memberof Social.RespFriendList
         * @static
         * @param {Social.IRespFriendList=} [properties] Properties to set
         * @returns {Social.RespFriendList} RespFriendList instance
         */
        RespFriendList.create = function create(properties) {
            return new RespFriendList(properties);
        };

        /**
         * Encodes the specified RespFriendList message. Does not implicitly {@link Social.RespFriendList.verify|verify} messages.
         * @function encode
         * @memberof Social.RespFriendList
         * @static
         * @param {Social.IRespFriendList} message RespFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespFriendList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Friends != null && message.Friends.length)
                for (var i = 0; i < message.Friends.length; ++i)
                    $root.Social.FriendInfo.encode(message.Friends[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespFriendList message, length delimited. Does not implicitly {@link Social.RespFriendList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.RespFriendList
         * @static
         * @param {Social.IRespFriendList} message RespFriendList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespFriendList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespFriendList message from the specified reader or buffer.
         * @function decode
         * @memberof Social.RespFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.RespFriendList} RespFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespFriendList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.RespFriendList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Friends && message.Friends.length))
                            message.Friends = [];
                        message.Friends.push($root.Social.FriendInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespFriendList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.RespFriendList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.RespFriendList} RespFriendList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespFriendList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespFriendList message.
         * @function verify
         * @memberof Social.RespFriendList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespFriendList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Friends != null && Object.hasOwnProperty.call(message, "Friends")) {
                if (!Array.isArray(message.Friends))
                    return "Friends: array expected";
                for (var i = 0; i < message.Friends.length; ++i) {
                    var error = $root.Social.FriendInfo.verify(message.Friends[i], long + 1);
                    if (error)
                        return "Friends." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RespFriendList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.RespFriendList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.RespFriendList} RespFriendList
         */
        RespFriendList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.RespFriendList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Social.RespFriendList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Social.RespFriendList();
            if (object.Friends) {
                if (!Array.isArray(object.Friends))
                    throw TypeError(".Social.RespFriendList.Friends: array expected");
                message.Friends = [];
                for (var i = 0; i < object.Friends.length; ++i) {
                    if (!$util.isObject(object.Friends[i]))
                        throw TypeError(".Social.RespFriendList.Friends: object expected");
                    message.Friends[i] = $root.Social.FriendInfo.fromObject(object.Friends[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespFriendList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.RespFriendList
         * @static
         * @param {Social.RespFriendList} message RespFriendList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespFriendList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Friends = [];
            if (message.Friends && message.Friends.length) {
                object.Friends = [];
                for (var j = 0; j < message.Friends.length; ++j)
                    object.Friends[j] = $root.Social.FriendInfo.toObject(message.Friends[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this RespFriendList to JSON.
         * @function toJSON
         * @memberof Social.RespFriendList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespFriendList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespFriendList
         * @function getTypeUrl
         * @memberof Social.RespFriendList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespFriendList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.RespFriendList";
        };

        return RespFriendList;
    })();

    Social.ReqFriendByAdd = (function() {

        /**
         * Properties of a ReqFriendByAdd.
         * @memberof Social
         * @interface IReqFriendByAdd
         * @property {number|Long|null} [PlayerId] ReqFriendByAdd PlayerId
         */

        /**
         * Constructs a new ReqFriendByAdd.
         * @memberof Social
         * @classdesc Represents a ReqFriendByAdd.
         * @implements IReqFriendByAdd
         * @constructor
         * @param {Social.IReqFriendByAdd=} [properties] Properties to set
         */
        function ReqFriendByAdd(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqFriendByAdd PlayerId.
         * @member {number|Long} PlayerId
         * @memberof Social.ReqFriendByAdd
         * @instance
         */
        ReqFriendByAdd.prototype.PlayerId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqFriendByAdd instance using the specified properties.
         * @function create
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {Social.IReqFriendByAdd=} [properties] Properties to set
         * @returns {Social.ReqFriendByAdd} ReqFriendByAdd instance
         */
        ReqFriendByAdd.create = function create(properties) {
            return new ReqFriendByAdd(properties);
        };

        /**
         * Encodes the specified ReqFriendByAdd message. Does not implicitly {@link Social.ReqFriendByAdd.verify|verify} messages.
         * @function encode
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {Social.IReqFriendByAdd} message ReqFriendByAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqFriendByAdd.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.PlayerId);
            return writer;
        };

        /**
         * Encodes the specified ReqFriendByAdd message, length delimited. Does not implicitly {@link Social.ReqFriendByAdd.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {Social.IReqFriendByAdd} message ReqFriendByAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqFriendByAdd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqFriendByAdd message from the specified reader or buffer.
         * @function decode
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.ReqFriendByAdd} ReqFriendByAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqFriendByAdd.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.ReqFriendByAdd();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlayerId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqFriendByAdd message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.ReqFriendByAdd} ReqFriendByAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqFriendByAdd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqFriendByAdd message.
         * @function verify
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqFriendByAdd.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                if (!$util.isInteger(message.PlayerId) && !(message.PlayerId && $util.isInteger(message.PlayerId.low) && $util.isInteger(message.PlayerId.high)))
                    return "PlayerId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqFriendByAdd message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.ReqFriendByAdd} ReqFriendByAdd
         */
        ReqFriendByAdd.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.ReqFriendByAdd)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Social.ReqFriendByAdd: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Social.ReqFriendByAdd();
            if (object.PlayerId != null)
                if ($util.Long)
                    message.PlayerId = $util.Long.fromValue(object.PlayerId, false);
                else if (typeof object.PlayerId === "string")
                    message.PlayerId = parseInt(object.PlayerId, 10);
                else if (typeof object.PlayerId === "number")
                    message.PlayerId = object.PlayerId;
                else if (typeof object.PlayerId === "object")
                    message.PlayerId = new $util.LongBits(object.PlayerId.low >>> 0, object.PlayerId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqFriendByAdd message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {Social.ReqFriendByAdd} message ReqFriendByAdd
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqFriendByAdd.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.PlayerId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.PlayerId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.PlayerId != null && Object.hasOwnProperty.call(message, "PlayerId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.PlayerId = typeof message.PlayerId === "number" ? BigInt(message.PlayerId) : $util.Long.fromBits(message.PlayerId.low >>> 0, message.PlayerId.high >>> 0, false).toBigInt();
                else if (typeof message.PlayerId === "number")
                    object.PlayerId = options.longs === String ? String(message.PlayerId) : message.PlayerId;
                else
                    object.PlayerId = options.longs === String ? $util.Long.prototype.toString.call(message.PlayerId) : options.longs === Number ? new $util.LongBits(message.PlayerId.low >>> 0, message.PlayerId.high >>> 0).toNumber() : message.PlayerId;
            return object;
        };

        /**
         * Converts this ReqFriendByAdd to JSON.
         * @function toJSON
         * @memberof Social.ReqFriendByAdd
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqFriendByAdd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqFriendByAdd
         * @function getTypeUrl
         * @memberof Social.ReqFriendByAdd
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqFriendByAdd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.ReqFriendByAdd";
        };

        return ReqFriendByAdd;
    })();

    Social.RespFriendByAdd = (function() {

        /**
         * Properties of a RespFriendByAdd.
         * @memberof Social
         * @interface IRespFriendByAdd
         * @property {boolean|null} [Success] RespFriendByAdd Success
         */

        /**
         * Constructs a new RespFriendByAdd.
         * @memberof Social
         * @classdesc Represents a RespFriendByAdd.
         * @implements IRespFriendByAdd
         * @constructor
         * @param {Social.IRespFriendByAdd=} [properties] Properties to set
         */
        function RespFriendByAdd(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespFriendByAdd Success.
         * @member {boolean} Success
         * @memberof Social.RespFriendByAdd
         * @instance
         */
        RespFriendByAdd.prototype.Success = false;

        /**
         * Creates a new RespFriendByAdd instance using the specified properties.
         * @function create
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {Social.IRespFriendByAdd=} [properties] Properties to set
         * @returns {Social.RespFriendByAdd} RespFriendByAdd instance
         */
        RespFriendByAdd.create = function create(properties) {
            return new RespFriendByAdd(properties);
        };

        /**
         * Encodes the specified RespFriendByAdd message. Does not implicitly {@link Social.RespFriendByAdd.verify|verify} messages.
         * @function encode
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {Social.IRespFriendByAdd} message RespFriendByAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespFriendByAdd.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.Success);
            return writer;
        };

        /**
         * Encodes the specified RespFriendByAdd message, length delimited. Does not implicitly {@link Social.RespFriendByAdd.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {Social.IRespFriendByAdd} message RespFriendByAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespFriendByAdd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespFriendByAdd message from the specified reader or buffer.
         * @function decode
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Social.RespFriendByAdd} RespFriendByAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespFriendByAdd.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Social.RespFriendByAdd();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Success = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespFriendByAdd message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Social.RespFriendByAdd} RespFriendByAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespFriendByAdd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespFriendByAdd message.
         * @function verify
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespFriendByAdd.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                if (typeof message.Success !== "boolean")
                    return "Success: boolean expected";
            return null;
        };

        /**
         * Creates a RespFriendByAdd message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Social.RespFriendByAdd} RespFriendByAdd
         */
        RespFriendByAdd.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Social.RespFriendByAdd)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Social.RespFriendByAdd: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Social.RespFriendByAdd();
            if (object.Success != null)
                message.Success = Boolean(object.Success);
            return message;
        };

        /**
         * Creates a plain object from a RespFriendByAdd message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {Social.RespFriendByAdd} message RespFriendByAdd
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespFriendByAdd.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.Success = false;
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                object.Success = message.Success;
            return object;
        };

        /**
         * Converts this RespFriendByAdd to JSON.
         * @function toJSON
         * @memberof Social.RespFriendByAdd
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespFriendByAdd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespFriendByAdd
         * @function getTypeUrl
         * @memberof Social.RespFriendByAdd
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespFriendByAdd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Social.RespFriendByAdd";
        };

        return RespFriendByAdd;
    })();

    return Social;
})();

$root.User = (function() {

    /**
     * Namespace User.
     * @exports User
     * @namespace
     */
    var User = {};

    User.ReqLogin = (function() {

        /**
         * Properties of a ReqLogin.
         * @memberof User
         * @interface IReqLogin
         * @property {string|null} [UserName] ReqLogin UserName
         * @property {string|null} [Platform] ReqLogin Platform
         * @property {number|null} [SdkType] ReqLogin SdkType
         * @property {string|null} [SdkToken] ReqLogin SdkToken
         * @property {string|null} [Device] ReqLogin Device
         * @property {string|null} [Password] ReqLogin Password
         */

        /**
         * Constructs a new ReqLogin.
         * @memberof User
         * @classdesc Represents a ReqLogin.
         * @implements IReqLogin
         * @constructor
         * @param {User.IReqLogin=} [properties] Properties to set
         */
        function ReqLogin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqLogin UserName.
         * @member {string} UserName
         * @memberof User.ReqLogin
         * @instance
         */
        ReqLogin.prototype.UserName = "";

        /**
         * ReqLogin Platform.
         * @member {string} Platform
         * @memberof User.ReqLogin
         * @instance
         */
        ReqLogin.prototype.Platform = "";

        /**
         * ReqLogin SdkType.
         * @member {number} SdkType
         * @memberof User.ReqLogin
         * @instance
         */
        ReqLogin.prototype.SdkType = 0;

        /**
         * ReqLogin SdkToken.
         * @member {string} SdkToken
         * @memberof User.ReqLogin
         * @instance
         */
        ReqLogin.prototype.SdkToken = "";

        /**
         * ReqLogin Device.
         * @member {string} Device
         * @memberof User.ReqLogin
         * @instance
         */
        ReqLogin.prototype.Device = "";

        /**
         * ReqLogin Password.
         * @member {string} Password
         * @memberof User.ReqLogin
         * @instance
         */
        ReqLogin.prototype.Password = "";

        /**
         * Creates a new ReqLogin instance using the specified properties.
         * @function create
         * @memberof User.ReqLogin
         * @static
         * @param {User.IReqLogin=} [properties] Properties to set
         * @returns {User.ReqLogin} ReqLogin instance
         */
        ReqLogin.create = function create(properties) {
            return new ReqLogin(properties);
        };

        /**
         * Encodes the specified ReqLogin message. Does not implicitly {@link User.ReqLogin.verify|verify} messages.
         * @function encode
         * @memberof User.ReqLogin
         * @static
         * @param {User.IReqLogin} message ReqLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqLogin.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.UserName != null && Object.hasOwnProperty.call(message, "UserName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.UserName);
            if (message.Platform != null && Object.hasOwnProperty.call(message, "Platform"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Platform);
            if (message.SdkType != null && Object.hasOwnProperty.call(message, "SdkType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.SdkType);
            if (message.SdkToken != null && Object.hasOwnProperty.call(message, "SdkToken"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.SdkToken);
            if (message.Device != null && Object.hasOwnProperty.call(message, "Device"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.Device);
            if (message.Password != null && Object.hasOwnProperty.call(message, "Password"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.Password);
            return writer;
        };

        /**
         * Encodes the specified ReqLogin message, length delimited. Does not implicitly {@link User.ReqLogin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.ReqLogin
         * @static
         * @param {User.IReqLogin} message ReqLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqLogin message from the specified reader or buffer.
         * @function decode
         * @memberof User.ReqLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.ReqLogin} ReqLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqLogin.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.ReqLogin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.UserName = reader.string();
                        break;
                    }
                case 2: {
                        message.Platform = reader.string();
                        break;
                    }
                case 3: {
                        message.SdkType = reader.int32();
                        break;
                    }
                case 4: {
                        message.SdkToken = reader.string();
                        break;
                    }
                case 5: {
                        message.Device = reader.string();
                        break;
                    }
                case 6: {
                        message.Password = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqLogin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.ReqLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.ReqLogin} ReqLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqLogin message.
         * @function verify
         * @memberof User.ReqLogin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqLogin.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.UserName != null && Object.hasOwnProperty.call(message, "UserName"))
                if (!$util.isString(message.UserName))
                    return "UserName: string expected";
            if (message.Platform != null && Object.hasOwnProperty.call(message, "Platform"))
                if (!$util.isString(message.Platform))
                    return "Platform: string expected";
            if (message.SdkType != null && Object.hasOwnProperty.call(message, "SdkType"))
                if (!$util.isInteger(message.SdkType))
                    return "SdkType: integer expected";
            if (message.SdkToken != null && Object.hasOwnProperty.call(message, "SdkToken"))
                if (!$util.isString(message.SdkToken))
                    return "SdkToken: string expected";
            if (message.Device != null && Object.hasOwnProperty.call(message, "Device"))
                if (!$util.isString(message.Device))
                    return "Device: string expected";
            if (message.Password != null && Object.hasOwnProperty.call(message, "Password"))
                if (!$util.isString(message.Password))
                    return "Password: string expected";
            return null;
        };

        /**
         * Creates a ReqLogin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.ReqLogin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.ReqLogin} ReqLogin
         */
        ReqLogin.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.ReqLogin)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.ReqLogin: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.ReqLogin();
            if (object.UserName != null)
                message.UserName = String(object.UserName);
            if (object.Platform != null)
                message.Platform = String(object.Platform);
            if (object.SdkType != null)
                message.SdkType = object.SdkType | 0;
            if (object.SdkToken != null)
                message.SdkToken = String(object.SdkToken);
            if (object.Device != null)
                message.Device = String(object.Device);
            if (object.Password != null)
                message.Password = String(object.Password);
            return message;
        };

        /**
         * Creates a plain object from a ReqLogin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.ReqLogin
         * @static
         * @param {User.ReqLogin} message ReqLogin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqLogin.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.UserName = "";
                object.Platform = "";
                object.SdkType = 0;
                object.SdkToken = "";
                object.Device = "";
                object.Password = "";
            }
            if (message.UserName != null && Object.hasOwnProperty.call(message, "UserName"))
                object.UserName = message.UserName;
            if (message.Platform != null && Object.hasOwnProperty.call(message, "Platform"))
                object.Platform = message.Platform;
            if (message.SdkType != null && Object.hasOwnProperty.call(message, "SdkType"))
                object.SdkType = message.SdkType;
            if (message.SdkToken != null && Object.hasOwnProperty.call(message, "SdkToken"))
                object.SdkToken = message.SdkToken;
            if (message.Device != null && Object.hasOwnProperty.call(message, "Device"))
                object.Device = message.Device;
            if (message.Password != null && Object.hasOwnProperty.call(message, "Password"))
                object.Password = message.Password;
            return object;
        };

        /**
         * Converts this ReqLogin to JSON.
         * @function toJSON
         * @memberof User.ReqLogin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqLogin
         * @function getTypeUrl
         * @memberof User.ReqLogin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqLogin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.ReqLogin";
        };

        return ReqLogin;
    })();

    User.RespLogin = (function() {

        /**
         * Properties of a RespLogin.
         * @memberof User
         * @interface IRespLogin
         * @property {number|null} [Code] RespLogin Code
         * @property {string|null} [RoleName] RespLogin RoleName
         * @property {number|Long|null} [Id] RespLogin Id
         * @property {number|null} [Level] RespLogin Level
         * @property {number|Long|null} [CreateTime] RespLogin CreateTime
         */

        /**
         * Constructs a new RespLogin.
         * @memberof User
         * @classdesc Represents a RespLogin.
         * @implements IRespLogin
         * @constructor
         * @param {User.IRespLogin=} [properties] Properties to set
         */
        function RespLogin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespLogin Code.
         * @member {number} Code
         * @memberof User.RespLogin
         * @instance
         */
        RespLogin.prototype.Code = 0;

        /**
         * RespLogin RoleName.
         * @member {string} RoleName
         * @memberof User.RespLogin
         * @instance
         */
        RespLogin.prototype.RoleName = "";

        /**
         * RespLogin Id.
         * @member {number|Long} Id
         * @memberof User.RespLogin
         * @instance
         */
        RespLogin.prototype.Id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RespLogin Level.
         * @member {number} Level
         * @memberof User.RespLogin
         * @instance
         */
        RespLogin.prototype.Level = 0;

        /**
         * RespLogin CreateTime.
         * @member {number|Long} CreateTime
         * @memberof User.RespLogin
         * @instance
         */
        RespLogin.prototype.CreateTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RespLogin instance using the specified properties.
         * @function create
         * @memberof User.RespLogin
         * @static
         * @param {User.IRespLogin=} [properties] Properties to set
         * @returns {User.RespLogin} RespLogin instance
         */
        RespLogin.create = function create(properties) {
            return new RespLogin(properties);
        };

        /**
         * Encodes the specified RespLogin message. Does not implicitly {@link User.RespLogin.verify|verify} messages.
         * @function encode
         * @memberof User.RespLogin
         * @static
         * @param {User.IRespLogin} message RespLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespLogin.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Code != null && Object.hasOwnProperty.call(message, "Code"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Code);
            if (message.RoleName != null && Object.hasOwnProperty.call(message, "RoleName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.RoleName);
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.Id);
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.Level);
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.CreateTime);
            return writer;
        };

        /**
         * Encodes the specified RespLogin message, length delimited. Does not implicitly {@link User.RespLogin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.RespLogin
         * @static
         * @param {User.IRespLogin} message RespLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespLogin message from the specified reader or buffer.
         * @function decode
         * @memberof User.RespLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.RespLogin} RespLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespLogin.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.RespLogin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Code = reader.int32();
                        break;
                    }
                case 2: {
                        message.RoleName = reader.string();
                        break;
                    }
                case 3: {
                        message.Id = reader.int64();
                        break;
                    }
                case 4: {
                        message.Level = reader.uint32();
                        break;
                    }
                case 5: {
                        message.CreateTime = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespLogin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.RespLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.RespLogin} RespLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespLogin message.
         * @function verify
         * @memberof User.RespLogin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespLogin.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Code != null && Object.hasOwnProperty.call(message, "Code"))
                if (!$util.isInteger(message.Code))
                    return "Code: integer expected";
            if (message.RoleName != null && Object.hasOwnProperty.call(message, "RoleName"))
                if (!$util.isString(message.RoleName))
                    return "RoleName: string expected";
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (!$util.isInteger(message.Id) && !(message.Id && $util.isInteger(message.Id.low) && $util.isInteger(message.Id.high)))
                    return "Id: integer|Long expected";
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                if (!$util.isInteger(message.Level))
                    return "Level: integer expected";
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                if (!$util.isInteger(message.CreateTime) && !(message.CreateTime && $util.isInteger(message.CreateTime.low) && $util.isInteger(message.CreateTime.high)))
                    return "CreateTime: integer|Long expected";
            return null;
        };

        /**
         * Creates a RespLogin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.RespLogin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.RespLogin} RespLogin
         */
        RespLogin.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.RespLogin)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.RespLogin: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.RespLogin();
            if (object.Code != null)
                message.Code = object.Code | 0;
            if (object.RoleName != null)
                message.RoleName = String(object.RoleName);
            if (object.Id != null)
                if ($util.Long)
                    message.Id = $util.Long.fromValue(object.Id, false);
                else if (typeof object.Id === "string")
                    message.Id = parseInt(object.Id, 10);
                else if (typeof object.Id === "number")
                    message.Id = object.Id;
                else if (typeof object.Id === "object")
                    message.Id = new $util.LongBits(object.Id.low >>> 0, object.Id.high >>> 0).toNumber();
            if (object.Level != null)
                message.Level = object.Level >>> 0;
            if (object.CreateTime != null)
                if ($util.Long)
                    message.CreateTime = $util.Long.fromValue(object.CreateTime, false);
                else if (typeof object.CreateTime === "string")
                    message.CreateTime = parseInt(object.CreateTime, 10);
                else if (typeof object.CreateTime === "number")
                    message.CreateTime = object.CreateTime;
                else if (typeof object.CreateTime === "object")
                    message.CreateTime = new $util.LongBits(object.CreateTime.low >>> 0, object.CreateTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RespLogin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.RespLogin
         * @static
         * @param {User.RespLogin} message RespLogin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespLogin.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.Code = 0;
                object.RoleName = "";
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Level = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.CreateTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.CreateTime = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.Code != null && Object.hasOwnProperty.call(message, "Code"))
                object.Code = message.Code;
            if (message.RoleName != null && Object.hasOwnProperty.call(message, "RoleName"))
                object.RoleName = message.RoleName;
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Id = typeof message.Id === "number" ? BigInt(message.Id) : $util.Long.fromBits(message.Id.low >>> 0, message.Id.high >>> 0, false).toBigInt();
                else if (typeof message.Id === "number")
                    object.Id = options.longs === String ? String(message.Id) : message.Id;
                else
                    object.Id = options.longs === String ? $util.Long.prototype.toString.call(message.Id) : options.longs === Number ? new $util.LongBits(message.Id.low >>> 0, message.Id.high >>> 0).toNumber() : message.Id;
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                object.Level = message.Level;
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.CreateTime = typeof message.CreateTime === "number" ? BigInt(message.CreateTime) : $util.Long.fromBits(message.CreateTime.low >>> 0, message.CreateTime.high >>> 0, false).toBigInt();
                else if (typeof message.CreateTime === "number")
                    object.CreateTime = options.longs === String ? String(message.CreateTime) : message.CreateTime;
                else
                    object.CreateTime = options.longs === String ? $util.Long.prototype.toString.call(message.CreateTime) : options.longs === Number ? new $util.LongBits(message.CreateTime.low >>> 0, message.CreateTime.high >>> 0).toNumber() : message.CreateTime;
            return object;
        };

        /**
         * Converts this RespLogin to JSON.
         * @function toJSON
         * @memberof User.RespLogin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespLogin
         * @function getTypeUrl
         * @memberof User.RespLogin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespLogin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.RespLogin";
        };

        return RespLogin;
    })();

    User.ReqPlayerCreate = (function() {

        /**
         * Properties of a ReqPlayerCreate.
         * @memberof User
         * @interface IReqPlayerCreate
         * @property {number|Long|null} [Id] ReqPlayerCreate Id
         * @property {string|null} [Name] ReqPlayerCreate Name
         */

        /**
         * Constructs a new ReqPlayerCreate.
         * @memberof User
         * @classdesc Represents a ReqPlayerCreate.
         * @implements IReqPlayerCreate
         * @constructor
         * @param {User.IReqPlayerCreate=} [properties] Properties to set
         */
        function ReqPlayerCreate(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqPlayerCreate Id.
         * @member {number|Long} Id
         * @memberof User.ReqPlayerCreate
         * @instance
         */
        ReqPlayerCreate.prototype.Id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReqPlayerCreate Name.
         * @member {string} Name
         * @memberof User.ReqPlayerCreate
         * @instance
         */
        ReqPlayerCreate.prototype.Name = "";

        /**
         * Creates a new ReqPlayerCreate instance using the specified properties.
         * @function create
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {User.IReqPlayerCreate=} [properties] Properties to set
         * @returns {User.ReqPlayerCreate} ReqPlayerCreate instance
         */
        ReqPlayerCreate.create = function create(properties) {
            return new ReqPlayerCreate(properties);
        };

        /**
         * Encodes the specified ReqPlayerCreate message. Does not implicitly {@link User.ReqPlayerCreate.verify|verify} messages.
         * @function encode
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {User.IReqPlayerCreate} message ReqPlayerCreate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqPlayerCreate.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.Id);
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Name);
            return writer;
        };

        /**
         * Encodes the specified ReqPlayerCreate message, length delimited. Does not implicitly {@link User.ReqPlayerCreate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {User.IReqPlayerCreate} message ReqPlayerCreate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqPlayerCreate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqPlayerCreate message from the specified reader or buffer.
         * @function decode
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.ReqPlayerCreate} ReqPlayerCreate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqPlayerCreate.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.ReqPlayerCreate();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Id = reader.int64();
                        break;
                    }
                case 2: {
                        message.Name = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqPlayerCreate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.ReqPlayerCreate} ReqPlayerCreate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqPlayerCreate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqPlayerCreate message.
         * @function verify
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqPlayerCreate.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (!$util.isInteger(message.Id) && !(message.Id && $util.isInteger(message.Id.low) && $util.isInteger(message.Id.high)))
                    return "Id: integer|Long expected";
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                if (!$util.isString(message.Name))
                    return "Name: string expected";
            return null;
        };

        /**
         * Creates a ReqPlayerCreate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.ReqPlayerCreate} ReqPlayerCreate
         */
        ReqPlayerCreate.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.ReqPlayerCreate)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.ReqPlayerCreate: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.ReqPlayerCreate();
            if (object.Id != null)
                if ($util.Long)
                    message.Id = $util.Long.fromValue(object.Id, false);
                else if (typeof object.Id === "string")
                    message.Id = parseInt(object.Id, 10);
                else if (typeof object.Id === "number")
                    message.Id = object.Id;
                else if (typeof object.Id === "object")
                    message.Id = new $util.LongBits(object.Id.low >>> 0, object.Id.high >>> 0).toNumber();
            if (object.Name != null)
                message.Name = String(object.Name);
            return message;
        };

        /**
         * Creates a plain object from a ReqPlayerCreate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {User.ReqPlayerCreate} message ReqPlayerCreate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqPlayerCreate.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Name = "";
            }
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Id = typeof message.Id === "number" ? BigInt(message.Id) : $util.Long.fromBits(message.Id.low >>> 0, message.Id.high >>> 0, false).toBigInt();
                else if (typeof message.Id === "number")
                    object.Id = options.longs === String ? String(message.Id) : message.Id;
                else
                    object.Id = options.longs === String ? $util.Long.prototype.toString.call(message.Id) : options.longs === Number ? new $util.LongBits(message.Id.low >>> 0, message.Id.high >>> 0).toNumber() : message.Id;
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                object.Name = message.Name;
            return object;
        };

        /**
         * Converts this ReqPlayerCreate to JSON.
         * @function toJSON
         * @memberof User.ReqPlayerCreate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqPlayerCreate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqPlayerCreate
         * @function getTypeUrl
         * @memberof User.ReqPlayerCreate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqPlayerCreate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.ReqPlayerCreate";
        };

        return ReqPlayerCreate;
    })();

    User.RespPlayerCreate = (function() {

        /**
         * Properties of a RespPlayerCreate.
         * @memberof User
         * @interface IRespPlayerCreate
         * @property {User.IPlayerInfo|null} [PlayerInfo] RespPlayerCreate PlayerInfo
         */

        /**
         * Constructs a new RespPlayerCreate.
         * @memberof User
         * @classdesc Represents a RespPlayerCreate.
         * @implements IRespPlayerCreate
         * @constructor
         * @param {User.IRespPlayerCreate=} [properties] Properties to set
         */
        function RespPlayerCreate(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespPlayerCreate PlayerInfo.
         * @member {User.IPlayerInfo|null|undefined} PlayerInfo
         * @memberof User.RespPlayerCreate
         * @instance
         */
        RespPlayerCreate.prototype.PlayerInfo = null;

        /**
         * Creates a new RespPlayerCreate instance using the specified properties.
         * @function create
         * @memberof User.RespPlayerCreate
         * @static
         * @param {User.IRespPlayerCreate=} [properties] Properties to set
         * @returns {User.RespPlayerCreate} RespPlayerCreate instance
         */
        RespPlayerCreate.create = function create(properties) {
            return new RespPlayerCreate(properties);
        };

        /**
         * Encodes the specified RespPlayerCreate message. Does not implicitly {@link User.RespPlayerCreate.verify|verify} messages.
         * @function encode
         * @memberof User.RespPlayerCreate
         * @static
         * @param {User.IRespPlayerCreate} message RespPlayerCreate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPlayerCreate.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.PlayerInfo != null && Object.hasOwnProperty.call(message, "PlayerInfo"))
                $root.User.PlayerInfo.encode(message.PlayerInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespPlayerCreate message, length delimited. Does not implicitly {@link User.RespPlayerCreate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.RespPlayerCreate
         * @static
         * @param {User.IRespPlayerCreate} message RespPlayerCreate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPlayerCreate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespPlayerCreate message from the specified reader or buffer.
         * @function decode
         * @memberof User.RespPlayerCreate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.RespPlayerCreate} RespPlayerCreate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPlayerCreate.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.RespPlayerCreate();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlayerInfo = $root.User.PlayerInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespPlayerCreate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.RespPlayerCreate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.RespPlayerCreate} RespPlayerCreate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPlayerCreate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespPlayerCreate message.
         * @function verify
         * @memberof User.RespPlayerCreate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespPlayerCreate.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.PlayerInfo != null && Object.hasOwnProperty.call(message, "PlayerInfo")) {
                var error = $root.User.PlayerInfo.verify(message.PlayerInfo, long + 1);
                if (error)
                    return "PlayerInfo." + error;
            }
            return null;
        };

        /**
         * Creates a RespPlayerCreate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.RespPlayerCreate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.RespPlayerCreate} RespPlayerCreate
         */
        RespPlayerCreate.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.RespPlayerCreate)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.RespPlayerCreate: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.RespPlayerCreate();
            if (object.PlayerInfo != null) {
                if (!$util.isObject(object.PlayerInfo))
                    throw TypeError(".User.RespPlayerCreate.PlayerInfo: object expected");
                message.PlayerInfo = $root.User.PlayerInfo.fromObject(object.PlayerInfo, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespPlayerCreate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.RespPlayerCreate
         * @static
         * @param {User.RespPlayerCreate} message RespPlayerCreate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespPlayerCreate.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.PlayerInfo = null;
            if (message.PlayerInfo != null && Object.hasOwnProperty.call(message, "PlayerInfo"))
                object.PlayerInfo = $root.User.PlayerInfo.toObject(message.PlayerInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this RespPlayerCreate to JSON.
         * @function toJSON
         * @memberof User.RespPlayerCreate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespPlayerCreate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespPlayerCreate
         * @function getTypeUrl
         * @memberof User.RespPlayerCreate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespPlayerCreate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.RespPlayerCreate";
        };

        return RespPlayerCreate;
    })();

    User.ReqPlayerList = (function() {

        /**
         * Properties of a ReqPlayerList.
         * @memberof User
         * @interface IReqPlayerList
         * @property {number|Long|null} [Id] ReqPlayerList Id
         */

        /**
         * Constructs a new ReqPlayerList.
         * @memberof User
         * @classdesc Represents a ReqPlayerList.
         * @implements IReqPlayerList
         * @constructor
         * @param {User.IReqPlayerList=} [properties] Properties to set
         */
        function ReqPlayerList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqPlayerList Id.
         * @member {number|Long} Id
         * @memberof User.ReqPlayerList
         * @instance
         */
        ReqPlayerList.prototype.Id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqPlayerList instance using the specified properties.
         * @function create
         * @memberof User.ReqPlayerList
         * @static
         * @param {User.IReqPlayerList=} [properties] Properties to set
         * @returns {User.ReqPlayerList} ReqPlayerList instance
         */
        ReqPlayerList.create = function create(properties) {
            return new ReqPlayerList(properties);
        };

        /**
         * Encodes the specified ReqPlayerList message. Does not implicitly {@link User.ReqPlayerList.verify|verify} messages.
         * @function encode
         * @memberof User.ReqPlayerList
         * @static
         * @param {User.IReqPlayerList} message ReqPlayerList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqPlayerList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.Id);
            return writer;
        };

        /**
         * Encodes the specified ReqPlayerList message, length delimited. Does not implicitly {@link User.ReqPlayerList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.ReqPlayerList
         * @static
         * @param {User.IReqPlayerList} message ReqPlayerList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqPlayerList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqPlayerList message from the specified reader or buffer.
         * @function decode
         * @memberof User.ReqPlayerList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.ReqPlayerList} ReqPlayerList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqPlayerList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.ReqPlayerList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Id = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqPlayerList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.ReqPlayerList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.ReqPlayerList} ReqPlayerList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqPlayerList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqPlayerList message.
         * @function verify
         * @memberof User.ReqPlayerList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqPlayerList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (!$util.isInteger(message.Id) && !(message.Id && $util.isInteger(message.Id.low) && $util.isInteger(message.Id.high)))
                    return "Id: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqPlayerList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.ReqPlayerList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.ReqPlayerList} ReqPlayerList
         */
        ReqPlayerList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.ReqPlayerList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.ReqPlayerList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.ReqPlayerList();
            if (object.Id != null)
                if ($util.Long)
                    message.Id = $util.Long.fromValue(object.Id, false);
                else if (typeof object.Id === "string")
                    message.Id = parseInt(object.Id, 10);
                else if (typeof object.Id === "number")
                    message.Id = object.Id;
                else if (typeof object.Id === "object")
                    message.Id = new $util.LongBits(object.Id.low >>> 0, object.Id.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqPlayerList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.ReqPlayerList
         * @static
         * @param {User.ReqPlayerList} message ReqPlayerList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqPlayerList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Id = typeof message.Id === "number" ? BigInt(message.Id) : $util.Long.fromBits(message.Id.low >>> 0, message.Id.high >>> 0, false).toBigInt();
                else if (typeof message.Id === "number")
                    object.Id = options.longs === String ? String(message.Id) : message.Id;
                else
                    object.Id = options.longs === String ? $util.Long.prototype.toString.call(message.Id) : options.longs === Number ? new $util.LongBits(message.Id.low >>> 0, message.Id.high >>> 0).toNumber() : message.Id;
            return object;
        };

        /**
         * Converts this ReqPlayerList to JSON.
         * @function toJSON
         * @memberof User.ReqPlayerList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqPlayerList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqPlayerList
         * @function getTypeUrl
         * @memberof User.ReqPlayerList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqPlayerList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.ReqPlayerList";
        };

        return ReqPlayerList;
    })();

    User.RespPlayerList = (function() {

        /**
         * Properties of a RespPlayerList.
         * @memberof User
         * @interface IRespPlayerList
         * @property {Array.<User.IPlayerInfo>|null} [PlayerList] RespPlayerList PlayerList
         */

        /**
         * Constructs a new RespPlayerList.
         * @memberof User
         * @classdesc Represents a RespPlayerList.
         * @implements IRespPlayerList
         * @constructor
         * @param {User.IRespPlayerList=} [properties] Properties to set
         */
        function RespPlayerList(properties) {
            this.PlayerList = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespPlayerList PlayerList.
         * @member {Array.<User.IPlayerInfo>} PlayerList
         * @memberof User.RespPlayerList
         * @instance
         */
        RespPlayerList.prototype.PlayerList = $util.emptyArray;

        /**
         * Creates a new RespPlayerList instance using the specified properties.
         * @function create
         * @memberof User.RespPlayerList
         * @static
         * @param {User.IRespPlayerList=} [properties] Properties to set
         * @returns {User.RespPlayerList} RespPlayerList instance
         */
        RespPlayerList.create = function create(properties) {
            return new RespPlayerList(properties);
        };

        /**
         * Encodes the specified RespPlayerList message. Does not implicitly {@link User.RespPlayerList.verify|verify} messages.
         * @function encode
         * @memberof User.RespPlayerList
         * @static
         * @param {User.IRespPlayerList} message RespPlayerList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPlayerList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.PlayerList != null && message.PlayerList.length)
                for (var i = 0; i < message.PlayerList.length; ++i)
                    $root.User.PlayerInfo.encode(message.PlayerList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespPlayerList message, length delimited. Does not implicitly {@link User.RespPlayerList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.RespPlayerList
         * @static
         * @param {User.IRespPlayerList} message RespPlayerList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPlayerList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespPlayerList message from the specified reader or buffer.
         * @function decode
         * @memberof User.RespPlayerList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.RespPlayerList} RespPlayerList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPlayerList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.RespPlayerList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlayerList && message.PlayerList.length))
                            message.PlayerList = [];
                        message.PlayerList.push($root.User.PlayerInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespPlayerList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.RespPlayerList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.RespPlayerList} RespPlayerList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPlayerList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespPlayerList message.
         * @function verify
         * @memberof User.RespPlayerList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespPlayerList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.PlayerList != null && Object.hasOwnProperty.call(message, "PlayerList")) {
                if (!Array.isArray(message.PlayerList))
                    return "PlayerList: array expected";
                for (var i = 0; i < message.PlayerList.length; ++i) {
                    var error = $root.User.PlayerInfo.verify(message.PlayerList[i], long + 1);
                    if (error)
                        return "PlayerList." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RespPlayerList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.RespPlayerList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.RespPlayerList} RespPlayerList
         */
        RespPlayerList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.RespPlayerList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.RespPlayerList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.RespPlayerList();
            if (object.PlayerList) {
                if (!Array.isArray(object.PlayerList))
                    throw TypeError(".User.RespPlayerList.PlayerList: array expected");
                message.PlayerList = [];
                for (var i = 0; i < object.PlayerList.length; ++i) {
                    if (!$util.isObject(object.PlayerList[i]))
                        throw TypeError(".User.RespPlayerList.PlayerList: object expected");
                    message.PlayerList[i] = $root.User.PlayerInfo.fromObject(object.PlayerList[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespPlayerList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.RespPlayerList
         * @static
         * @param {User.RespPlayerList} message RespPlayerList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespPlayerList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.PlayerList = [];
            if (message.PlayerList && message.PlayerList.length) {
                object.PlayerList = [];
                for (var j = 0; j < message.PlayerList.length; ++j)
                    object.PlayerList[j] = $root.User.PlayerInfo.toObject(message.PlayerList[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this RespPlayerList to JSON.
         * @function toJSON
         * @memberof User.RespPlayerList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespPlayerList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespPlayerList
         * @function getTypeUrl
         * @memberof User.RespPlayerList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespPlayerList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.RespPlayerList";
        };

        return RespPlayerList;
    })();

    User.PlayerInfo = (function() {

        /**
         * Properties of a PlayerInfo.
         * @memberof User
         * @interface IPlayerInfo
         * @property {number|Long|null} [Id] PlayerInfo Id
         * @property {string|null} [Name] PlayerInfo Name
         * @property {number|null} [Level] PlayerInfo Level
         * @property {number|null} [State] PlayerInfo State
         * @property {number|null} [Avatar] PlayerInfo Avatar
         * @property {number|Long|null} [CurrentExp] PlayerInfo CurrentExp
         */

        /**
         * Constructs a new PlayerInfo.
         * @memberof User
         * @classdesc Represents a PlayerInfo.
         * @implements IPlayerInfo
         * @constructor
         * @param {User.IPlayerInfo=} [properties] Properties to set
         */
        function PlayerInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlayerInfo Id.
         * @member {number|Long} Id
         * @memberof User.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.Id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerInfo Name.
         * @member {string} Name
         * @memberof User.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.Name = "";

        /**
         * PlayerInfo Level.
         * @member {number} Level
         * @memberof User.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.Level = 0;

        /**
         * PlayerInfo State.
         * @member {number} State
         * @memberof User.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.State = 0;

        /**
         * PlayerInfo Avatar.
         * @member {number} Avatar
         * @memberof User.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.Avatar = 0;

        /**
         * PlayerInfo CurrentExp.
         * @member {number|Long} CurrentExp
         * @memberof User.PlayerInfo
         * @instance
         */
        PlayerInfo.prototype.CurrentExp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new PlayerInfo instance using the specified properties.
         * @function create
         * @memberof User.PlayerInfo
         * @static
         * @param {User.IPlayerInfo=} [properties] Properties to set
         * @returns {User.PlayerInfo} PlayerInfo instance
         */
        PlayerInfo.create = function create(properties) {
            return new PlayerInfo(properties);
        };

        /**
         * Encodes the specified PlayerInfo message. Does not implicitly {@link User.PlayerInfo.verify|verify} messages.
         * @function encode
         * @memberof User.PlayerInfo
         * @static
         * @param {User.IPlayerInfo} message PlayerInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.Id);
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Name);
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.Level);
            if (message.State != null && Object.hasOwnProperty.call(message, "State"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.State);
            if (message.Avatar != null && Object.hasOwnProperty.call(message, "Avatar"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.Avatar);
            if (message.CurrentExp != null && Object.hasOwnProperty.call(message, "CurrentExp"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.CurrentExp);
            return writer;
        };

        /**
         * Encodes the specified PlayerInfo message, length delimited. Does not implicitly {@link User.PlayerInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.PlayerInfo
         * @static
         * @param {User.IPlayerInfo} message PlayerInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PlayerInfo message from the specified reader or buffer.
         * @function decode
         * @memberof User.PlayerInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.PlayerInfo} PlayerInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.PlayerInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Id = reader.int64();
                        break;
                    }
                case 2: {
                        message.Name = reader.string();
                        break;
                    }
                case 3: {
                        message.Level = reader.uint32();
                        break;
                    }
                case 4: {
                        message.State = reader.int32();
                        break;
                    }
                case 5: {
                        message.Avatar = reader.uint32();
                        break;
                    }
                case 6: {
                        message.CurrentExp = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a PlayerInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.PlayerInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.PlayerInfo} PlayerInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlayerInfo message.
         * @function verify
         * @memberof User.PlayerInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (!$util.isInteger(message.Id) && !(message.Id && $util.isInteger(message.Id.low) && $util.isInteger(message.Id.high)))
                    return "Id: integer|Long expected";
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                if (!$util.isString(message.Name))
                    return "Name: string expected";
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                if (!$util.isInteger(message.Level))
                    return "Level: integer expected";
            if (message.State != null && Object.hasOwnProperty.call(message, "State"))
                if (!$util.isInteger(message.State))
                    return "State: integer expected";
            if (message.Avatar != null && Object.hasOwnProperty.call(message, "Avatar"))
                if (!$util.isInteger(message.Avatar))
                    return "Avatar: integer expected";
            if (message.CurrentExp != null && Object.hasOwnProperty.call(message, "CurrentExp"))
                if (!$util.isInteger(message.CurrentExp) && !(message.CurrentExp && $util.isInteger(message.CurrentExp.low) && $util.isInteger(message.CurrentExp.high)))
                    return "CurrentExp: integer|Long expected";
            return null;
        };

        /**
         * Creates a PlayerInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.PlayerInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.PlayerInfo} PlayerInfo
         */
        PlayerInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.PlayerInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.PlayerInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.PlayerInfo();
            if (object.Id != null)
                if ($util.Long)
                    message.Id = $util.Long.fromValue(object.Id, false);
                else if (typeof object.Id === "string")
                    message.Id = parseInt(object.Id, 10);
                else if (typeof object.Id === "number")
                    message.Id = object.Id;
                else if (typeof object.Id === "object")
                    message.Id = new $util.LongBits(object.Id.low >>> 0, object.Id.high >>> 0).toNumber();
            if (object.Name != null)
                message.Name = String(object.Name);
            if (object.Level != null)
                message.Level = object.Level >>> 0;
            if (object.State != null)
                message.State = object.State | 0;
            if (object.Avatar != null)
                message.Avatar = object.Avatar >>> 0;
            if (object.CurrentExp != null)
                if ($util.Long)
                    message.CurrentExp = $util.Long.fromValue(object.CurrentExp, true);
                else if (typeof object.CurrentExp === "string")
                    message.CurrentExp = parseInt(object.CurrentExp, 10);
                else if (typeof object.CurrentExp === "number")
                    message.CurrentExp = object.CurrentExp;
                else if (typeof object.CurrentExp === "object")
                    message.CurrentExp = new $util.LongBits(object.CurrentExp.low >>> 0, object.CurrentExp.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a PlayerInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.PlayerInfo
         * @static
         * @param {User.PlayerInfo} message PlayerInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlayerInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Name = "";
                object.Level = 0;
                object.State = 0;
                object.Avatar = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, true);
                    object.CurrentExp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.CurrentExp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Id = typeof message.Id === "number" ? BigInt(message.Id) : $util.Long.fromBits(message.Id.low >>> 0, message.Id.high >>> 0, false).toBigInt();
                else if (typeof message.Id === "number")
                    object.Id = options.longs === String ? String(message.Id) : message.Id;
                else
                    object.Id = options.longs === String ? $util.Long.prototype.toString.call(message.Id) : options.longs === Number ? new $util.LongBits(message.Id.low >>> 0, message.Id.high >>> 0).toNumber() : message.Id;
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                object.Name = message.Name;
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                object.Level = message.Level;
            if (message.State != null && Object.hasOwnProperty.call(message, "State"))
                object.State = message.State;
            if (message.Avatar != null && Object.hasOwnProperty.call(message, "Avatar"))
                object.Avatar = message.Avatar;
            if (message.CurrentExp != null && Object.hasOwnProperty.call(message, "CurrentExp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.CurrentExp = typeof message.CurrentExp === "number" ? BigInt(message.CurrentExp) : $util.Long.fromBits(message.CurrentExp.low >>> 0, message.CurrentExp.high >>> 0, true).toBigInt();
                else if (typeof message.CurrentExp === "number")
                    object.CurrentExp = options.longs === String ? String(message.CurrentExp) : message.CurrentExp;
                else
                    object.CurrentExp = options.longs === String ? $util.Long.prototype.toString.call(message.CurrentExp) : options.longs === Number ? new $util.LongBits(message.CurrentExp.low >>> 0, message.CurrentExp.high >>> 0).toNumber(true) : message.CurrentExp;
            return object;
        };

        /**
         * Converts this PlayerInfo to JSON.
         * @function toJSON
         * @memberof User.PlayerInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlayerInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlayerInfo
         * @function getTypeUrl
         * @memberof User.PlayerInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlayerInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.PlayerInfo";
        };

        return PlayerInfo;
    })();

    User.ReqPlayerLogin = (function() {

        /**
         * Properties of a ReqPlayerLogin.
         * @memberof User
         * @interface IReqPlayerLogin
         * @property {number|Long|null} [Id] ReqPlayerLogin Id
         */

        /**
         * Constructs a new ReqPlayerLogin.
         * @memberof User
         * @classdesc Represents a ReqPlayerLogin.
         * @implements IReqPlayerLogin
         * @constructor
         * @param {User.IReqPlayerLogin=} [properties] Properties to set
         */
        function ReqPlayerLogin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqPlayerLogin Id.
         * @member {number|Long} Id
         * @memberof User.ReqPlayerLogin
         * @instance
         */
        ReqPlayerLogin.prototype.Id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqPlayerLogin instance using the specified properties.
         * @function create
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {User.IReqPlayerLogin=} [properties] Properties to set
         * @returns {User.ReqPlayerLogin} ReqPlayerLogin instance
         */
        ReqPlayerLogin.create = function create(properties) {
            return new ReqPlayerLogin(properties);
        };

        /**
         * Encodes the specified ReqPlayerLogin message. Does not implicitly {@link User.ReqPlayerLogin.verify|verify} messages.
         * @function encode
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {User.IReqPlayerLogin} message ReqPlayerLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqPlayerLogin.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.Id);
            return writer;
        };

        /**
         * Encodes the specified ReqPlayerLogin message, length delimited. Does not implicitly {@link User.ReqPlayerLogin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {User.IReqPlayerLogin} message ReqPlayerLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqPlayerLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqPlayerLogin message from the specified reader or buffer.
         * @function decode
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.ReqPlayerLogin} ReqPlayerLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqPlayerLogin.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.ReqPlayerLogin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Id = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqPlayerLogin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.ReqPlayerLogin} ReqPlayerLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqPlayerLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqPlayerLogin message.
         * @function verify
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqPlayerLogin.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (!$util.isInteger(message.Id) && !(message.Id && $util.isInteger(message.Id.low) && $util.isInteger(message.Id.high)))
                    return "Id: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqPlayerLogin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.ReqPlayerLogin} ReqPlayerLogin
         */
        ReqPlayerLogin.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.ReqPlayerLogin)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.ReqPlayerLogin: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.ReqPlayerLogin();
            if (object.Id != null)
                if ($util.Long)
                    message.Id = $util.Long.fromValue(object.Id, false);
                else if (typeof object.Id === "string")
                    message.Id = parseInt(object.Id, 10);
                else if (typeof object.Id === "number")
                    message.Id = object.Id;
                else if (typeof object.Id === "object")
                    message.Id = new $util.LongBits(object.Id.low >>> 0, object.Id.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqPlayerLogin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {User.ReqPlayerLogin} message ReqPlayerLogin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqPlayerLogin.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.Id != null && Object.hasOwnProperty.call(message, "Id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Id = typeof message.Id === "number" ? BigInt(message.Id) : $util.Long.fromBits(message.Id.low >>> 0, message.Id.high >>> 0, false).toBigInt();
                else if (typeof message.Id === "number")
                    object.Id = options.longs === String ? String(message.Id) : message.Id;
                else
                    object.Id = options.longs === String ? $util.Long.prototype.toString.call(message.Id) : options.longs === Number ? new $util.LongBits(message.Id.low >>> 0, message.Id.high >>> 0).toNumber() : message.Id;
            return object;
        };

        /**
         * Converts this ReqPlayerLogin to JSON.
         * @function toJSON
         * @memberof User.ReqPlayerLogin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqPlayerLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqPlayerLogin
         * @function getTypeUrl
         * @memberof User.ReqPlayerLogin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqPlayerLogin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.ReqPlayerLogin";
        };

        return ReqPlayerLogin;
    })();

    User.RespPlayerLogin = (function() {

        /**
         * Properties of a RespPlayerLogin.
         * @memberof User
         * @interface IRespPlayerLogin
         * @property {number|null} [Code] RespPlayerLogin Code
         * @property {number|Long|null} [CreateTime] RespPlayerLogin CreateTime
         * @property {User.IPlayerInfo|null} [PlayerInfo] RespPlayerLogin PlayerInfo
         */

        /**
         * Constructs a new RespPlayerLogin.
         * @memberof User
         * @classdesc Represents a RespPlayerLogin.
         * @implements IRespPlayerLogin
         * @constructor
         * @param {User.IRespPlayerLogin=} [properties] Properties to set
         */
        function RespPlayerLogin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespPlayerLogin Code.
         * @member {number} Code
         * @memberof User.RespPlayerLogin
         * @instance
         */
        RespPlayerLogin.prototype.Code = 0;

        /**
         * RespPlayerLogin CreateTime.
         * @member {number|Long} CreateTime
         * @memberof User.RespPlayerLogin
         * @instance
         */
        RespPlayerLogin.prototype.CreateTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RespPlayerLogin PlayerInfo.
         * @member {User.IPlayerInfo|null|undefined} PlayerInfo
         * @memberof User.RespPlayerLogin
         * @instance
         */
        RespPlayerLogin.prototype.PlayerInfo = null;

        /**
         * Creates a new RespPlayerLogin instance using the specified properties.
         * @function create
         * @memberof User.RespPlayerLogin
         * @static
         * @param {User.IRespPlayerLogin=} [properties] Properties to set
         * @returns {User.RespPlayerLogin} RespPlayerLogin instance
         */
        RespPlayerLogin.create = function create(properties) {
            return new RespPlayerLogin(properties);
        };

        /**
         * Encodes the specified RespPlayerLogin message. Does not implicitly {@link User.RespPlayerLogin.verify|verify} messages.
         * @function encode
         * @memberof User.RespPlayerLogin
         * @static
         * @param {User.IRespPlayerLogin} message RespPlayerLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPlayerLogin.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Code != null && Object.hasOwnProperty.call(message, "Code"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Code);
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.CreateTime);
            if (message.PlayerInfo != null && Object.hasOwnProperty.call(message, "PlayerInfo"))
                $root.User.PlayerInfo.encode(message.PlayerInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespPlayerLogin message, length delimited. Does not implicitly {@link User.RespPlayerLogin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.RespPlayerLogin
         * @static
         * @param {User.IRespPlayerLogin} message RespPlayerLogin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPlayerLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespPlayerLogin message from the specified reader or buffer.
         * @function decode
         * @memberof User.RespPlayerLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.RespPlayerLogin} RespPlayerLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPlayerLogin.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.RespPlayerLogin();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Code = reader.int32();
                        break;
                    }
                case 2: {
                        message.CreateTime = reader.int64();
                        break;
                    }
                case 3: {
                        message.PlayerInfo = $root.User.PlayerInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespPlayerLogin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.RespPlayerLogin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.RespPlayerLogin} RespPlayerLogin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPlayerLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespPlayerLogin message.
         * @function verify
         * @memberof User.RespPlayerLogin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespPlayerLogin.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Code != null && Object.hasOwnProperty.call(message, "Code"))
                if (!$util.isInteger(message.Code))
                    return "Code: integer expected";
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                if (!$util.isInteger(message.CreateTime) && !(message.CreateTime && $util.isInteger(message.CreateTime.low) && $util.isInteger(message.CreateTime.high)))
                    return "CreateTime: integer|Long expected";
            if (message.PlayerInfo != null && Object.hasOwnProperty.call(message, "PlayerInfo")) {
                var error = $root.User.PlayerInfo.verify(message.PlayerInfo, long + 1);
                if (error)
                    return "PlayerInfo." + error;
            }
            return null;
        };

        /**
         * Creates a RespPlayerLogin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.RespPlayerLogin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.RespPlayerLogin} RespPlayerLogin
         */
        RespPlayerLogin.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.RespPlayerLogin)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.RespPlayerLogin: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.RespPlayerLogin();
            if (object.Code != null)
                message.Code = object.Code | 0;
            if (object.CreateTime != null)
                if ($util.Long)
                    message.CreateTime = $util.Long.fromValue(object.CreateTime, false);
                else if (typeof object.CreateTime === "string")
                    message.CreateTime = parseInt(object.CreateTime, 10);
                else if (typeof object.CreateTime === "number")
                    message.CreateTime = object.CreateTime;
                else if (typeof object.CreateTime === "object")
                    message.CreateTime = new $util.LongBits(object.CreateTime.low >>> 0, object.CreateTime.high >>> 0).toNumber();
            if (object.PlayerInfo != null) {
                if (!$util.isObject(object.PlayerInfo))
                    throw TypeError(".User.RespPlayerLogin.PlayerInfo: object expected");
                message.PlayerInfo = $root.User.PlayerInfo.fromObject(object.PlayerInfo, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespPlayerLogin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.RespPlayerLogin
         * @static
         * @param {User.RespPlayerLogin} message RespPlayerLogin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespPlayerLogin.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.Code = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.CreateTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.CreateTime = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.PlayerInfo = null;
            }
            if (message.Code != null && Object.hasOwnProperty.call(message, "Code"))
                object.Code = message.Code;
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.CreateTime = typeof message.CreateTime === "number" ? BigInt(message.CreateTime) : $util.Long.fromBits(message.CreateTime.low >>> 0, message.CreateTime.high >>> 0, false).toBigInt();
                else if (typeof message.CreateTime === "number")
                    object.CreateTime = options.longs === String ? String(message.CreateTime) : message.CreateTime;
                else
                    object.CreateTime = options.longs === String ? $util.Long.prototype.toString.call(message.CreateTime) : options.longs === Number ? new $util.LongBits(message.CreateTime.low >>> 0, message.CreateTime.high >>> 0).toNumber() : message.CreateTime;
            if (message.PlayerInfo != null && Object.hasOwnProperty.call(message, "PlayerInfo"))
                object.PlayerInfo = $root.User.PlayerInfo.toObject(message.PlayerInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this RespPlayerLogin to JSON.
         * @function toJSON
         * @memberof User.RespPlayerLogin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespPlayerLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespPlayerLogin
         * @function getTypeUrl
         * @memberof User.RespPlayerLogin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespPlayerLogin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.RespPlayerLogin";
        };

        return RespPlayerLogin;
    })();

    User.RespErrorCode = (function() {

        /**
         * Properties of a RespErrorCode.
         * @memberof User
         * @interface IRespErrorCode
         * @property {number|Long|null} [ErrCode] RespErrorCode ErrCode
         * @property {string|null} [Desc] RespErrorCode Desc
         */

        /**
         * Constructs a new RespErrorCode.
         * @memberof User
         * @classdesc Represents a RespErrorCode.
         * @implements IRespErrorCode
         * @constructor
         * @param {User.IRespErrorCode=} [properties] Properties to set
         */
        function RespErrorCode(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespErrorCode ErrCode.
         * @member {number|Long} ErrCode
         * @memberof User.RespErrorCode
         * @instance
         */
        RespErrorCode.prototype.ErrCode = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RespErrorCode Desc.
         * @member {string} Desc
         * @memberof User.RespErrorCode
         * @instance
         */
        RespErrorCode.prototype.Desc = "";

        /**
         * Creates a new RespErrorCode instance using the specified properties.
         * @function create
         * @memberof User.RespErrorCode
         * @static
         * @param {User.IRespErrorCode=} [properties] Properties to set
         * @returns {User.RespErrorCode} RespErrorCode instance
         */
        RespErrorCode.create = function create(properties) {
            return new RespErrorCode(properties);
        };

        /**
         * Encodes the specified RespErrorCode message. Does not implicitly {@link User.RespErrorCode.verify|verify} messages.
         * @function encode
         * @memberof User.RespErrorCode
         * @static
         * @param {User.IRespErrorCode} message RespErrorCode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespErrorCode.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ErrCode != null && Object.hasOwnProperty.call(message, "ErrCode"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.ErrCode);
            if (message.Desc != null && Object.hasOwnProperty.call(message, "Desc"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Desc);
            return writer;
        };

        /**
         * Encodes the specified RespErrorCode message, length delimited. Does not implicitly {@link User.RespErrorCode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.RespErrorCode
         * @static
         * @param {User.IRespErrorCode} message RespErrorCode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespErrorCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespErrorCode message from the specified reader or buffer.
         * @function decode
         * @memberof User.RespErrorCode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.RespErrorCode} RespErrorCode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespErrorCode.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.RespErrorCode();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ErrCode = reader.int64();
                        break;
                    }
                case 2: {
                        message.Desc = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespErrorCode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.RespErrorCode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.RespErrorCode} RespErrorCode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespErrorCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespErrorCode message.
         * @function verify
         * @memberof User.RespErrorCode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespErrorCode.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ErrCode != null && Object.hasOwnProperty.call(message, "ErrCode"))
                if (!$util.isInteger(message.ErrCode) && !(message.ErrCode && $util.isInteger(message.ErrCode.low) && $util.isInteger(message.ErrCode.high)))
                    return "ErrCode: integer|Long expected";
            if (message.Desc != null && Object.hasOwnProperty.call(message, "Desc"))
                if (!$util.isString(message.Desc))
                    return "Desc: string expected";
            return null;
        };

        /**
         * Creates a RespErrorCode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.RespErrorCode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.RespErrorCode} RespErrorCode
         */
        RespErrorCode.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.RespErrorCode)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.RespErrorCode: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.RespErrorCode();
            if (object.ErrCode != null)
                if ($util.Long)
                    message.ErrCode = $util.Long.fromValue(object.ErrCode, false);
                else if (typeof object.ErrCode === "string")
                    message.ErrCode = parseInt(object.ErrCode, 10);
                else if (typeof object.ErrCode === "number")
                    message.ErrCode = object.ErrCode;
                else if (typeof object.ErrCode === "object")
                    message.ErrCode = new $util.LongBits(object.ErrCode.low >>> 0, object.ErrCode.high >>> 0).toNumber();
            if (object.Desc != null)
                message.Desc = String(object.Desc);
            return message;
        };

        /**
         * Creates a plain object from a RespErrorCode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.RespErrorCode
         * @static
         * @param {User.RespErrorCode} message RespErrorCode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespErrorCode.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.ErrCode = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.ErrCode = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Desc = "";
            }
            if (message.ErrCode != null && Object.hasOwnProperty.call(message, "ErrCode"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ErrCode = typeof message.ErrCode === "number" ? BigInt(message.ErrCode) : $util.Long.fromBits(message.ErrCode.low >>> 0, message.ErrCode.high >>> 0, false).toBigInt();
                else if (typeof message.ErrCode === "number")
                    object.ErrCode = options.longs === String ? String(message.ErrCode) : message.ErrCode;
                else
                    object.ErrCode = options.longs === String ? $util.Long.prototype.toString.call(message.ErrCode) : options.longs === Number ? new $util.LongBits(message.ErrCode.low >>> 0, message.ErrCode.high >>> 0).toNumber() : message.ErrCode;
            if (message.Desc != null && Object.hasOwnProperty.call(message, "Desc"))
                object.Desc = message.Desc;
            return object;
        };

        /**
         * Converts this RespErrorCode to JSON.
         * @function toJSON
         * @memberof User.RespErrorCode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespErrorCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespErrorCode
         * @function getTypeUrl
         * @memberof User.RespErrorCode
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespErrorCode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.RespErrorCode";
        };

        return RespErrorCode;
    })();

    User.RespPrompt = (function() {

        /**
         * Properties of a RespPrompt.
         * @memberof User
         * @interface IRespPrompt
         * @property {number|null} [Type] RespPrompt Type
         * @property {string|null} [Content] RespPrompt Content
         */

        /**
         * Constructs a new RespPrompt.
         * @memberof User
         * @classdesc Represents a RespPrompt.
         * @implements IRespPrompt
         * @constructor
         * @param {User.IRespPrompt=} [properties] Properties to set
         */
        function RespPrompt(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespPrompt Type.
         * @member {number} Type
         * @memberof User.RespPrompt
         * @instance
         */
        RespPrompt.prototype.Type = 0;

        /**
         * RespPrompt Content.
         * @member {string} Content
         * @memberof User.RespPrompt
         * @instance
         */
        RespPrompt.prototype.Content = "";

        /**
         * Creates a new RespPrompt instance using the specified properties.
         * @function create
         * @memberof User.RespPrompt
         * @static
         * @param {User.IRespPrompt=} [properties] Properties to set
         * @returns {User.RespPrompt} RespPrompt instance
         */
        RespPrompt.create = function create(properties) {
            return new RespPrompt(properties);
        };

        /**
         * Encodes the specified RespPrompt message. Does not implicitly {@link User.RespPrompt.verify|verify} messages.
         * @function encode
         * @memberof User.RespPrompt
         * @static
         * @param {User.IRespPrompt} message RespPrompt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPrompt.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Type);
            if (message.Content != null && Object.hasOwnProperty.call(message, "Content"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Content);
            return writer;
        };

        /**
         * Encodes the specified RespPrompt message, length delimited. Does not implicitly {@link User.RespPrompt.verify|verify} messages.
         * @function encodeDelimited
         * @memberof User.RespPrompt
         * @static
         * @param {User.IRespPrompt} message RespPrompt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespPrompt.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespPrompt message from the specified reader or buffer.
         * @function decode
         * @memberof User.RespPrompt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {User.RespPrompt} RespPrompt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPrompt.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.User.RespPrompt();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Type = reader.int32();
                        break;
                    }
                case 2: {
                        message.Content = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespPrompt message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof User.RespPrompt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {User.RespPrompt} RespPrompt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespPrompt.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespPrompt message.
         * @function verify
         * @memberof User.RespPrompt
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespPrompt.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                if (!$util.isInteger(message.Type))
                    return "Type: integer expected";
            if (message.Content != null && Object.hasOwnProperty.call(message, "Content"))
                if (!$util.isString(message.Content))
                    return "Content: string expected";
            return null;
        };

        /**
         * Creates a RespPrompt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof User.RespPrompt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {User.RespPrompt} RespPrompt
         */
        RespPrompt.fromObject = function fromObject(object, long) {
            if (object instanceof $root.User.RespPrompt)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".User.RespPrompt: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.User.RespPrompt();
            if (object.Type != null)
                message.Type = object.Type | 0;
            if (object.Content != null)
                message.Content = String(object.Content);
            return message;
        };

        /**
         * Creates a plain object from a RespPrompt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof User.RespPrompt
         * @static
         * @param {User.RespPrompt} message RespPrompt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespPrompt.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.Type = 0;
                object.Content = "";
            }
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                object.Type = message.Type;
            if (message.Content != null && Object.hasOwnProperty.call(message, "Content"))
                object.Content = message.Content;
            return object;
        };

        /**
         * Converts this RespPrompt to JSON.
         * @function toJSON
         * @memberof User.RespPrompt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespPrompt.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespPrompt
         * @function getTypeUrl
         * @memberof User.RespPrompt
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespPrompt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/User.RespPrompt";
        };

        return RespPrompt;
    })();

    return User;
})();

$root.Attribute = (function() {

    /**
     * Namespace Attribute.
     * @exports Attribute
     * @namespace
     */
    var Attribute = {};

    Attribute.PlayerAttributeEntry = (function() {

        /**
         * Properties of a PlayerAttributeEntry.
         * @memberof Attribute
         * @interface IPlayerAttributeEntry
         * @property {number|null} [Type] PlayerAttributeEntry Type
         * @property {number|Long|null} [Value] PlayerAttributeEntry Value
         * @property {number|Long|null} [Base] PlayerAttributeEntry Base
         * @property {number|Long|null} [Add] PlayerAttributeEntry Add
         * @property {number|Long|null} [Pct] PlayerAttributeEntry Pct
         */

        /**
         * Constructs a new PlayerAttributeEntry.
         * @memberof Attribute
         * @classdesc Represents a PlayerAttributeEntry.
         * @implements IPlayerAttributeEntry
         * @constructor
         * @param {Attribute.IPlayerAttributeEntry=} [properties] Properties to set
         */
        function PlayerAttributeEntry(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlayerAttributeEntry Type.
         * @member {number} Type
         * @memberof Attribute.PlayerAttributeEntry
         * @instance
         */
        PlayerAttributeEntry.prototype.Type = 0;

        /**
         * PlayerAttributeEntry Value.
         * @member {number|Long} Value
         * @memberof Attribute.PlayerAttributeEntry
         * @instance
         */
        PlayerAttributeEntry.prototype.Value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerAttributeEntry Base.
         * @member {number|Long} Base
         * @memberof Attribute.PlayerAttributeEntry
         * @instance
         */
        PlayerAttributeEntry.prototype.Base = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerAttributeEntry Add.
         * @member {number|Long} Add
         * @memberof Attribute.PlayerAttributeEntry
         * @instance
         */
        PlayerAttributeEntry.prototype.Add = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PlayerAttributeEntry Pct.
         * @member {number|Long} Pct
         * @memberof Attribute.PlayerAttributeEntry
         * @instance
         */
        PlayerAttributeEntry.prototype.Pct = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PlayerAttributeEntry instance using the specified properties.
         * @function create
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {Attribute.IPlayerAttributeEntry=} [properties] Properties to set
         * @returns {Attribute.PlayerAttributeEntry} PlayerAttributeEntry instance
         */
        PlayerAttributeEntry.create = function create(properties) {
            return new PlayerAttributeEntry(properties);
        };

        /**
         * Encodes the specified PlayerAttributeEntry message. Does not implicitly {@link Attribute.PlayerAttributeEntry.verify|verify} messages.
         * @function encode
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {Attribute.IPlayerAttributeEntry} message PlayerAttributeEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerAttributeEntry.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Type);
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Value);
            if (message.Base != null && Object.hasOwnProperty.call(message, "Base"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.Base);
            if (message.Add != null && Object.hasOwnProperty.call(message, "Add"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.Add);
            if (message.Pct != null && Object.hasOwnProperty.call(message, "Pct"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.Pct);
            return writer;
        };

        /**
         * Encodes the specified PlayerAttributeEntry message, length delimited. Does not implicitly {@link Attribute.PlayerAttributeEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {Attribute.IPlayerAttributeEntry} message PlayerAttributeEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlayerAttributeEntry.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PlayerAttributeEntry message from the specified reader or buffer.
         * @function decode
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Attribute.PlayerAttributeEntry} PlayerAttributeEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerAttributeEntry.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Attribute.PlayerAttributeEntry();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Type = reader.int32();
                        break;
                    }
                case 2: {
                        message.Value = reader.int64();
                        break;
                    }
                case 3: {
                        message.Base = reader.int64();
                        break;
                    }
                case 4: {
                        message.Add = reader.int64();
                        break;
                    }
                case 5: {
                        message.Pct = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a PlayerAttributeEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Attribute.PlayerAttributeEntry} PlayerAttributeEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlayerAttributeEntry.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlayerAttributeEntry message.
         * @function verify
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlayerAttributeEntry.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                if (!$util.isInteger(message.Type))
                    return "Type: integer expected";
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                if (!$util.isInteger(message.Value) && !(message.Value && $util.isInteger(message.Value.low) && $util.isInteger(message.Value.high)))
                    return "Value: integer|Long expected";
            if (message.Base != null && Object.hasOwnProperty.call(message, "Base"))
                if (!$util.isInteger(message.Base) && !(message.Base && $util.isInteger(message.Base.low) && $util.isInteger(message.Base.high)))
                    return "Base: integer|Long expected";
            if (message.Add != null && Object.hasOwnProperty.call(message, "Add"))
                if (!$util.isInteger(message.Add) && !(message.Add && $util.isInteger(message.Add.low) && $util.isInteger(message.Add.high)))
                    return "Add: integer|Long expected";
            if (message.Pct != null && Object.hasOwnProperty.call(message, "Pct"))
                if (!$util.isInteger(message.Pct) && !(message.Pct && $util.isInteger(message.Pct.low) && $util.isInteger(message.Pct.high)))
                    return "Pct: integer|Long expected";
            return null;
        };

        /**
         * Creates a PlayerAttributeEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Attribute.PlayerAttributeEntry} PlayerAttributeEntry
         */
        PlayerAttributeEntry.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Attribute.PlayerAttributeEntry)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Attribute.PlayerAttributeEntry: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Attribute.PlayerAttributeEntry();
            if (object.Type != null)
                message.Type = object.Type | 0;
            if (object.Value != null)
                if ($util.Long)
                    message.Value = $util.Long.fromValue(object.Value, false);
                else if (typeof object.Value === "string")
                    message.Value = parseInt(object.Value, 10);
                else if (typeof object.Value === "number")
                    message.Value = object.Value;
                else if (typeof object.Value === "object")
                    message.Value = new $util.LongBits(object.Value.low >>> 0, object.Value.high >>> 0).toNumber();
            if (object.Base != null)
                if ($util.Long)
                    message.Base = $util.Long.fromValue(object.Base, false);
                else if (typeof object.Base === "string")
                    message.Base = parseInt(object.Base, 10);
                else if (typeof object.Base === "number")
                    message.Base = object.Base;
                else if (typeof object.Base === "object")
                    message.Base = new $util.LongBits(object.Base.low >>> 0, object.Base.high >>> 0).toNumber();
            if (object.Add != null)
                if ($util.Long)
                    message.Add = $util.Long.fromValue(object.Add, false);
                else if (typeof object.Add === "string")
                    message.Add = parseInt(object.Add, 10);
                else if (typeof object.Add === "number")
                    message.Add = object.Add;
                else if (typeof object.Add === "object")
                    message.Add = new $util.LongBits(object.Add.low >>> 0, object.Add.high >>> 0).toNumber();
            if (object.Pct != null)
                if ($util.Long)
                    message.Pct = $util.Long.fromValue(object.Pct, false);
                else if (typeof object.Pct === "string")
                    message.Pct = parseInt(object.Pct, 10);
                else if (typeof object.Pct === "number")
                    message.Pct = object.Pct;
                else if (typeof object.Pct === "object")
                    message.Pct = new $util.LongBits(object.Pct.low >>> 0, object.Pct.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PlayerAttributeEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {Attribute.PlayerAttributeEntry} message PlayerAttributeEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlayerAttributeEntry.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.Type = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Value = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Value = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Base = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Base = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Add = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Add = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Pct = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Pct = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                object.Type = message.Type;
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Value = typeof message.Value === "number" ? BigInt(message.Value) : $util.Long.fromBits(message.Value.low >>> 0, message.Value.high >>> 0, false).toBigInt();
                else if (typeof message.Value === "number")
                    object.Value = options.longs === String ? String(message.Value) : message.Value;
                else
                    object.Value = options.longs === String ? $util.Long.prototype.toString.call(message.Value) : options.longs === Number ? new $util.LongBits(message.Value.low >>> 0, message.Value.high >>> 0).toNumber() : message.Value;
            if (message.Base != null && Object.hasOwnProperty.call(message, "Base"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Base = typeof message.Base === "number" ? BigInt(message.Base) : $util.Long.fromBits(message.Base.low >>> 0, message.Base.high >>> 0, false).toBigInt();
                else if (typeof message.Base === "number")
                    object.Base = options.longs === String ? String(message.Base) : message.Base;
                else
                    object.Base = options.longs === String ? $util.Long.prototype.toString.call(message.Base) : options.longs === Number ? new $util.LongBits(message.Base.low >>> 0, message.Base.high >>> 0).toNumber() : message.Base;
            if (message.Add != null && Object.hasOwnProperty.call(message, "Add"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Add = typeof message.Add === "number" ? BigInt(message.Add) : $util.Long.fromBits(message.Add.low >>> 0, message.Add.high >>> 0, false).toBigInt();
                else if (typeof message.Add === "number")
                    object.Add = options.longs === String ? String(message.Add) : message.Add;
                else
                    object.Add = options.longs === String ? $util.Long.prototype.toString.call(message.Add) : options.longs === Number ? new $util.LongBits(message.Add.low >>> 0, message.Add.high >>> 0).toNumber() : message.Add;
            if (message.Pct != null && Object.hasOwnProperty.call(message, "Pct"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Pct = typeof message.Pct === "number" ? BigInt(message.Pct) : $util.Long.fromBits(message.Pct.low >>> 0, message.Pct.high >>> 0, false).toBigInt();
                else if (typeof message.Pct === "number")
                    object.Pct = options.longs === String ? String(message.Pct) : message.Pct;
                else
                    object.Pct = options.longs === String ? $util.Long.prototype.toString.call(message.Pct) : options.longs === Number ? new $util.LongBits(message.Pct.low >>> 0, message.Pct.high >>> 0).toNumber() : message.Pct;
            return object;
        };

        /**
         * Converts this PlayerAttributeEntry to JSON.
         * @function toJSON
         * @memberof Attribute.PlayerAttributeEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlayerAttributeEntry.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlayerAttributeEntry
         * @function getTypeUrl
         * @memberof Attribute.PlayerAttributeEntry
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlayerAttributeEntry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Attribute.PlayerAttributeEntry";
        };

        return PlayerAttributeEntry;
    })();

    Attribute.NotifyPlayerAttributeSync = (function() {

        /**
         * Properties of a NotifyPlayerAttributeSync.
         * @memberof Attribute
         * @interface INotifyPlayerAttributeSync
         * @property {Array.<Attribute.IPlayerAttributeEntry>|null} [Attributes] NotifyPlayerAttributeSync Attributes
         */

        /**
         * Constructs a new NotifyPlayerAttributeSync.
         * @memberof Attribute
         * @classdesc Represents a NotifyPlayerAttributeSync.
         * @implements INotifyPlayerAttributeSync
         * @constructor
         * @param {Attribute.INotifyPlayerAttributeSync=} [properties] Properties to set
         */
        function NotifyPlayerAttributeSync(properties) {
            this.Attributes = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyPlayerAttributeSync Attributes.
         * @member {Array.<Attribute.IPlayerAttributeEntry>} Attributes
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @instance
         */
        NotifyPlayerAttributeSync.prototype.Attributes = $util.emptyArray;

        /**
         * Creates a new NotifyPlayerAttributeSync instance using the specified properties.
         * @function create
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {Attribute.INotifyPlayerAttributeSync=} [properties] Properties to set
         * @returns {Attribute.NotifyPlayerAttributeSync} NotifyPlayerAttributeSync instance
         */
        NotifyPlayerAttributeSync.create = function create(properties) {
            return new NotifyPlayerAttributeSync(properties);
        };

        /**
         * Encodes the specified NotifyPlayerAttributeSync message. Does not implicitly {@link Attribute.NotifyPlayerAttributeSync.verify|verify} messages.
         * @function encode
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {Attribute.INotifyPlayerAttributeSync} message NotifyPlayerAttributeSync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyPlayerAttributeSync.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Attributes != null && message.Attributes.length)
                for (var i = 0; i < message.Attributes.length; ++i)
                    $root.Attribute.PlayerAttributeEntry.encode(message.Attributes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NotifyPlayerAttributeSync message, length delimited. Does not implicitly {@link Attribute.NotifyPlayerAttributeSync.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {Attribute.INotifyPlayerAttributeSync} message NotifyPlayerAttributeSync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyPlayerAttributeSync.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyPlayerAttributeSync message from the specified reader or buffer.
         * @function decode
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Attribute.NotifyPlayerAttributeSync} NotifyPlayerAttributeSync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyPlayerAttributeSync.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Attribute.NotifyPlayerAttributeSync();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Attributes && message.Attributes.length))
                            message.Attributes = [];
                        message.Attributes.push($root.Attribute.PlayerAttributeEntry.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyPlayerAttributeSync message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Attribute.NotifyPlayerAttributeSync} NotifyPlayerAttributeSync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyPlayerAttributeSync.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyPlayerAttributeSync message.
         * @function verify
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyPlayerAttributeSync.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Attributes != null && Object.hasOwnProperty.call(message, "Attributes")) {
                if (!Array.isArray(message.Attributes))
                    return "Attributes: array expected";
                for (var i = 0; i < message.Attributes.length; ++i) {
                    var error = $root.Attribute.PlayerAttributeEntry.verify(message.Attributes[i], long + 1);
                    if (error)
                        return "Attributes." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NotifyPlayerAttributeSync message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Attribute.NotifyPlayerAttributeSync} NotifyPlayerAttributeSync
         */
        NotifyPlayerAttributeSync.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Attribute.NotifyPlayerAttributeSync)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Attribute.NotifyPlayerAttributeSync: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Attribute.NotifyPlayerAttributeSync();
            if (object.Attributes) {
                if (!Array.isArray(object.Attributes))
                    throw TypeError(".Attribute.NotifyPlayerAttributeSync.Attributes: array expected");
                message.Attributes = [];
                for (var i = 0; i < object.Attributes.length; ++i) {
                    if (!$util.isObject(object.Attributes[i]))
                        throw TypeError(".Attribute.NotifyPlayerAttributeSync.Attributes: object expected");
                    message.Attributes[i] = $root.Attribute.PlayerAttributeEntry.fromObject(object.Attributes[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NotifyPlayerAttributeSync message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {Attribute.NotifyPlayerAttributeSync} message NotifyPlayerAttributeSync
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyPlayerAttributeSync.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Attributes = [];
            if (message.Attributes && message.Attributes.length) {
                object.Attributes = [];
                for (var j = 0; j < message.Attributes.length; ++j)
                    object.Attributes[j] = $root.Attribute.PlayerAttributeEntry.toObject(message.Attributes[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this NotifyPlayerAttributeSync to JSON.
         * @function toJSON
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyPlayerAttributeSync.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyPlayerAttributeSync
         * @function getTypeUrl
         * @memberof Attribute.NotifyPlayerAttributeSync
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyPlayerAttributeSync.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Attribute.NotifyPlayerAttributeSync";
        };

        return NotifyPlayerAttributeSync;
    })();

    Attribute.NotifyPlayerAttributeChanged = (function() {

        /**
         * Properties of a NotifyPlayerAttributeChanged.
         * @memberof Attribute
         * @interface INotifyPlayerAttributeChanged
         * @property {number|null} [Type] NotifyPlayerAttributeChanged Type
         * @property {number|Long|null} [Value] NotifyPlayerAttributeChanged Value
         */

        /**
         * Constructs a new NotifyPlayerAttributeChanged.
         * @memberof Attribute
         * @classdesc Represents a NotifyPlayerAttributeChanged.
         * @implements INotifyPlayerAttributeChanged
         * @constructor
         * @param {Attribute.INotifyPlayerAttributeChanged=} [properties] Properties to set
         */
        function NotifyPlayerAttributeChanged(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyPlayerAttributeChanged Type.
         * @member {number} Type
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @instance
         */
        NotifyPlayerAttributeChanged.prototype.Type = 0;

        /**
         * NotifyPlayerAttributeChanged Value.
         * @member {number|Long} Value
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @instance
         */
        NotifyPlayerAttributeChanged.prototype.Value = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new NotifyPlayerAttributeChanged instance using the specified properties.
         * @function create
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {Attribute.INotifyPlayerAttributeChanged=} [properties] Properties to set
         * @returns {Attribute.NotifyPlayerAttributeChanged} NotifyPlayerAttributeChanged instance
         */
        NotifyPlayerAttributeChanged.create = function create(properties) {
            return new NotifyPlayerAttributeChanged(properties);
        };

        /**
         * Encodes the specified NotifyPlayerAttributeChanged message. Does not implicitly {@link Attribute.NotifyPlayerAttributeChanged.verify|verify} messages.
         * @function encode
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {Attribute.INotifyPlayerAttributeChanged} message NotifyPlayerAttributeChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyPlayerAttributeChanged.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Type);
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.Value);
            return writer;
        };

        /**
         * Encodes the specified NotifyPlayerAttributeChanged message, length delimited. Does not implicitly {@link Attribute.NotifyPlayerAttributeChanged.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {Attribute.INotifyPlayerAttributeChanged} message NotifyPlayerAttributeChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyPlayerAttributeChanged.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyPlayerAttributeChanged message from the specified reader or buffer.
         * @function decode
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Attribute.NotifyPlayerAttributeChanged} NotifyPlayerAttributeChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyPlayerAttributeChanged.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Attribute.NotifyPlayerAttributeChanged();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Type = reader.int32();
                        break;
                    }
                case 2: {
                        message.Value = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyPlayerAttributeChanged message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Attribute.NotifyPlayerAttributeChanged} NotifyPlayerAttributeChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyPlayerAttributeChanged.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyPlayerAttributeChanged message.
         * @function verify
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyPlayerAttributeChanged.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                if (!$util.isInteger(message.Type))
                    return "Type: integer expected";
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                if (!$util.isInteger(message.Value) && !(message.Value && $util.isInteger(message.Value.low) && $util.isInteger(message.Value.high)))
                    return "Value: integer|Long expected";
            return null;
        };

        /**
         * Creates a NotifyPlayerAttributeChanged message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Attribute.NotifyPlayerAttributeChanged} NotifyPlayerAttributeChanged
         */
        NotifyPlayerAttributeChanged.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Attribute.NotifyPlayerAttributeChanged)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Attribute.NotifyPlayerAttributeChanged: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Attribute.NotifyPlayerAttributeChanged();
            if (object.Type != null)
                message.Type = object.Type | 0;
            if (object.Value != null)
                if ($util.Long)
                    message.Value = $util.Long.fromValue(object.Value, false);
                else if (typeof object.Value === "string")
                    message.Value = parseInt(object.Value, 10);
                else if (typeof object.Value === "number")
                    message.Value = object.Value;
                else if (typeof object.Value === "object")
                    message.Value = new $util.LongBits(object.Value.low >>> 0, object.Value.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a NotifyPlayerAttributeChanged message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {Attribute.NotifyPlayerAttributeChanged} message NotifyPlayerAttributeChanged
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyPlayerAttributeChanged.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.Type = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Value = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Value = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                object.Type = message.Type;
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Value = typeof message.Value === "number" ? BigInt(message.Value) : $util.Long.fromBits(message.Value.low >>> 0, message.Value.high >>> 0, false).toBigInt();
                else if (typeof message.Value === "number")
                    object.Value = options.longs === String ? String(message.Value) : message.Value;
                else
                    object.Value = options.longs === String ? $util.Long.prototype.toString.call(message.Value) : options.longs === Number ? new $util.LongBits(message.Value.low >>> 0, message.Value.high >>> 0).toNumber() : message.Value;
            return object;
        };

        /**
         * Converts this NotifyPlayerAttributeChanged to JSON.
         * @function toJSON
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyPlayerAttributeChanged.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyPlayerAttributeChanged
         * @function getTypeUrl
         * @memberof Attribute.NotifyPlayerAttributeChanged
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyPlayerAttributeChanged.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Attribute.NotifyPlayerAttributeChanged";
        };

        return NotifyPlayerAttributeChanged;
    })();

    return Attribute;
})();

$root.Room = (function() {

    /**
     * Namespace Room.
     * @exports Room
     * @namespace
     */
    var Room = {};

    /**
     * GameType enum.
     * @name Room.GameType
     * @enum {number}
     * @property {number} None=0 None value
     * @property {number} RockPaperScissors=1 RockPaperScissors value
     */
    Room.GameType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "None"] = 0;
        values[valuesById[1] = "RockPaperScissors"] = 1;
        return values;
    })();

    /**
     * RoomStatus enum.
     * @name Room.RoomStatus
     * @enum {number}
     * @property {number} None=0 None value
     * @property {number} Waiting=1 Waiting value
     * @property {number} Ready=2 Ready value
     * @property {number} Playing=3 Playing value
     * @property {number} Settling=4 Settling value
     * @property {number} Settled=5 Settled value
     * @property {number} Closed=6 Closed value
     * @property {number} Disbanded=7 Disbanded value
     */
    Room.RoomStatus = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "None"] = 0;
        values[valuesById[1] = "Waiting"] = 1;
        values[valuesById[2] = "Ready"] = 2;
        values[valuesById[3] = "Playing"] = 3;
        values[valuesById[4] = "Settling"] = 4;
        values[valuesById[5] = "Settled"] = 5;
        values[valuesById[6] = "Closed"] = 6;
        values[valuesById[7] = "Disbanded"] = 7;
        return values;
    })();

    /**
     * RoomChangeType enum.
     * @name Room.RoomChangeType
     * @enum {number}
     * @property {number} Created=0 Created value
     * @property {number} Joined=1 Joined value
     * @property {number} Left=2 Left value
     * @property {number} Started=3 Started value
     * @property {number} Settling=4 Settling value
     * @property {number} Settled=5 Settled value
     * @property {number} Closed=6 Closed value
     * @property {number} Disbanded=7 Disbanded value
     * @property {number} Reset=8 Reset value
     */
    Room.RoomChangeType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Created"] = 0;
        values[valuesById[1] = "Joined"] = 1;
        values[valuesById[2] = "Left"] = 2;
        values[valuesById[3] = "Started"] = 3;
        values[valuesById[4] = "Settling"] = 4;
        values[valuesById[5] = "Settled"] = 5;
        values[valuesById[6] = "Closed"] = 6;
        values[valuesById[7] = "Disbanded"] = 7;
        values[valuesById[8] = "Reset"] = 8;
        return values;
    })();

    /**
     * RoomPlayerOnlineStatus enum.
     * @name Room.RoomPlayerOnlineStatus
     * @enum {number}
     * @property {number} OnlineUnknown=0 OnlineUnknown value
     * @property {number} Online=1 Online value
     * @property {number} Reconnecting=2 Reconnecting value
     * @property {number} Offline=3 Offline value
     */
    Room.RoomPlayerOnlineStatus = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "OnlineUnknown"] = 0;
        values[valuesById[1] = "Online"] = 1;
        values[valuesById[2] = "Reconnecting"] = 2;
        values[valuesById[3] = "Offline"] = 3;
        return values;
    })();

    /**
     * RoomPlayerStatus enum.
     * @name Room.RoomPlayerStatus
     * @enum {number}
     * @property {number} PlayerStatusNone=0 PlayerStatusNone value
     * @property {number} Idle=1 Idle value
     * @property {number} ReadyInRoom=2 ReadyInRoom value
     * @property {number} InGame=3 InGame value
     * @property {number} Submitted=4 Submitted value
     * @property {number} SettlingInRoom=5 SettlingInRoom value
     * @property {number} SettledInRoom=6 SettledInRoom value
     */
    Room.RoomPlayerStatus = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "PlayerStatusNone"] = 0;
        values[valuesById[1] = "Idle"] = 1;
        values[valuesById[2] = "ReadyInRoom"] = 2;
        values[valuesById[3] = "InGame"] = 3;
        values[valuesById[4] = "Submitted"] = 4;
        values[valuesById[5] = "SettlingInRoom"] = 5;
        values[valuesById[6] = "SettledInRoom"] = 6;
        return values;
    })();

    Room.RoomPlayerInfo = (function() {

        /**
         * Properties of a RoomPlayerInfo.
         * @memberof Room
         * @interface IRoomPlayerInfo
         * @property {number|Long|null} [RoleId] RoomPlayerInfo RoleId
         * @property {number|null} [SeatIndex] RoomPlayerInfo SeatIndex
         * @property {boolean|null} [IsOwner] RoomPlayerInfo IsOwner
         * @property {string|null} [Name] RoomPlayerInfo Name
         * @property {number|null} [Avatar] RoomPlayerInfo Avatar
         * @property {Room.RoomPlayerOnlineStatus|null} [OnlineStatus] RoomPlayerInfo OnlineStatus
         * @property {Room.RoomPlayerStatus|null} [PlayerStatus] RoomPlayerInfo PlayerStatus
         */

        /**
         * Constructs a new RoomPlayerInfo.
         * @memberof Room
         * @classdesc Represents a RoomPlayerInfo.
         * @implements IRoomPlayerInfo
         * @constructor
         * @param {Room.IRoomPlayerInfo=} [properties] Properties to set
         */
        function RoomPlayerInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoomPlayerInfo RoleId.
         * @member {number|Long} RoleId
         * @memberof Room.RoomPlayerInfo
         * @instance
         */
        RoomPlayerInfo.prototype.RoleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoomPlayerInfo SeatIndex.
         * @member {number} SeatIndex
         * @memberof Room.RoomPlayerInfo
         * @instance
         */
        RoomPlayerInfo.prototype.SeatIndex = 0;

        /**
         * RoomPlayerInfo IsOwner.
         * @member {boolean} IsOwner
         * @memberof Room.RoomPlayerInfo
         * @instance
         */
        RoomPlayerInfo.prototype.IsOwner = false;

        /**
         * RoomPlayerInfo Name.
         * @member {string} Name
         * @memberof Room.RoomPlayerInfo
         * @instance
         */
        RoomPlayerInfo.prototype.Name = "";

        /**
         * RoomPlayerInfo Avatar.
         * @member {number} Avatar
         * @memberof Room.RoomPlayerInfo
         * @instance
         */
        RoomPlayerInfo.prototype.Avatar = 0;

        /**
         * RoomPlayerInfo OnlineStatus.
         * @member {Room.RoomPlayerOnlineStatus} OnlineStatus
         * @memberof Room.RoomPlayerInfo
         * @instance
         */
        RoomPlayerInfo.prototype.OnlineStatus = 0;

        /**
         * RoomPlayerInfo PlayerStatus.
         * @member {Room.RoomPlayerStatus} PlayerStatus
         * @memberof Room.RoomPlayerInfo
         * @instance
         */
        RoomPlayerInfo.prototype.PlayerStatus = 0;

        /**
         * Creates a new RoomPlayerInfo instance using the specified properties.
         * @function create
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {Room.IRoomPlayerInfo=} [properties] Properties to set
         * @returns {Room.RoomPlayerInfo} RoomPlayerInfo instance
         */
        RoomPlayerInfo.create = function create(properties) {
            return new RoomPlayerInfo(properties);
        };

        /**
         * Encodes the specified RoomPlayerInfo message. Does not implicitly {@link Room.RoomPlayerInfo.verify|verify} messages.
         * @function encode
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {Room.IRoomPlayerInfo} message RoomPlayerInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomPlayerInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoleId != null && Object.hasOwnProperty.call(message, "RoleId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoleId);
            if (message.SeatIndex != null && Object.hasOwnProperty.call(message, "SeatIndex"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.SeatIndex);
            if (message.IsOwner != null && Object.hasOwnProperty.call(message, "IsOwner"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.IsOwner);
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.Name);
            if (message.Avatar != null && Object.hasOwnProperty.call(message, "Avatar"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.Avatar);
            if (message.OnlineStatus != null && Object.hasOwnProperty.call(message, "OnlineStatus"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.OnlineStatus);
            if (message.PlayerStatus != null && Object.hasOwnProperty.call(message, "PlayerStatus"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.PlayerStatus);
            return writer;
        };

        /**
         * Encodes the specified RoomPlayerInfo message, length delimited. Does not implicitly {@link Room.RoomPlayerInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {Room.IRoomPlayerInfo} message RoomPlayerInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomPlayerInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RoomPlayerInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.RoomPlayerInfo} RoomPlayerInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomPlayerInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.RoomPlayerInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoleId = reader.int64();
                        break;
                    }
                case 2: {
                        message.SeatIndex = reader.int32();
                        break;
                    }
                case 3: {
                        message.IsOwner = reader.bool();
                        break;
                    }
                case 4: {
                        message.Name = reader.string();
                        break;
                    }
                case 5: {
                        message.Avatar = reader.uint32();
                        break;
                    }
                case 6: {
                        message.OnlineStatus = reader.int32();
                        break;
                    }
                case 7: {
                        message.PlayerStatus = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RoomPlayerInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.RoomPlayerInfo} RoomPlayerInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomPlayerInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomPlayerInfo message.
         * @function verify
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomPlayerInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoleId != null && Object.hasOwnProperty.call(message, "RoleId"))
                if (!$util.isInteger(message.RoleId) && !(message.RoleId && $util.isInteger(message.RoleId.low) && $util.isInteger(message.RoleId.high)))
                    return "RoleId: integer|Long expected";
            if (message.SeatIndex != null && Object.hasOwnProperty.call(message, "SeatIndex"))
                if (!$util.isInteger(message.SeatIndex))
                    return "SeatIndex: integer expected";
            if (message.IsOwner != null && Object.hasOwnProperty.call(message, "IsOwner"))
                if (typeof message.IsOwner !== "boolean")
                    return "IsOwner: boolean expected";
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                if (!$util.isString(message.Name))
                    return "Name: string expected";
            if (message.Avatar != null && Object.hasOwnProperty.call(message, "Avatar"))
                if (!$util.isInteger(message.Avatar))
                    return "Avatar: integer expected";
            if (message.OnlineStatus != null && Object.hasOwnProperty.call(message, "OnlineStatus"))
                switch (message.OnlineStatus) {
                default:
                    return "OnlineStatus: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            if (message.PlayerStatus != null && Object.hasOwnProperty.call(message, "PlayerStatus"))
                switch (message.PlayerStatus) {
                default:
                    return "PlayerStatus: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            return null;
        };

        /**
         * Creates a RoomPlayerInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.RoomPlayerInfo} RoomPlayerInfo
         */
        RoomPlayerInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.RoomPlayerInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.RoomPlayerInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.RoomPlayerInfo();
            if (object.RoleId != null)
                if ($util.Long)
                    message.RoleId = $util.Long.fromValue(object.RoleId, false);
                else if (typeof object.RoleId === "string")
                    message.RoleId = parseInt(object.RoleId, 10);
                else if (typeof object.RoleId === "number")
                    message.RoleId = object.RoleId;
                else if (typeof object.RoleId === "object")
                    message.RoleId = new $util.LongBits(object.RoleId.low >>> 0, object.RoleId.high >>> 0).toNumber();
            if (object.SeatIndex != null)
                message.SeatIndex = object.SeatIndex | 0;
            if (object.IsOwner != null)
                message.IsOwner = Boolean(object.IsOwner);
            if (object.Name != null)
                message.Name = String(object.Name);
            if (object.Avatar != null)
                message.Avatar = object.Avatar >>> 0;
            switch (object.OnlineStatus) {
            default:
                if (typeof object.OnlineStatus === "number") {
                    message.OnlineStatus = object.OnlineStatus;
                    break;
                }
                break;
            case "OnlineUnknown":
            case 0:
                message.OnlineStatus = 0;
                break;
            case "Online":
            case 1:
                message.OnlineStatus = 1;
                break;
            case "Reconnecting":
            case 2:
                message.OnlineStatus = 2;
                break;
            case "Offline":
            case 3:
                message.OnlineStatus = 3;
                break;
            }
            switch (object.PlayerStatus) {
            default:
                if (typeof object.PlayerStatus === "number") {
                    message.PlayerStatus = object.PlayerStatus;
                    break;
                }
                break;
            case "PlayerStatusNone":
            case 0:
                message.PlayerStatus = 0;
                break;
            case "Idle":
            case 1:
                message.PlayerStatus = 1;
                break;
            case "ReadyInRoom":
            case 2:
                message.PlayerStatus = 2;
                break;
            case "InGame":
            case 3:
                message.PlayerStatus = 3;
                break;
            case "Submitted":
            case 4:
                message.PlayerStatus = 4;
                break;
            case "SettlingInRoom":
            case 5:
                message.PlayerStatus = 5;
                break;
            case "SettledInRoom":
            case 6:
                message.PlayerStatus = 6;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a RoomPlayerInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {Room.RoomPlayerInfo} message RoomPlayerInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomPlayerInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoleId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.SeatIndex = 0;
                object.IsOwner = false;
                object.Name = "";
                object.Avatar = 0;
                object.OnlineStatus = options.enums === String ? "OnlineUnknown" : 0;
                object.PlayerStatus = options.enums === String ? "PlayerStatusNone" : 0;
            }
            if (message.RoleId != null && Object.hasOwnProperty.call(message, "RoleId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoleId = typeof message.RoleId === "number" ? BigInt(message.RoleId) : $util.Long.fromBits(message.RoleId.low >>> 0, message.RoleId.high >>> 0, false).toBigInt();
                else if (typeof message.RoleId === "number")
                    object.RoleId = options.longs === String ? String(message.RoleId) : message.RoleId;
                else
                    object.RoleId = options.longs === String ? $util.Long.prototype.toString.call(message.RoleId) : options.longs === Number ? new $util.LongBits(message.RoleId.low >>> 0, message.RoleId.high >>> 0).toNumber() : message.RoleId;
            if (message.SeatIndex != null && Object.hasOwnProperty.call(message, "SeatIndex"))
                object.SeatIndex = message.SeatIndex;
            if (message.IsOwner != null && Object.hasOwnProperty.call(message, "IsOwner"))
                object.IsOwner = message.IsOwner;
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                object.Name = message.Name;
            if (message.Avatar != null && Object.hasOwnProperty.call(message, "Avatar"))
                object.Avatar = message.Avatar;
            if (message.OnlineStatus != null && Object.hasOwnProperty.call(message, "OnlineStatus"))
                object.OnlineStatus = options.enums === String ? $root.Room.RoomPlayerOnlineStatus[message.OnlineStatus] === undefined ? message.OnlineStatus : $root.Room.RoomPlayerOnlineStatus[message.OnlineStatus] : message.OnlineStatus;
            if (message.PlayerStatus != null && Object.hasOwnProperty.call(message, "PlayerStatus"))
                object.PlayerStatus = options.enums === String ? $root.Room.RoomPlayerStatus[message.PlayerStatus] === undefined ? message.PlayerStatus : $root.Room.RoomPlayerStatus[message.PlayerStatus] : message.PlayerStatus;
            return object;
        };

        /**
         * Converts this RoomPlayerInfo to JSON.
         * @function toJSON
         * @memberof Room.RoomPlayerInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomPlayerInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoomPlayerInfo
         * @function getTypeUrl
         * @memberof Room.RoomPlayerInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoomPlayerInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.RoomPlayerInfo";
        };

        return RoomPlayerInfo;
    })();

    Room.RoomInfo = (function() {

        /**
         * Properties of a RoomInfo.
         * @memberof Room
         * @interface IRoomInfo
         * @property {number|Long|null} [RoomId] RoomInfo RoomId
         * @property {string|null} [Name] RoomInfo Name
         * @property {Room.GameType|null} [GameType] RoomInfo GameType
         * @property {Room.RoomStatus|null} [Status] RoomInfo Status
         * @property {number|null} [PlayerCount] RoomInfo PlayerCount
         * @property {number|null} [MinPlayerCount] RoomInfo MinPlayerCount
         * @property {number|null} [MaxPlayerCount] RoomInfo MaxPlayerCount
         * @property {number|Long|null} [OwnerRoleId] RoomInfo OwnerRoleId
         * @property {Array.<Room.IRoomPlayerInfo>|null} [Players] RoomInfo Players
         * @property {number|null} [Round] RoomInfo Round
         * @property {number|Long|null} [CreatedTime] RoomInfo CreatedTime
         * @property {number|Long|null} [UpdatedTime] RoomInfo UpdatedTime
         */

        /**
         * Constructs a new RoomInfo.
         * @memberof Room
         * @classdesc Represents a RoomInfo.
         * @implements IRoomInfo
         * @constructor
         * @param {Room.IRoomInfo=} [properties] Properties to set
         */
        function RoomInfo(properties) {
            this.Players = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoomInfo RoomId.
         * @member {number|Long} RoomId
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoomInfo Name.
         * @member {string} Name
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.Name = "";

        /**
         * RoomInfo GameType.
         * @member {Room.GameType} GameType
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.GameType = 0;

        /**
         * RoomInfo Status.
         * @member {Room.RoomStatus} Status
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.Status = 0;

        /**
         * RoomInfo PlayerCount.
         * @member {number} PlayerCount
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.PlayerCount = 0;

        /**
         * RoomInfo MinPlayerCount.
         * @member {number} MinPlayerCount
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.MinPlayerCount = 0;

        /**
         * RoomInfo MaxPlayerCount.
         * @member {number} MaxPlayerCount
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.MaxPlayerCount = 0;

        /**
         * RoomInfo OwnerRoleId.
         * @member {number|Long} OwnerRoleId
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.OwnerRoleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoomInfo Players.
         * @member {Array.<Room.IRoomPlayerInfo>} Players
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.Players = $util.emptyArray;

        /**
         * RoomInfo Round.
         * @member {number} Round
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.Round = 0;

        /**
         * RoomInfo CreatedTime.
         * @member {number|Long} CreatedTime
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.CreatedTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoomInfo UpdatedTime.
         * @member {number|Long} UpdatedTime
         * @memberof Room.RoomInfo
         * @instance
         */
        RoomInfo.prototype.UpdatedTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RoomInfo instance using the specified properties.
         * @function create
         * @memberof Room.RoomInfo
         * @static
         * @param {Room.IRoomInfo=} [properties] Properties to set
         * @returns {Room.RoomInfo} RoomInfo instance
         */
        RoomInfo.create = function create(properties) {
            return new RoomInfo(properties);
        };

        /**
         * Encodes the specified RoomInfo message. Does not implicitly {@link Room.RoomInfo.verify|verify} messages.
         * @function encode
         * @memberof Room.RoomInfo
         * @static
         * @param {Room.IRoomInfo} message RoomInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Name);
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.GameType);
            if (message.Status != null && Object.hasOwnProperty.call(message, "Status"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Status);
            if (message.PlayerCount != null && Object.hasOwnProperty.call(message, "PlayerCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.PlayerCount);
            if (message.MinPlayerCount != null && Object.hasOwnProperty.call(message, "MinPlayerCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.MinPlayerCount);
            if (message.MaxPlayerCount != null && Object.hasOwnProperty.call(message, "MaxPlayerCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.MaxPlayerCount);
            if (message.OwnerRoleId != null && Object.hasOwnProperty.call(message, "OwnerRoleId"))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.OwnerRoleId);
            if (message.Players != null && message.Players.length)
                for (var i = 0; i < message.Players.length; ++i)
                    $root.Room.RoomPlayerInfo.encode(message.Players[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.Round != null && Object.hasOwnProperty.call(message, "Round"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.Round);
            if (message.CreatedTime != null && Object.hasOwnProperty.call(message, "CreatedTime"))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.CreatedTime);
            if (message.UpdatedTime != null && Object.hasOwnProperty.call(message, "UpdatedTime"))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.UpdatedTime);
            return writer;
        };

        /**
         * Encodes the specified RoomInfo message, length delimited. Does not implicitly {@link Room.RoomInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.RoomInfo
         * @static
         * @param {Room.IRoomInfo} message RoomInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoomInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RoomInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Room.RoomInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.RoomInfo} RoomInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.RoomInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                case 2: {
                        message.Name = reader.string();
                        break;
                    }
                case 3: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 4: {
                        message.Status = reader.int32();
                        break;
                    }
                case 5: {
                        message.PlayerCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.MinPlayerCount = reader.int32();
                        break;
                    }
                case 7: {
                        message.MaxPlayerCount = reader.int32();
                        break;
                    }
                case 8: {
                        message.OwnerRoleId = reader.int64();
                        break;
                    }
                case 9: {
                        if (!(message.Players && message.Players.length))
                            message.Players = [];
                        message.Players.push($root.Room.RoomPlayerInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 10: {
                        message.Round = reader.int32();
                        break;
                    }
                case 11: {
                        message.CreatedTime = reader.int64();
                        break;
                    }
                case 12: {
                        message.UpdatedTime = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RoomInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.RoomInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.RoomInfo} RoomInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoomInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoomInfo message.
         * @function verify
         * @memberof Room.RoomInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoomInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                if (!$util.isString(message.Name))
                    return "Name: string expected";
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                switch (message.GameType) {
                default:
                    return "GameType: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.Status != null && Object.hasOwnProperty.call(message, "Status"))
                switch (message.Status) {
                default:
                    return "Status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                    break;
                }
            if (message.PlayerCount != null && Object.hasOwnProperty.call(message, "PlayerCount"))
                if (!$util.isInteger(message.PlayerCount))
                    return "PlayerCount: integer expected";
            if (message.MinPlayerCount != null && Object.hasOwnProperty.call(message, "MinPlayerCount"))
                if (!$util.isInteger(message.MinPlayerCount))
                    return "MinPlayerCount: integer expected";
            if (message.MaxPlayerCount != null && Object.hasOwnProperty.call(message, "MaxPlayerCount"))
                if (!$util.isInteger(message.MaxPlayerCount))
                    return "MaxPlayerCount: integer expected";
            if (message.OwnerRoleId != null && Object.hasOwnProperty.call(message, "OwnerRoleId"))
                if (!$util.isInteger(message.OwnerRoleId) && !(message.OwnerRoleId && $util.isInteger(message.OwnerRoleId.low) && $util.isInteger(message.OwnerRoleId.high)))
                    return "OwnerRoleId: integer|Long expected";
            if (message.Players != null && Object.hasOwnProperty.call(message, "Players")) {
                if (!Array.isArray(message.Players))
                    return "Players: array expected";
                for (var i = 0; i < message.Players.length; ++i) {
                    var error = $root.Room.RoomPlayerInfo.verify(message.Players[i], long + 1);
                    if (error)
                        return "Players." + error;
                }
            }
            if (message.Round != null && Object.hasOwnProperty.call(message, "Round"))
                if (!$util.isInteger(message.Round))
                    return "Round: integer expected";
            if (message.CreatedTime != null && Object.hasOwnProperty.call(message, "CreatedTime"))
                if (!$util.isInteger(message.CreatedTime) && !(message.CreatedTime && $util.isInteger(message.CreatedTime.low) && $util.isInteger(message.CreatedTime.high)))
                    return "CreatedTime: integer|Long expected";
            if (message.UpdatedTime != null && Object.hasOwnProperty.call(message, "UpdatedTime"))
                if (!$util.isInteger(message.UpdatedTime) && !(message.UpdatedTime && $util.isInteger(message.UpdatedTime.low) && $util.isInteger(message.UpdatedTime.high)))
                    return "UpdatedTime: integer|Long expected";
            return null;
        };

        /**
         * Creates a RoomInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.RoomInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.RoomInfo} RoomInfo
         */
        RoomInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.RoomInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.RoomInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.RoomInfo();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            if (object.Name != null)
                message.Name = String(object.Name);
            switch (object.GameType) {
            default:
                if (typeof object.GameType === "number") {
                    message.GameType = object.GameType;
                    break;
                }
                break;
            case "None":
            case 0:
                message.GameType = 0;
                break;
            case "RockPaperScissors":
            case 1:
                message.GameType = 1;
                break;
            }
            switch (object.Status) {
            default:
                if (typeof object.Status === "number") {
                    message.Status = object.Status;
                    break;
                }
                break;
            case "None":
            case 0:
                message.Status = 0;
                break;
            case "Waiting":
            case 1:
                message.Status = 1;
                break;
            case "Ready":
            case 2:
                message.Status = 2;
                break;
            case "Playing":
            case 3:
                message.Status = 3;
                break;
            case "Settling":
            case 4:
                message.Status = 4;
                break;
            case "Settled":
            case 5:
                message.Status = 5;
                break;
            case "Closed":
            case 6:
                message.Status = 6;
                break;
            case "Disbanded":
            case 7:
                message.Status = 7;
                break;
            }
            if (object.PlayerCount != null)
                message.PlayerCount = object.PlayerCount | 0;
            if (object.MinPlayerCount != null)
                message.MinPlayerCount = object.MinPlayerCount | 0;
            if (object.MaxPlayerCount != null)
                message.MaxPlayerCount = object.MaxPlayerCount | 0;
            if (object.OwnerRoleId != null)
                if ($util.Long)
                    message.OwnerRoleId = $util.Long.fromValue(object.OwnerRoleId, false);
                else if (typeof object.OwnerRoleId === "string")
                    message.OwnerRoleId = parseInt(object.OwnerRoleId, 10);
                else if (typeof object.OwnerRoleId === "number")
                    message.OwnerRoleId = object.OwnerRoleId;
                else if (typeof object.OwnerRoleId === "object")
                    message.OwnerRoleId = new $util.LongBits(object.OwnerRoleId.low >>> 0, object.OwnerRoleId.high >>> 0).toNumber();
            if (object.Players) {
                if (!Array.isArray(object.Players))
                    throw TypeError(".Room.RoomInfo.Players: array expected");
                message.Players = [];
                for (var i = 0; i < object.Players.length; ++i) {
                    if (!$util.isObject(object.Players[i]))
                        throw TypeError(".Room.RoomInfo.Players: object expected");
                    message.Players[i] = $root.Room.RoomPlayerInfo.fromObject(object.Players[i], long + 1);
                }
            }
            if (object.Round != null)
                message.Round = object.Round | 0;
            if (object.CreatedTime != null)
                if ($util.Long)
                    message.CreatedTime = $util.Long.fromValue(object.CreatedTime, false);
                else if (typeof object.CreatedTime === "string")
                    message.CreatedTime = parseInt(object.CreatedTime, 10);
                else if (typeof object.CreatedTime === "number")
                    message.CreatedTime = object.CreatedTime;
                else if (typeof object.CreatedTime === "object")
                    message.CreatedTime = new $util.LongBits(object.CreatedTime.low >>> 0, object.CreatedTime.high >>> 0).toNumber();
            if (object.UpdatedTime != null)
                if ($util.Long)
                    message.UpdatedTime = $util.Long.fromValue(object.UpdatedTime, false);
                else if (typeof object.UpdatedTime === "string")
                    message.UpdatedTime = parseInt(object.UpdatedTime, 10);
                else if (typeof object.UpdatedTime === "number")
                    message.UpdatedTime = object.UpdatedTime;
                else if (typeof object.UpdatedTime === "object")
                    message.UpdatedTime = new $util.LongBits(object.UpdatedTime.low >>> 0, object.UpdatedTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RoomInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.RoomInfo
         * @static
         * @param {Room.RoomInfo} message RoomInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoomInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Players = [];
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Name = "";
                object.GameType = options.enums === String ? "None" : 0;
                object.Status = options.enums === String ? "None" : 0;
                object.PlayerCount = 0;
                object.MinPlayerCount = 0;
                object.MaxPlayerCount = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.OwnerRoleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.OwnerRoleId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Round = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.CreatedTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.CreatedTime = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.UpdatedTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.UpdatedTime = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                object.Name = message.Name;
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                object.GameType = options.enums === String ? $root.Room.GameType[message.GameType] === undefined ? message.GameType : $root.Room.GameType[message.GameType] : message.GameType;
            if (message.Status != null && Object.hasOwnProperty.call(message, "Status"))
                object.Status = options.enums === String ? $root.Room.RoomStatus[message.Status] === undefined ? message.Status : $root.Room.RoomStatus[message.Status] : message.Status;
            if (message.PlayerCount != null && Object.hasOwnProperty.call(message, "PlayerCount"))
                object.PlayerCount = message.PlayerCount;
            if (message.MinPlayerCount != null && Object.hasOwnProperty.call(message, "MinPlayerCount"))
                object.MinPlayerCount = message.MinPlayerCount;
            if (message.MaxPlayerCount != null && Object.hasOwnProperty.call(message, "MaxPlayerCount"))
                object.MaxPlayerCount = message.MaxPlayerCount;
            if (message.OwnerRoleId != null && Object.hasOwnProperty.call(message, "OwnerRoleId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.OwnerRoleId = typeof message.OwnerRoleId === "number" ? BigInt(message.OwnerRoleId) : $util.Long.fromBits(message.OwnerRoleId.low >>> 0, message.OwnerRoleId.high >>> 0, false).toBigInt();
                else if (typeof message.OwnerRoleId === "number")
                    object.OwnerRoleId = options.longs === String ? String(message.OwnerRoleId) : message.OwnerRoleId;
                else
                    object.OwnerRoleId = options.longs === String ? $util.Long.prototype.toString.call(message.OwnerRoleId) : options.longs === Number ? new $util.LongBits(message.OwnerRoleId.low >>> 0, message.OwnerRoleId.high >>> 0).toNumber() : message.OwnerRoleId;
            if (message.Players && message.Players.length) {
                object.Players = [];
                for (var j = 0; j < message.Players.length; ++j)
                    object.Players[j] = $root.Room.RoomPlayerInfo.toObject(message.Players[j], options, q + 1);
            }
            if (message.Round != null && Object.hasOwnProperty.call(message, "Round"))
                object.Round = message.Round;
            if (message.CreatedTime != null && Object.hasOwnProperty.call(message, "CreatedTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.CreatedTime = typeof message.CreatedTime === "number" ? BigInt(message.CreatedTime) : $util.Long.fromBits(message.CreatedTime.low >>> 0, message.CreatedTime.high >>> 0, false).toBigInt();
                else if (typeof message.CreatedTime === "number")
                    object.CreatedTime = options.longs === String ? String(message.CreatedTime) : message.CreatedTime;
                else
                    object.CreatedTime = options.longs === String ? $util.Long.prototype.toString.call(message.CreatedTime) : options.longs === Number ? new $util.LongBits(message.CreatedTime.low >>> 0, message.CreatedTime.high >>> 0).toNumber() : message.CreatedTime;
            if (message.UpdatedTime != null && Object.hasOwnProperty.call(message, "UpdatedTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.UpdatedTime = typeof message.UpdatedTime === "number" ? BigInt(message.UpdatedTime) : $util.Long.fromBits(message.UpdatedTime.low >>> 0, message.UpdatedTime.high >>> 0, false).toBigInt();
                else if (typeof message.UpdatedTime === "number")
                    object.UpdatedTime = options.longs === String ? String(message.UpdatedTime) : message.UpdatedTime;
                else
                    object.UpdatedTime = options.longs === String ? $util.Long.prototype.toString.call(message.UpdatedTime) : options.longs === Number ? new $util.LongBits(message.UpdatedTime.low >>> 0, message.UpdatedTime.high >>> 0).toNumber() : message.UpdatedTime;
            return object;
        };

        /**
         * Converts this RoomInfo to JSON.
         * @function toJSON
         * @memberof Room.RoomInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoomInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoomInfo
         * @function getTypeUrl
         * @memberof Room.RoomInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoomInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.RoomInfo";
        };

        return RoomInfo;
    })();

    Room.ReqRoomList = (function() {

        /**
         * Properties of a ReqRoomList.
         * @memberof Room
         * @interface IReqRoomList
         * @property {Room.GameType|null} [GameType] ReqRoomList GameType
         * @property {boolean|null} [IncludeClosed] ReqRoomList IncludeClosed
         */

        /**
         * Constructs a new ReqRoomList.
         * @memberof Room
         * @classdesc Represents a ReqRoomList.
         * @implements IReqRoomList
         * @constructor
         * @param {Room.IReqRoomList=} [properties] Properties to set
         */
        function ReqRoomList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqRoomList GameType.
         * @member {Room.GameType} GameType
         * @memberof Room.ReqRoomList
         * @instance
         */
        ReqRoomList.prototype.GameType = 0;

        /**
         * ReqRoomList IncludeClosed.
         * @member {boolean} IncludeClosed
         * @memberof Room.ReqRoomList
         * @instance
         */
        ReqRoomList.prototype.IncludeClosed = false;

        /**
         * Creates a new ReqRoomList instance using the specified properties.
         * @function create
         * @memberof Room.ReqRoomList
         * @static
         * @param {Room.IReqRoomList=} [properties] Properties to set
         * @returns {Room.ReqRoomList} ReqRoomList instance
         */
        ReqRoomList.create = function create(properties) {
            return new ReqRoomList(properties);
        };

        /**
         * Encodes the specified ReqRoomList message. Does not implicitly {@link Room.ReqRoomList.verify|verify} messages.
         * @function encode
         * @memberof Room.ReqRoomList
         * @static
         * @param {Room.IReqRoomList} message ReqRoomList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRoomList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.GameType);
            if (message.IncludeClosed != null && Object.hasOwnProperty.call(message, "IncludeClosed"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.IncludeClosed);
            return writer;
        };

        /**
         * Encodes the specified ReqRoomList message, length delimited. Does not implicitly {@link Room.ReqRoomList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.ReqRoomList
         * @static
         * @param {Room.IReqRoomList} message ReqRoomList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRoomList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqRoomList message from the specified reader or buffer.
         * @function decode
         * @memberof Room.ReqRoomList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.ReqRoomList} ReqRoomList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRoomList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.ReqRoomList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 2: {
                        message.IncludeClosed = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqRoomList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.ReqRoomList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.ReqRoomList} ReqRoomList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRoomList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqRoomList message.
         * @function verify
         * @memberof Room.ReqRoomList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqRoomList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                switch (message.GameType) {
                default:
                    return "GameType: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.IncludeClosed != null && Object.hasOwnProperty.call(message, "IncludeClosed"))
                if (typeof message.IncludeClosed !== "boolean")
                    return "IncludeClosed: boolean expected";
            return null;
        };

        /**
         * Creates a ReqRoomList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.ReqRoomList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.ReqRoomList} ReqRoomList
         */
        ReqRoomList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.ReqRoomList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.ReqRoomList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.ReqRoomList();
            switch (object.GameType) {
            default:
                if (typeof object.GameType === "number") {
                    message.GameType = object.GameType;
                    break;
                }
                break;
            case "None":
            case 0:
                message.GameType = 0;
                break;
            case "RockPaperScissors":
            case 1:
                message.GameType = 1;
                break;
            }
            if (object.IncludeClosed != null)
                message.IncludeClosed = Boolean(object.IncludeClosed);
            return message;
        };

        /**
         * Creates a plain object from a ReqRoomList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.ReqRoomList
         * @static
         * @param {Room.ReqRoomList} message ReqRoomList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqRoomList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.GameType = options.enums === String ? "None" : 0;
                object.IncludeClosed = false;
            }
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                object.GameType = options.enums === String ? $root.Room.GameType[message.GameType] === undefined ? message.GameType : $root.Room.GameType[message.GameType] : message.GameType;
            if (message.IncludeClosed != null && Object.hasOwnProperty.call(message, "IncludeClosed"))
                object.IncludeClosed = message.IncludeClosed;
            return object;
        };

        /**
         * Converts this ReqRoomList to JSON.
         * @function toJSON
         * @memberof Room.ReqRoomList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqRoomList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqRoomList
         * @function getTypeUrl
         * @memberof Room.ReqRoomList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqRoomList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.ReqRoomList";
        };

        return ReqRoomList;
    })();

    Room.RespRoomList = (function() {

        /**
         * Properties of a RespRoomList.
         * @memberof Room
         * @interface IRespRoomList
         * @property {Array.<Room.IRoomInfo>|null} [Rooms] RespRoomList Rooms
         */

        /**
         * Constructs a new RespRoomList.
         * @memberof Room
         * @classdesc Represents a RespRoomList.
         * @implements IRespRoomList
         * @constructor
         * @param {Room.IRespRoomList=} [properties] Properties to set
         */
        function RespRoomList(properties) {
            this.Rooms = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespRoomList Rooms.
         * @member {Array.<Room.IRoomInfo>} Rooms
         * @memberof Room.RespRoomList
         * @instance
         */
        RespRoomList.prototype.Rooms = $util.emptyArray;

        /**
         * Creates a new RespRoomList instance using the specified properties.
         * @function create
         * @memberof Room.RespRoomList
         * @static
         * @param {Room.IRespRoomList=} [properties] Properties to set
         * @returns {Room.RespRoomList} RespRoomList instance
         */
        RespRoomList.create = function create(properties) {
            return new RespRoomList(properties);
        };

        /**
         * Encodes the specified RespRoomList message. Does not implicitly {@link Room.RespRoomList.verify|verify} messages.
         * @function encode
         * @memberof Room.RespRoomList
         * @static
         * @param {Room.IRespRoomList} message RespRoomList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRoomList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Rooms != null && message.Rooms.length)
                for (var i = 0; i < message.Rooms.length; ++i)
                    $root.Room.RoomInfo.encode(message.Rooms[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespRoomList message, length delimited. Does not implicitly {@link Room.RespRoomList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.RespRoomList
         * @static
         * @param {Room.IRespRoomList} message RespRoomList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRoomList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespRoomList message from the specified reader or buffer.
         * @function decode
         * @memberof Room.RespRoomList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.RespRoomList} RespRoomList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRoomList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.RespRoomList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Rooms && message.Rooms.length))
                            message.Rooms = [];
                        message.Rooms.push($root.Room.RoomInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespRoomList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.RespRoomList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.RespRoomList} RespRoomList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRoomList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespRoomList message.
         * @function verify
         * @memberof Room.RespRoomList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespRoomList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Rooms != null && Object.hasOwnProperty.call(message, "Rooms")) {
                if (!Array.isArray(message.Rooms))
                    return "Rooms: array expected";
                for (var i = 0; i < message.Rooms.length; ++i) {
                    var error = $root.Room.RoomInfo.verify(message.Rooms[i], long + 1);
                    if (error)
                        return "Rooms." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RespRoomList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.RespRoomList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.RespRoomList} RespRoomList
         */
        RespRoomList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.RespRoomList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.RespRoomList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.RespRoomList();
            if (object.Rooms) {
                if (!Array.isArray(object.Rooms))
                    throw TypeError(".Room.RespRoomList.Rooms: array expected");
                message.Rooms = [];
                for (var i = 0; i < object.Rooms.length; ++i) {
                    if (!$util.isObject(object.Rooms[i]))
                        throw TypeError(".Room.RespRoomList.Rooms: object expected");
                    message.Rooms[i] = $root.Room.RoomInfo.fromObject(object.Rooms[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespRoomList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.RespRoomList
         * @static
         * @param {Room.RespRoomList} message RespRoomList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespRoomList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Rooms = [];
            if (message.Rooms && message.Rooms.length) {
                object.Rooms = [];
                for (var j = 0; j < message.Rooms.length; ++j)
                    object.Rooms[j] = $root.Room.RoomInfo.toObject(message.Rooms[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this RespRoomList to JSON.
         * @function toJSON
         * @memberof Room.RespRoomList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespRoomList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespRoomList
         * @function getTypeUrl
         * @memberof Room.RespRoomList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespRoomList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.RespRoomList";
        };

        return RespRoomList;
    })();

    Room.ReqCreateRoom = (function() {

        /**
         * Properties of a ReqCreateRoom.
         * @memberof Room
         * @interface IReqCreateRoom
         * @property {Room.GameType|null} [GameType] ReqCreateRoom GameType
         * @property {string|null} [Name] ReqCreateRoom Name
         * @property {number|null} [MinPlayerCount] ReqCreateRoom MinPlayerCount
         * @property {number|null} [MaxPlayerCount] ReqCreateRoom MaxPlayerCount
         */

        /**
         * Constructs a new ReqCreateRoom.
         * @memberof Room
         * @classdesc Represents a ReqCreateRoom.
         * @implements IReqCreateRoom
         * @constructor
         * @param {Room.IReqCreateRoom=} [properties] Properties to set
         */
        function ReqCreateRoom(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqCreateRoom GameType.
         * @member {Room.GameType} GameType
         * @memberof Room.ReqCreateRoom
         * @instance
         */
        ReqCreateRoom.prototype.GameType = 0;

        /**
         * ReqCreateRoom Name.
         * @member {string} Name
         * @memberof Room.ReqCreateRoom
         * @instance
         */
        ReqCreateRoom.prototype.Name = "";

        /**
         * ReqCreateRoom MinPlayerCount.
         * @member {number} MinPlayerCount
         * @memberof Room.ReqCreateRoom
         * @instance
         */
        ReqCreateRoom.prototype.MinPlayerCount = 0;

        /**
         * ReqCreateRoom MaxPlayerCount.
         * @member {number} MaxPlayerCount
         * @memberof Room.ReqCreateRoom
         * @instance
         */
        ReqCreateRoom.prototype.MaxPlayerCount = 0;

        /**
         * Creates a new ReqCreateRoom instance using the specified properties.
         * @function create
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {Room.IReqCreateRoom=} [properties] Properties to set
         * @returns {Room.ReqCreateRoom} ReqCreateRoom instance
         */
        ReqCreateRoom.create = function create(properties) {
            return new ReqCreateRoom(properties);
        };

        /**
         * Encodes the specified ReqCreateRoom message. Does not implicitly {@link Room.ReqCreateRoom.verify|verify} messages.
         * @function encode
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {Room.IReqCreateRoom} message ReqCreateRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqCreateRoom.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.GameType);
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Name);
            if (message.MinPlayerCount != null && Object.hasOwnProperty.call(message, "MinPlayerCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.MinPlayerCount);
            if (message.MaxPlayerCount != null && Object.hasOwnProperty.call(message, "MaxPlayerCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.MaxPlayerCount);
            return writer;
        };

        /**
         * Encodes the specified ReqCreateRoom message, length delimited. Does not implicitly {@link Room.ReqCreateRoom.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {Room.IReqCreateRoom} message ReqCreateRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqCreateRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqCreateRoom message from the specified reader or buffer.
         * @function decode
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.ReqCreateRoom} ReqCreateRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqCreateRoom.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.ReqCreateRoom();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 2: {
                        message.Name = reader.string();
                        break;
                    }
                case 3: {
                        message.MinPlayerCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.MaxPlayerCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqCreateRoom message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.ReqCreateRoom} ReqCreateRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqCreateRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqCreateRoom message.
         * @function verify
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqCreateRoom.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                switch (message.GameType) {
                default:
                    return "GameType: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                if (!$util.isString(message.Name))
                    return "Name: string expected";
            if (message.MinPlayerCount != null && Object.hasOwnProperty.call(message, "MinPlayerCount"))
                if (!$util.isInteger(message.MinPlayerCount))
                    return "MinPlayerCount: integer expected";
            if (message.MaxPlayerCount != null && Object.hasOwnProperty.call(message, "MaxPlayerCount"))
                if (!$util.isInteger(message.MaxPlayerCount))
                    return "MaxPlayerCount: integer expected";
            return null;
        };

        /**
         * Creates a ReqCreateRoom message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.ReqCreateRoom} ReqCreateRoom
         */
        ReqCreateRoom.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.ReqCreateRoom)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.ReqCreateRoom: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.ReqCreateRoom();
            switch (object.GameType) {
            default:
                if (typeof object.GameType === "number") {
                    message.GameType = object.GameType;
                    break;
                }
                break;
            case "None":
            case 0:
                message.GameType = 0;
                break;
            case "RockPaperScissors":
            case 1:
                message.GameType = 1;
                break;
            }
            if (object.Name != null)
                message.Name = String(object.Name);
            if (object.MinPlayerCount != null)
                message.MinPlayerCount = object.MinPlayerCount | 0;
            if (object.MaxPlayerCount != null)
                message.MaxPlayerCount = object.MaxPlayerCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReqCreateRoom message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {Room.ReqCreateRoom} message ReqCreateRoom
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqCreateRoom.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.GameType = options.enums === String ? "None" : 0;
                object.Name = "";
                object.MinPlayerCount = 0;
                object.MaxPlayerCount = 0;
            }
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                object.GameType = options.enums === String ? $root.Room.GameType[message.GameType] === undefined ? message.GameType : $root.Room.GameType[message.GameType] : message.GameType;
            if (message.Name != null && Object.hasOwnProperty.call(message, "Name"))
                object.Name = message.Name;
            if (message.MinPlayerCount != null && Object.hasOwnProperty.call(message, "MinPlayerCount"))
                object.MinPlayerCount = message.MinPlayerCount;
            if (message.MaxPlayerCount != null && Object.hasOwnProperty.call(message, "MaxPlayerCount"))
                object.MaxPlayerCount = message.MaxPlayerCount;
            return object;
        };

        /**
         * Converts this ReqCreateRoom to JSON.
         * @function toJSON
         * @memberof Room.ReqCreateRoom
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqCreateRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqCreateRoom
         * @function getTypeUrl
         * @memberof Room.ReqCreateRoom
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqCreateRoom.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.ReqCreateRoom";
        };

        return ReqCreateRoom;
    })();

    Room.RespCreateRoom = (function() {

        /**
         * Properties of a RespCreateRoom.
         * @memberof Room
         * @interface IRespCreateRoom
         * @property {Room.IRoomInfo|null} [Room] RespCreateRoom Room
         */

        /**
         * Constructs a new RespCreateRoom.
         * @memberof Room
         * @classdesc Represents a RespCreateRoom.
         * @implements IRespCreateRoom
         * @constructor
         * @param {Room.IRespCreateRoom=} [properties] Properties to set
         */
        function RespCreateRoom(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespCreateRoom Room.
         * @member {Room.IRoomInfo|null|undefined} Room
         * @memberof Room.RespCreateRoom
         * @instance
         */
        RespCreateRoom.prototype.Room = null;

        /**
         * Creates a new RespCreateRoom instance using the specified properties.
         * @function create
         * @memberof Room.RespCreateRoom
         * @static
         * @param {Room.IRespCreateRoom=} [properties] Properties to set
         * @returns {Room.RespCreateRoom} RespCreateRoom instance
         */
        RespCreateRoom.create = function create(properties) {
            return new RespCreateRoom(properties);
        };

        /**
         * Encodes the specified RespCreateRoom message. Does not implicitly {@link Room.RespCreateRoom.verify|verify} messages.
         * @function encode
         * @memberof Room.RespCreateRoom
         * @static
         * @param {Room.IRespCreateRoom} message RespCreateRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespCreateRoom.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                $root.Room.RoomInfo.encode(message.Room, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespCreateRoom message, length delimited. Does not implicitly {@link Room.RespCreateRoom.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.RespCreateRoom
         * @static
         * @param {Room.IRespCreateRoom} message RespCreateRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespCreateRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespCreateRoom message from the specified reader or buffer.
         * @function decode
         * @memberof Room.RespCreateRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.RespCreateRoom} RespCreateRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespCreateRoom.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.RespCreateRoom();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Room = $root.Room.RoomInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespCreateRoom message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.RespCreateRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.RespCreateRoom} RespCreateRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespCreateRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespCreateRoom message.
         * @function verify
         * @memberof Room.RespCreateRoom
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespCreateRoom.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room")) {
                var error = $root.Room.RoomInfo.verify(message.Room, long + 1);
                if (error)
                    return "Room." + error;
            }
            return null;
        };

        /**
         * Creates a RespCreateRoom message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.RespCreateRoom
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.RespCreateRoom} RespCreateRoom
         */
        RespCreateRoom.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.RespCreateRoom)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.RespCreateRoom: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.RespCreateRoom();
            if (object.Room != null) {
                if (!$util.isObject(object.Room))
                    throw TypeError(".Room.RespCreateRoom.Room: object expected");
                message.Room = $root.Room.RoomInfo.fromObject(object.Room, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespCreateRoom message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.RespCreateRoom
         * @static
         * @param {Room.RespCreateRoom} message RespCreateRoom
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespCreateRoom.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.Room = null;
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                object.Room = $root.Room.RoomInfo.toObject(message.Room, options, q + 1);
            return object;
        };

        /**
         * Converts this RespCreateRoom to JSON.
         * @function toJSON
         * @memberof Room.RespCreateRoom
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespCreateRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespCreateRoom
         * @function getTypeUrl
         * @memberof Room.RespCreateRoom
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespCreateRoom.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.RespCreateRoom";
        };

        return RespCreateRoom;
    })();

    Room.ReqJoinRoom = (function() {

        /**
         * Properties of a ReqJoinRoom.
         * @memberof Room
         * @interface IReqJoinRoom
         * @property {number|Long|null} [RoomId] ReqJoinRoom RoomId
         */

        /**
         * Constructs a new ReqJoinRoom.
         * @memberof Room
         * @classdesc Represents a ReqJoinRoom.
         * @implements IReqJoinRoom
         * @constructor
         * @param {Room.IReqJoinRoom=} [properties] Properties to set
         */
        function ReqJoinRoom(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqJoinRoom RoomId.
         * @member {number|Long} RoomId
         * @memberof Room.ReqJoinRoom
         * @instance
         */
        ReqJoinRoom.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqJoinRoom instance using the specified properties.
         * @function create
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {Room.IReqJoinRoom=} [properties] Properties to set
         * @returns {Room.ReqJoinRoom} ReqJoinRoom instance
         */
        ReqJoinRoom.create = function create(properties) {
            return new ReqJoinRoom(properties);
        };

        /**
         * Encodes the specified ReqJoinRoom message. Does not implicitly {@link Room.ReqJoinRoom.verify|verify} messages.
         * @function encode
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {Room.IReqJoinRoom} message ReqJoinRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqJoinRoom.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            return writer;
        };

        /**
         * Encodes the specified ReqJoinRoom message, length delimited. Does not implicitly {@link Room.ReqJoinRoom.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {Room.IReqJoinRoom} message ReqJoinRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqJoinRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqJoinRoom message from the specified reader or buffer.
         * @function decode
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.ReqJoinRoom} ReqJoinRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqJoinRoom.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.ReqJoinRoom();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqJoinRoom message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.ReqJoinRoom} ReqJoinRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqJoinRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqJoinRoom message.
         * @function verify
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqJoinRoom.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqJoinRoom message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.ReqJoinRoom} ReqJoinRoom
         */
        ReqJoinRoom.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.ReqJoinRoom)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.ReqJoinRoom: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.ReqJoinRoom();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqJoinRoom message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {Room.ReqJoinRoom} message ReqJoinRoom
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqJoinRoom.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            return object;
        };

        /**
         * Converts this ReqJoinRoom to JSON.
         * @function toJSON
         * @memberof Room.ReqJoinRoom
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqJoinRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqJoinRoom
         * @function getTypeUrl
         * @memberof Room.ReqJoinRoom
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqJoinRoom.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.ReqJoinRoom";
        };

        return ReqJoinRoom;
    })();

    Room.RespJoinRoom = (function() {

        /**
         * Properties of a RespJoinRoom.
         * @memberof Room
         * @interface IRespJoinRoom
         * @property {Room.IRoomInfo|null} [Room] RespJoinRoom Room
         */

        /**
         * Constructs a new RespJoinRoom.
         * @memberof Room
         * @classdesc Represents a RespJoinRoom.
         * @implements IRespJoinRoom
         * @constructor
         * @param {Room.IRespJoinRoom=} [properties] Properties to set
         */
        function RespJoinRoom(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespJoinRoom Room.
         * @member {Room.IRoomInfo|null|undefined} Room
         * @memberof Room.RespJoinRoom
         * @instance
         */
        RespJoinRoom.prototype.Room = null;

        /**
         * Creates a new RespJoinRoom instance using the specified properties.
         * @function create
         * @memberof Room.RespJoinRoom
         * @static
         * @param {Room.IRespJoinRoom=} [properties] Properties to set
         * @returns {Room.RespJoinRoom} RespJoinRoom instance
         */
        RespJoinRoom.create = function create(properties) {
            return new RespJoinRoom(properties);
        };

        /**
         * Encodes the specified RespJoinRoom message. Does not implicitly {@link Room.RespJoinRoom.verify|verify} messages.
         * @function encode
         * @memberof Room.RespJoinRoom
         * @static
         * @param {Room.IRespJoinRoom} message RespJoinRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespJoinRoom.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                $root.Room.RoomInfo.encode(message.Room, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespJoinRoom message, length delimited. Does not implicitly {@link Room.RespJoinRoom.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.RespJoinRoom
         * @static
         * @param {Room.IRespJoinRoom} message RespJoinRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespJoinRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespJoinRoom message from the specified reader or buffer.
         * @function decode
         * @memberof Room.RespJoinRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.RespJoinRoom} RespJoinRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespJoinRoom.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.RespJoinRoom();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Room = $root.Room.RoomInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespJoinRoom message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.RespJoinRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.RespJoinRoom} RespJoinRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespJoinRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespJoinRoom message.
         * @function verify
         * @memberof Room.RespJoinRoom
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespJoinRoom.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room")) {
                var error = $root.Room.RoomInfo.verify(message.Room, long + 1);
                if (error)
                    return "Room." + error;
            }
            return null;
        };

        /**
         * Creates a RespJoinRoom message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.RespJoinRoom
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.RespJoinRoom} RespJoinRoom
         */
        RespJoinRoom.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.RespJoinRoom)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.RespJoinRoom: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.RespJoinRoom();
            if (object.Room != null) {
                if (!$util.isObject(object.Room))
                    throw TypeError(".Room.RespJoinRoom.Room: object expected");
                message.Room = $root.Room.RoomInfo.fromObject(object.Room, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespJoinRoom message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.RespJoinRoom
         * @static
         * @param {Room.RespJoinRoom} message RespJoinRoom
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespJoinRoom.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.Room = null;
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                object.Room = $root.Room.RoomInfo.toObject(message.Room, options, q + 1);
            return object;
        };

        /**
         * Converts this RespJoinRoom to JSON.
         * @function toJSON
         * @memberof Room.RespJoinRoom
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespJoinRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespJoinRoom
         * @function getTypeUrl
         * @memberof Room.RespJoinRoom
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespJoinRoom.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.RespJoinRoom";
        };

        return RespJoinRoom;
    })();

    Room.ReqLeaveRoom = (function() {

        /**
         * Properties of a ReqLeaveRoom.
         * @memberof Room
         * @interface IReqLeaveRoom
         * @property {number|Long|null} [RoomId] ReqLeaveRoom RoomId
         */

        /**
         * Constructs a new ReqLeaveRoom.
         * @memberof Room
         * @classdesc Represents a ReqLeaveRoom.
         * @implements IReqLeaveRoom
         * @constructor
         * @param {Room.IReqLeaveRoom=} [properties] Properties to set
         */
        function ReqLeaveRoom(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqLeaveRoom RoomId.
         * @member {number|Long} RoomId
         * @memberof Room.ReqLeaveRoom
         * @instance
         */
        ReqLeaveRoom.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqLeaveRoom instance using the specified properties.
         * @function create
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {Room.IReqLeaveRoom=} [properties] Properties to set
         * @returns {Room.ReqLeaveRoom} ReqLeaveRoom instance
         */
        ReqLeaveRoom.create = function create(properties) {
            return new ReqLeaveRoom(properties);
        };

        /**
         * Encodes the specified ReqLeaveRoom message. Does not implicitly {@link Room.ReqLeaveRoom.verify|verify} messages.
         * @function encode
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {Room.IReqLeaveRoom} message ReqLeaveRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqLeaveRoom.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            return writer;
        };

        /**
         * Encodes the specified ReqLeaveRoom message, length delimited. Does not implicitly {@link Room.ReqLeaveRoom.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {Room.IReqLeaveRoom} message ReqLeaveRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqLeaveRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqLeaveRoom message from the specified reader or buffer.
         * @function decode
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.ReqLeaveRoom} ReqLeaveRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqLeaveRoom.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.ReqLeaveRoom();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqLeaveRoom message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.ReqLeaveRoom} ReqLeaveRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqLeaveRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqLeaveRoom message.
         * @function verify
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqLeaveRoom.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqLeaveRoom message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.ReqLeaveRoom} ReqLeaveRoom
         */
        ReqLeaveRoom.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.ReqLeaveRoom)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.ReqLeaveRoom: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.ReqLeaveRoom();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqLeaveRoom message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {Room.ReqLeaveRoom} message ReqLeaveRoom
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqLeaveRoom.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            return object;
        };

        /**
         * Converts this ReqLeaveRoom to JSON.
         * @function toJSON
         * @memberof Room.ReqLeaveRoom
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqLeaveRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqLeaveRoom
         * @function getTypeUrl
         * @memberof Room.ReqLeaveRoom
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqLeaveRoom.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.ReqLeaveRoom";
        };

        return ReqLeaveRoom;
    })();

    Room.RespLeaveRoom = (function() {

        /**
         * Properties of a RespLeaveRoom.
         * @memberof Room
         * @interface IRespLeaveRoom
         * @property {Room.IRoomInfo|null} [Room] RespLeaveRoom Room
         */

        /**
         * Constructs a new RespLeaveRoom.
         * @memberof Room
         * @classdesc Represents a RespLeaveRoom.
         * @implements IRespLeaveRoom
         * @constructor
         * @param {Room.IRespLeaveRoom=} [properties] Properties to set
         */
        function RespLeaveRoom(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespLeaveRoom Room.
         * @member {Room.IRoomInfo|null|undefined} Room
         * @memberof Room.RespLeaveRoom
         * @instance
         */
        RespLeaveRoom.prototype.Room = null;

        /**
         * Creates a new RespLeaveRoom instance using the specified properties.
         * @function create
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {Room.IRespLeaveRoom=} [properties] Properties to set
         * @returns {Room.RespLeaveRoom} RespLeaveRoom instance
         */
        RespLeaveRoom.create = function create(properties) {
            return new RespLeaveRoom(properties);
        };

        /**
         * Encodes the specified RespLeaveRoom message. Does not implicitly {@link Room.RespLeaveRoom.verify|verify} messages.
         * @function encode
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {Room.IRespLeaveRoom} message RespLeaveRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespLeaveRoom.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                $root.Room.RoomInfo.encode(message.Room, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespLeaveRoom message, length delimited. Does not implicitly {@link Room.RespLeaveRoom.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {Room.IRespLeaveRoom} message RespLeaveRoom message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespLeaveRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespLeaveRoom message from the specified reader or buffer.
         * @function decode
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.RespLeaveRoom} RespLeaveRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespLeaveRoom.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.RespLeaveRoom();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Room = $root.Room.RoomInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespLeaveRoom message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.RespLeaveRoom} RespLeaveRoom
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespLeaveRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespLeaveRoom message.
         * @function verify
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespLeaveRoom.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room")) {
                var error = $root.Room.RoomInfo.verify(message.Room, long + 1);
                if (error)
                    return "Room." + error;
            }
            return null;
        };

        /**
         * Creates a RespLeaveRoom message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.RespLeaveRoom} RespLeaveRoom
         */
        RespLeaveRoom.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.RespLeaveRoom)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.RespLeaveRoom: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.RespLeaveRoom();
            if (object.Room != null) {
                if (!$util.isObject(object.Room))
                    throw TypeError(".Room.RespLeaveRoom.Room: object expected");
                message.Room = $root.Room.RoomInfo.fromObject(object.Room, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespLeaveRoom message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {Room.RespLeaveRoom} message RespLeaveRoom
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespLeaveRoom.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.Room = null;
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                object.Room = $root.Room.RoomInfo.toObject(message.Room, options, q + 1);
            return object;
        };

        /**
         * Converts this RespLeaveRoom to JSON.
         * @function toJSON
         * @memberof Room.RespLeaveRoom
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespLeaveRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespLeaveRoom
         * @function getTypeUrl
         * @memberof Room.RespLeaveRoom
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespLeaveRoom.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.RespLeaveRoom";
        };

        return RespLeaveRoom;
    })();

    Room.ReqStartRoomGame = (function() {

        /**
         * Properties of a ReqStartRoomGame.
         * @memberof Room
         * @interface IReqStartRoomGame
         * @property {number|Long|null} [RoomId] ReqStartRoomGame RoomId
         */

        /**
         * Constructs a new ReqStartRoomGame.
         * @memberof Room
         * @classdesc Represents a ReqStartRoomGame.
         * @implements IReqStartRoomGame
         * @constructor
         * @param {Room.IReqStartRoomGame=} [properties] Properties to set
         */
        function ReqStartRoomGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqStartRoomGame RoomId.
         * @member {number|Long} RoomId
         * @memberof Room.ReqStartRoomGame
         * @instance
         */
        ReqStartRoomGame.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqStartRoomGame instance using the specified properties.
         * @function create
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {Room.IReqStartRoomGame=} [properties] Properties to set
         * @returns {Room.ReqStartRoomGame} ReqStartRoomGame instance
         */
        ReqStartRoomGame.create = function create(properties) {
            return new ReqStartRoomGame(properties);
        };

        /**
         * Encodes the specified ReqStartRoomGame message. Does not implicitly {@link Room.ReqStartRoomGame.verify|verify} messages.
         * @function encode
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {Room.IReqStartRoomGame} message ReqStartRoomGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqStartRoomGame.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            return writer;
        };

        /**
         * Encodes the specified ReqStartRoomGame message, length delimited. Does not implicitly {@link Room.ReqStartRoomGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {Room.IReqStartRoomGame} message ReqStartRoomGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqStartRoomGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqStartRoomGame message from the specified reader or buffer.
         * @function decode
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.ReqStartRoomGame} ReqStartRoomGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqStartRoomGame.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.ReqStartRoomGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqStartRoomGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.ReqStartRoomGame} ReqStartRoomGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqStartRoomGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqStartRoomGame message.
         * @function verify
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqStartRoomGame.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqStartRoomGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.ReqStartRoomGame} ReqStartRoomGame
         */
        ReqStartRoomGame.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.ReqStartRoomGame)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.ReqStartRoomGame: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.ReqStartRoomGame();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqStartRoomGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {Room.ReqStartRoomGame} message ReqStartRoomGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqStartRoomGame.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            return object;
        };

        /**
         * Converts this ReqStartRoomGame to JSON.
         * @function toJSON
         * @memberof Room.ReqStartRoomGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqStartRoomGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqStartRoomGame
         * @function getTypeUrl
         * @memberof Room.ReqStartRoomGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqStartRoomGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.ReqStartRoomGame";
        };

        return ReqStartRoomGame;
    })();

    Room.RespStartRoomGame = (function() {

        /**
         * Properties of a RespStartRoomGame.
         * @memberof Room
         * @interface IRespStartRoomGame
         * @property {Room.IRoomInfo|null} [Room] RespStartRoomGame Room
         */

        /**
         * Constructs a new RespStartRoomGame.
         * @memberof Room
         * @classdesc Represents a RespStartRoomGame.
         * @implements IRespStartRoomGame
         * @constructor
         * @param {Room.IRespStartRoomGame=} [properties] Properties to set
         */
        function RespStartRoomGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespStartRoomGame Room.
         * @member {Room.IRoomInfo|null|undefined} Room
         * @memberof Room.RespStartRoomGame
         * @instance
         */
        RespStartRoomGame.prototype.Room = null;

        /**
         * Creates a new RespStartRoomGame instance using the specified properties.
         * @function create
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {Room.IRespStartRoomGame=} [properties] Properties to set
         * @returns {Room.RespStartRoomGame} RespStartRoomGame instance
         */
        RespStartRoomGame.create = function create(properties) {
            return new RespStartRoomGame(properties);
        };

        /**
         * Encodes the specified RespStartRoomGame message. Does not implicitly {@link Room.RespStartRoomGame.verify|verify} messages.
         * @function encode
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {Room.IRespStartRoomGame} message RespStartRoomGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespStartRoomGame.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                $root.Room.RoomInfo.encode(message.Room, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespStartRoomGame message, length delimited. Does not implicitly {@link Room.RespStartRoomGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {Room.IRespStartRoomGame} message RespStartRoomGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespStartRoomGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespStartRoomGame message from the specified reader or buffer.
         * @function decode
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.RespStartRoomGame} RespStartRoomGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespStartRoomGame.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.RespStartRoomGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Room = $root.Room.RoomInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespStartRoomGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.RespStartRoomGame} RespStartRoomGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespStartRoomGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespStartRoomGame message.
         * @function verify
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespStartRoomGame.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room")) {
                var error = $root.Room.RoomInfo.verify(message.Room, long + 1);
                if (error)
                    return "Room." + error;
            }
            return null;
        };

        /**
         * Creates a RespStartRoomGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.RespStartRoomGame} RespStartRoomGame
         */
        RespStartRoomGame.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.RespStartRoomGame)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.RespStartRoomGame: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.RespStartRoomGame();
            if (object.Room != null) {
                if (!$util.isObject(object.Room))
                    throw TypeError(".Room.RespStartRoomGame.Room: object expected");
                message.Room = $root.Room.RoomInfo.fromObject(object.Room, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespStartRoomGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {Room.RespStartRoomGame} message RespStartRoomGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespStartRoomGame.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.Room = null;
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                object.Room = $root.Room.RoomInfo.toObject(message.Room, options, q + 1);
            return object;
        };

        /**
         * Converts this RespStartRoomGame to JSON.
         * @function toJSON
         * @memberof Room.RespStartRoomGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespStartRoomGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespStartRoomGame
         * @function getTypeUrl
         * @memberof Room.RespStartRoomGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespStartRoomGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.RespStartRoomGame";
        };

        return RespStartRoomGame;
    })();

    Room.NotifyRoomChanged = (function() {

        /**
         * Properties of a NotifyRoomChanged.
         * @memberof Room
         * @interface INotifyRoomChanged
         * @property {Room.RoomChangeType|null} [ChangeType] NotifyRoomChanged ChangeType
         * @property {Room.IRoomInfo|null} [Room] NotifyRoomChanged Room
         */

        /**
         * Constructs a new NotifyRoomChanged.
         * @memberof Room
         * @classdesc Represents a NotifyRoomChanged.
         * @implements INotifyRoomChanged
         * @constructor
         * @param {Room.INotifyRoomChanged=} [properties] Properties to set
         */
        function NotifyRoomChanged(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyRoomChanged ChangeType.
         * @member {Room.RoomChangeType} ChangeType
         * @memberof Room.NotifyRoomChanged
         * @instance
         */
        NotifyRoomChanged.prototype.ChangeType = 0;

        /**
         * NotifyRoomChanged Room.
         * @member {Room.IRoomInfo|null|undefined} Room
         * @memberof Room.NotifyRoomChanged
         * @instance
         */
        NotifyRoomChanged.prototype.Room = null;

        /**
         * Creates a new NotifyRoomChanged instance using the specified properties.
         * @function create
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {Room.INotifyRoomChanged=} [properties] Properties to set
         * @returns {Room.NotifyRoomChanged} NotifyRoomChanged instance
         */
        NotifyRoomChanged.create = function create(properties) {
            return new NotifyRoomChanged(properties);
        };

        /**
         * Encodes the specified NotifyRoomChanged message. Does not implicitly {@link Room.NotifyRoomChanged.verify|verify} messages.
         * @function encode
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {Room.INotifyRoomChanged} message NotifyRoomChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyRoomChanged.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ChangeType != null && Object.hasOwnProperty.call(message, "ChangeType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ChangeType);
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                $root.Room.RoomInfo.encode(message.Room, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NotifyRoomChanged message, length delimited. Does not implicitly {@link Room.NotifyRoomChanged.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {Room.INotifyRoomChanged} message NotifyRoomChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyRoomChanged.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyRoomChanged message from the specified reader or buffer.
         * @function decode
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Room.NotifyRoomChanged} NotifyRoomChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyRoomChanged.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Room.NotifyRoomChanged();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ChangeType = reader.int32();
                        break;
                    }
                case 2: {
                        message.Room = $root.Room.RoomInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyRoomChanged message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Room.NotifyRoomChanged} NotifyRoomChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyRoomChanged.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyRoomChanged message.
         * @function verify
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyRoomChanged.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ChangeType != null && Object.hasOwnProperty.call(message, "ChangeType"))
                switch (message.ChangeType) {
                default:
                    return "ChangeType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                    break;
                }
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room")) {
                var error = $root.Room.RoomInfo.verify(message.Room, long + 1);
                if (error)
                    return "Room." + error;
            }
            return null;
        };

        /**
         * Creates a NotifyRoomChanged message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Room.NotifyRoomChanged} NotifyRoomChanged
         */
        NotifyRoomChanged.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Room.NotifyRoomChanged)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Room.NotifyRoomChanged: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Room.NotifyRoomChanged();
            switch (object.ChangeType) {
            default:
                if (typeof object.ChangeType === "number") {
                    message.ChangeType = object.ChangeType;
                    break;
                }
                break;
            case "Created":
            case 0:
                message.ChangeType = 0;
                break;
            case "Joined":
            case 1:
                message.ChangeType = 1;
                break;
            case "Left":
            case 2:
                message.ChangeType = 2;
                break;
            case "Started":
            case 3:
                message.ChangeType = 3;
                break;
            case "Settling":
            case 4:
                message.ChangeType = 4;
                break;
            case "Settled":
            case 5:
                message.ChangeType = 5;
                break;
            case "Closed":
            case 6:
                message.ChangeType = 6;
                break;
            case "Disbanded":
            case 7:
                message.ChangeType = 7;
                break;
            case "Reset":
            case 8:
                message.ChangeType = 8;
                break;
            }
            if (object.Room != null) {
                if (!$util.isObject(object.Room))
                    throw TypeError(".Room.NotifyRoomChanged.Room: object expected");
                message.Room = $root.Room.RoomInfo.fromObject(object.Room, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NotifyRoomChanged message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {Room.NotifyRoomChanged} message NotifyRoomChanged
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyRoomChanged.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.ChangeType = options.enums === String ? "Created" : 0;
                object.Room = null;
            }
            if (message.ChangeType != null && Object.hasOwnProperty.call(message, "ChangeType"))
                object.ChangeType = options.enums === String ? $root.Room.RoomChangeType[message.ChangeType] === undefined ? message.ChangeType : $root.Room.RoomChangeType[message.ChangeType] : message.ChangeType;
            if (message.Room != null && Object.hasOwnProperty.call(message, "Room"))
                object.Room = $root.Room.RoomInfo.toObject(message.Room, options, q + 1);
            return object;
        };

        /**
         * Converts this NotifyRoomChanged to JSON.
         * @function toJSON
         * @memberof Room.NotifyRoomChanged
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyRoomChanged.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyRoomChanged
         * @function getTypeUrl
         * @memberof Room.NotifyRoomChanged
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyRoomChanged.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Room.NotifyRoomChanged";
        };

        return NotifyRoomChanged;
    })();

    return Room;
})();

$root.RockPaperScissors = (function() {

    /**
     * Namespace RockPaperScissors.
     * @exports RockPaperScissors
     * @namespace
     */
    var RockPaperScissors = {};

    /**
     * RockPaperScissorsGesture enum.
     * @name RockPaperScissors.RockPaperScissorsGesture
     * @enum {number}
     * @property {number} None=0 None value
     * @property {number} Rock=1 Rock value
     * @property {number} Scissors=2 Scissors value
     * @property {number} Paper=3 Paper value
     */
    RockPaperScissors.RockPaperScissorsGesture = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "None"] = 0;
        values[valuesById[1] = "Rock"] = 1;
        values[valuesById[2] = "Scissors"] = 2;
        values[valuesById[3] = "Paper"] = 3;
        return values;
    })();

    RockPaperScissors.RockPaperScissorsPlayerInfo = (function() {

        /**
         * Properties of a RockPaperScissorsPlayerInfo.
         * @memberof RockPaperScissors
         * @interface IRockPaperScissorsPlayerInfo
         * @property {number|Long|null} [RoleId] RockPaperScissorsPlayerInfo RoleId
         * @property {boolean|null} [HasGesture] RockPaperScissorsPlayerInfo HasGesture
         * @property {RockPaperScissors.RockPaperScissorsGesture|null} [Gesture] RockPaperScissorsPlayerInfo Gesture
         */

        /**
         * Constructs a new RockPaperScissorsPlayerInfo.
         * @memberof RockPaperScissors
         * @classdesc Represents a RockPaperScissorsPlayerInfo.
         * @implements IRockPaperScissorsPlayerInfo
         * @constructor
         * @param {RockPaperScissors.IRockPaperScissorsPlayerInfo=} [properties] Properties to set
         */
        function RockPaperScissorsPlayerInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RockPaperScissorsPlayerInfo RoleId.
         * @member {number|Long} RoleId
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @instance
         */
        RockPaperScissorsPlayerInfo.prototype.RoleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RockPaperScissorsPlayerInfo HasGesture.
         * @member {boolean} HasGesture
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @instance
         */
        RockPaperScissorsPlayerInfo.prototype.HasGesture = false;

        /**
         * RockPaperScissorsPlayerInfo Gesture.
         * @member {RockPaperScissors.RockPaperScissorsGesture} Gesture
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @instance
         */
        RockPaperScissorsPlayerInfo.prototype.Gesture = 0;

        /**
         * Creates a new RockPaperScissorsPlayerInfo instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {RockPaperScissors.IRockPaperScissorsPlayerInfo=} [properties] Properties to set
         * @returns {RockPaperScissors.RockPaperScissorsPlayerInfo} RockPaperScissorsPlayerInfo instance
         */
        RockPaperScissorsPlayerInfo.create = function create(properties) {
            return new RockPaperScissorsPlayerInfo(properties);
        };

        /**
         * Encodes the specified RockPaperScissorsPlayerInfo message. Does not implicitly {@link RockPaperScissors.RockPaperScissorsPlayerInfo.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {RockPaperScissors.IRockPaperScissorsPlayerInfo} message RockPaperScissorsPlayerInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RockPaperScissorsPlayerInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoleId != null && Object.hasOwnProperty.call(message, "RoleId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoleId);
            if (message.HasGesture != null && Object.hasOwnProperty.call(message, "HasGesture"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.HasGesture);
            if (message.Gesture != null && Object.hasOwnProperty.call(message, "Gesture"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Gesture);
            return writer;
        };

        /**
         * Encodes the specified RockPaperScissorsPlayerInfo message, length delimited. Does not implicitly {@link RockPaperScissors.RockPaperScissorsPlayerInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {RockPaperScissors.IRockPaperScissorsPlayerInfo} message RockPaperScissorsPlayerInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RockPaperScissorsPlayerInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RockPaperScissorsPlayerInfo message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.RockPaperScissorsPlayerInfo} RockPaperScissorsPlayerInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RockPaperScissorsPlayerInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.RockPaperScissorsPlayerInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoleId = reader.int64();
                        break;
                    }
                case 2: {
                        message.HasGesture = reader.bool();
                        break;
                    }
                case 3: {
                        message.Gesture = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RockPaperScissorsPlayerInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.RockPaperScissorsPlayerInfo} RockPaperScissorsPlayerInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RockPaperScissorsPlayerInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RockPaperScissorsPlayerInfo message.
         * @function verify
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RockPaperScissorsPlayerInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoleId != null && Object.hasOwnProperty.call(message, "RoleId"))
                if (!$util.isInteger(message.RoleId) && !(message.RoleId && $util.isInteger(message.RoleId.low) && $util.isInteger(message.RoleId.high)))
                    return "RoleId: integer|Long expected";
            if (message.HasGesture != null && Object.hasOwnProperty.call(message, "HasGesture"))
                if (typeof message.HasGesture !== "boolean")
                    return "HasGesture: boolean expected";
            if (message.Gesture != null && Object.hasOwnProperty.call(message, "Gesture"))
                switch (message.Gesture) {
                default:
                    return "Gesture: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            return null;
        };

        /**
         * Creates a RockPaperScissorsPlayerInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.RockPaperScissorsPlayerInfo} RockPaperScissorsPlayerInfo
         */
        RockPaperScissorsPlayerInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.RockPaperScissorsPlayerInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.RockPaperScissorsPlayerInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.RockPaperScissorsPlayerInfo();
            if (object.RoleId != null)
                if ($util.Long)
                    message.RoleId = $util.Long.fromValue(object.RoleId, false);
                else if (typeof object.RoleId === "string")
                    message.RoleId = parseInt(object.RoleId, 10);
                else if (typeof object.RoleId === "number")
                    message.RoleId = object.RoleId;
                else if (typeof object.RoleId === "object")
                    message.RoleId = new $util.LongBits(object.RoleId.low >>> 0, object.RoleId.high >>> 0).toNumber();
            if (object.HasGesture != null)
                message.HasGesture = Boolean(object.HasGesture);
            switch (object.Gesture) {
            default:
                if (typeof object.Gesture === "number") {
                    message.Gesture = object.Gesture;
                    break;
                }
                break;
            case "None":
            case 0:
                message.Gesture = 0;
                break;
            case "Rock":
            case 1:
                message.Gesture = 1;
                break;
            case "Scissors":
            case 2:
                message.Gesture = 2;
                break;
            case "Paper":
            case 3:
                message.Gesture = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a RockPaperScissorsPlayerInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {RockPaperScissors.RockPaperScissorsPlayerInfo} message RockPaperScissorsPlayerInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RockPaperScissorsPlayerInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoleId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.HasGesture = false;
                object.Gesture = options.enums === String ? "None" : 0;
            }
            if (message.RoleId != null && Object.hasOwnProperty.call(message, "RoleId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoleId = typeof message.RoleId === "number" ? BigInt(message.RoleId) : $util.Long.fromBits(message.RoleId.low >>> 0, message.RoleId.high >>> 0, false).toBigInt();
                else if (typeof message.RoleId === "number")
                    object.RoleId = options.longs === String ? String(message.RoleId) : message.RoleId;
                else
                    object.RoleId = options.longs === String ? $util.Long.prototype.toString.call(message.RoleId) : options.longs === Number ? new $util.LongBits(message.RoleId.low >>> 0, message.RoleId.high >>> 0).toNumber() : message.RoleId;
            if (message.HasGesture != null && Object.hasOwnProperty.call(message, "HasGesture"))
                object.HasGesture = message.HasGesture;
            if (message.Gesture != null && Object.hasOwnProperty.call(message, "Gesture"))
                object.Gesture = options.enums === String ? $root.RockPaperScissors.RockPaperScissorsGesture[message.Gesture] === undefined ? message.Gesture : $root.RockPaperScissors.RockPaperScissorsGesture[message.Gesture] : message.Gesture;
            return object;
        };

        /**
         * Converts this RockPaperScissorsPlayerInfo to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RockPaperScissorsPlayerInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RockPaperScissorsPlayerInfo
         * @function getTypeUrl
         * @memberof RockPaperScissors.RockPaperScissorsPlayerInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RockPaperScissorsPlayerInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.RockPaperScissorsPlayerInfo";
        };

        return RockPaperScissorsPlayerInfo;
    })();

    RockPaperScissors.RockPaperScissorsGameInfo = (function() {

        /**
         * Properties of a RockPaperScissorsGameInfo.
         * @memberof RockPaperScissors
         * @interface IRockPaperScissorsGameInfo
         * @property {number|Long|null} [RoomId] RockPaperScissorsGameInfo RoomId
         * @property {number|null} [Round] RockPaperScissorsGameInfo Round
         * @property {number|Long|null} [WinnerRoleId] RockPaperScissorsGameInfo WinnerRoleId
         * @property {Array.<RockPaperScissors.IRockPaperScissorsPlayerInfo>|null} [Players] RockPaperScissorsGameInfo Players
         */

        /**
         * Constructs a new RockPaperScissorsGameInfo.
         * @memberof RockPaperScissors
         * @classdesc Represents a RockPaperScissorsGameInfo.
         * @implements IRockPaperScissorsGameInfo
         * @constructor
         * @param {RockPaperScissors.IRockPaperScissorsGameInfo=} [properties] Properties to set
         */
        function RockPaperScissorsGameInfo(properties) {
            this.Players = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RockPaperScissorsGameInfo RoomId.
         * @member {number|Long} RoomId
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @instance
         */
        RockPaperScissorsGameInfo.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RockPaperScissorsGameInfo Round.
         * @member {number} Round
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @instance
         */
        RockPaperScissorsGameInfo.prototype.Round = 0;

        /**
         * RockPaperScissorsGameInfo WinnerRoleId.
         * @member {number|Long} WinnerRoleId
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @instance
         */
        RockPaperScissorsGameInfo.prototype.WinnerRoleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RockPaperScissorsGameInfo Players.
         * @member {Array.<RockPaperScissors.IRockPaperScissorsPlayerInfo>} Players
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @instance
         */
        RockPaperScissorsGameInfo.prototype.Players = $util.emptyArray;

        /**
         * Creates a new RockPaperScissorsGameInfo instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IRockPaperScissorsGameInfo=} [properties] Properties to set
         * @returns {RockPaperScissors.RockPaperScissorsGameInfo} RockPaperScissorsGameInfo instance
         */
        RockPaperScissorsGameInfo.create = function create(properties) {
            return new RockPaperScissorsGameInfo(properties);
        };

        /**
         * Encodes the specified RockPaperScissorsGameInfo message. Does not implicitly {@link RockPaperScissors.RockPaperScissorsGameInfo.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IRockPaperScissorsGameInfo} message RockPaperScissorsGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RockPaperScissorsGameInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            if (message.Round != null && Object.hasOwnProperty.call(message, "Round"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Round);
            if (message.WinnerRoleId != null && Object.hasOwnProperty.call(message, "WinnerRoleId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.WinnerRoleId);
            if (message.Players != null && message.Players.length)
                for (var i = 0; i < message.Players.length; ++i)
                    $root.RockPaperScissors.RockPaperScissorsPlayerInfo.encode(message.Players[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RockPaperScissorsGameInfo message, length delimited. Does not implicitly {@link RockPaperScissors.RockPaperScissorsGameInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IRockPaperScissorsGameInfo} message RockPaperScissorsGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RockPaperScissorsGameInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RockPaperScissorsGameInfo message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.RockPaperScissorsGameInfo} RockPaperScissorsGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RockPaperScissorsGameInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.RockPaperScissorsGameInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                case 2: {
                        message.Round = reader.int32();
                        break;
                    }
                case 3: {
                        message.WinnerRoleId = reader.int64();
                        break;
                    }
                case 4: {
                        if (!(message.Players && message.Players.length))
                            message.Players = [];
                        message.Players.push($root.RockPaperScissors.RockPaperScissorsPlayerInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RockPaperScissorsGameInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.RockPaperScissorsGameInfo} RockPaperScissorsGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RockPaperScissorsGameInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RockPaperScissorsGameInfo message.
         * @function verify
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RockPaperScissorsGameInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            if (message.Round != null && Object.hasOwnProperty.call(message, "Round"))
                if (!$util.isInteger(message.Round))
                    return "Round: integer expected";
            if (message.WinnerRoleId != null && Object.hasOwnProperty.call(message, "WinnerRoleId"))
                if (!$util.isInteger(message.WinnerRoleId) && !(message.WinnerRoleId && $util.isInteger(message.WinnerRoleId.low) && $util.isInteger(message.WinnerRoleId.high)))
                    return "WinnerRoleId: integer|Long expected";
            if (message.Players != null && Object.hasOwnProperty.call(message, "Players")) {
                if (!Array.isArray(message.Players))
                    return "Players: array expected";
                for (var i = 0; i < message.Players.length; ++i) {
                    var error = $root.RockPaperScissors.RockPaperScissorsPlayerInfo.verify(message.Players[i], long + 1);
                    if (error)
                        return "Players." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RockPaperScissorsGameInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.RockPaperScissorsGameInfo} RockPaperScissorsGameInfo
         */
        RockPaperScissorsGameInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.RockPaperScissorsGameInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.RockPaperScissorsGameInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.RockPaperScissorsGameInfo();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            if (object.Round != null)
                message.Round = object.Round | 0;
            if (object.WinnerRoleId != null)
                if ($util.Long)
                    message.WinnerRoleId = $util.Long.fromValue(object.WinnerRoleId, false);
                else if (typeof object.WinnerRoleId === "string")
                    message.WinnerRoleId = parseInt(object.WinnerRoleId, 10);
                else if (typeof object.WinnerRoleId === "number")
                    message.WinnerRoleId = object.WinnerRoleId;
                else if (typeof object.WinnerRoleId === "object")
                    message.WinnerRoleId = new $util.LongBits(object.WinnerRoleId.low >>> 0, object.WinnerRoleId.high >>> 0).toNumber();
            if (object.Players) {
                if (!Array.isArray(object.Players))
                    throw TypeError(".RockPaperScissors.RockPaperScissorsGameInfo.Players: array expected");
                message.Players = [];
                for (var i = 0; i < object.Players.length; ++i) {
                    if (!$util.isObject(object.Players[i]))
                        throw TypeError(".RockPaperScissors.RockPaperScissorsGameInfo.Players: object expected");
                    message.Players[i] = $root.RockPaperScissors.RockPaperScissorsPlayerInfo.fromObject(object.Players[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RockPaperScissorsGameInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.RockPaperScissorsGameInfo} message RockPaperScissorsGameInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RockPaperScissorsGameInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Players = [];
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Round = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.WinnerRoleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.WinnerRoleId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            if (message.Round != null && Object.hasOwnProperty.call(message, "Round"))
                object.Round = message.Round;
            if (message.WinnerRoleId != null && Object.hasOwnProperty.call(message, "WinnerRoleId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.WinnerRoleId = typeof message.WinnerRoleId === "number" ? BigInt(message.WinnerRoleId) : $util.Long.fromBits(message.WinnerRoleId.low >>> 0, message.WinnerRoleId.high >>> 0, false).toBigInt();
                else if (typeof message.WinnerRoleId === "number")
                    object.WinnerRoleId = options.longs === String ? String(message.WinnerRoleId) : message.WinnerRoleId;
                else
                    object.WinnerRoleId = options.longs === String ? $util.Long.prototype.toString.call(message.WinnerRoleId) : options.longs === Number ? new $util.LongBits(message.WinnerRoleId.low >>> 0, message.WinnerRoleId.high >>> 0).toNumber() : message.WinnerRoleId;
            if (message.Players && message.Players.length) {
                object.Players = [];
                for (var j = 0; j < message.Players.length; ++j)
                    object.Players[j] = $root.RockPaperScissors.RockPaperScissorsPlayerInfo.toObject(message.Players[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this RockPaperScissorsGameInfo to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RockPaperScissorsGameInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RockPaperScissorsGameInfo
         * @function getTypeUrl
         * @memberof RockPaperScissors.RockPaperScissorsGameInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RockPaperScissorsGameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.RockPaperScissorsGameInfo";
        };

        return RockPaperScissorsGameInfo;
    })();

    RockPaperScissors.ReqRockPaperScissorsGameInfo = (function() {

        /**
         * Properties of a ReqRockPaperScissorsGameInfo.
         * @memberof RockPaperScissors
         * @interface IReqRockPaperScissorsGameInfo
         * @property {number|Long|null} [RoomId] ReqRockPaperScissorsGameInfo RoomId
         */

        /**
         * Constructs a new ReqRockPaperScissorsGameInfo.
         * @memberof RockPaperScissors
         * @classdesc Represents a ReqRockPaperScissorsGameInfo.
         * @implements IReqRockPaperScissorsGameInfo
         * @constructor
         * @param {RockPaperScissors.IReqRockPaperScissorsGameInfo=} [properties] Properties to set
         */
        function ReqRockPaperScissorsGameInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqRockPaperScissorsGameInfo RoomId.
         * @member {number|Long} RoomId
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @instance
         */
        ReqRockPaperScissorsGameInfo.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqRockPaperScissorsGameInfo instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IReqRockPaperScissorsGameInfo=} [properties] Properties to set
         * @returns {RockPaperScissors.ReqRockPaperScissorsGameInfo} ReqRockPaperScissorsGameInfo instance
         */
        ReqRockPaperScissorsGameInfo.create = function create(properties) {
            return new ReqRockPaperScissorsGameInfo(properties);
        };

        /**
         * Encodes the specified ReqRockPaperScissorsGameInfo message. Does not implicitly {@link RockPaperScissors.ReqRockPaperScissorsGameInfo.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IReqRockPaperScissorsGameInfo} message ReqRockPaperScissorsGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRockPaperScissorsGameInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            return writer;
        };

        /**
         * Encodes the specified ReqRockPaperScissorsGameInfo message, length delimited. Does not implicitly {@link RockPaperScissors.ReqRockPaperScissorsGameInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IReqRockPaperScissorsGameInfo} message ReqRockPaperScissorsGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRockPaperScissorsGameInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqRockPaperScissorsGameInfo message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.ReqRockPaperScissorsGameInfo} ReqRockPaperScissorsGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRockPaperScissorsGameInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.ReqRockPaperScissorsGameInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqRockPaperScissorsGameInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.ReqRockPaperScissorsGameInfo} ReqRockPaperScissorsGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRockPaperScissorsGameInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqRockPaperScissorsGameInfo message.
         * @function verify
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqRockPaperScissorsGameInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqRockPaperScissorsGameInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.ReqRockPaperScissorsGameInfo} ReqRockPaperScissorsGameInfo
         */
        ReqRockPaperScissorsGameInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.ReqRockPaperScissorsGameInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.ReqRockPaperScissorsGameInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.ReqRockPaperScissorsGameInfo();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqRockPaperScissorsGameInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.ReqRockPaperScissorsGameInfo} message ReqRockPaperScissorsGameInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqRockPaperScissorsGameInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            return object;
        };

        /**
         * Converts this ReqRockPaperScissorsGameInfo to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqRockPaperScissorsGameInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqRockPaperScissorsGameInfo
         * @function getTypeUrl
         * @memberof RockPaperScissors.ReqRockPaperScissorsGameInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqRockPaperScissorsGameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.ReqRockPaperScissorsGameInfo";
        };

        return ReqRockPaperScissorsGameInfo;
    })();

    RockPaperScissors.RespRockPaperScissorsGameInfo = (function() {

        /**
         * Properties of a RespRockPaperScissorsGameInfo.
         * @memberof RockPaperScissors
         * @interface IRespRockPaperScissorsGameInfo
         * @property {RockPaperScissors.IRockPaperScissorsGameInfo|null} [GameInfo] RespRockPaperScissorsGameInfo GameInfo
         */

        /**
         * Constructs a new RespRockPaperScissorsGameInfo.
         * @memberof RockPaperScissors
         * @classdesc Represents a RespRockPaperScissorsGameInfo.
         * @implements IRespRockPaperScissorsGameInfo
         * @constructor
         * @param {RockPaperScissors.IRespRockPaperScissorsGameInfo=} [properties] Properties to set
         */
        function RespRockPaperScissorsGameInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespRockPaperScissorsGameInfo GameInfo.
         * @member {RockPaperScissors.IRockPaperScissorsGameInfo|null|undefined} GameInfo
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @instance
         */
        RespRockPaperScissorsGameInfo.prototype.GameInfo = null;

        /**
         * Creates a new RespRockPaperScissorsGameInfo instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IRespRockPaperScissorsGameInfo=} [properties] Properties to set
         * @returns {RockPaperScissors.RespRockPaperScissorsGameInfo} RespRockPaperScissorsGameInfo instance
         */
        RespRockPaperScissorsGameInfo.create = function create(properties) {
            return new RespRockPaperScissorsGameInfo(properties);
        };

        /**
         * Encodes the specified RespRockPaperScissorsGameInfo message. Does not implicitly {@link RockPaperScissors.RespRockPaperScissorsGameInfo.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IRespRockPaperScissorsGameInfo} message RespRockPaperScissorsGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRockPaperScissorsGameInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                $root.RockPaperScissors.RockPaperScissorsGameInfo.encode(message.GameInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespRockPaperScissorsGameInfo message, length delimited. Does not implicitly {@link RockPaperScissors.RespRockPaperScissorsGameInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.IRespRockPaperScissorsGameInfo} message RespRockPaperScissorsGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRockPaperScissorsGameInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespRockPaperScissorsGameInfo message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.RespRockPaperScissorsGameInfo} RespRockPaperScissorsGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRockPaperScissorsGameInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.RespRockPaperScissorsGameInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespRockPaperScissorsGameInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.RespRockPaperScissorsGameInfo} RespRockPaperScissorsGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRockPaperScissorsGameInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespRockPaperScissorsGameInfo message.
         * @function verify
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespRockPaperScissorsGameInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo")) {
                var error = $root.RockPaperScissors.RockPaperScissorsGameInfo.verify(message.GameInfo, long + 1);
                if (error)
                    return "GameInfo." + error;
            }
            return null;
        };

        /**
         * Creates a RespRockPaperScissorsGameInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.RespRockPaperScissorsGameInfo} RespRockPaperScissorsGameInfo
         */
        RespRockPaperScissorsGameInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.RespRockPaperScissorsGameInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.RespRockPaperScissorsGameInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.RespRockPaperScissorsGameInfo();
            if (object.GameInfo != null) {
                if (!$util.isObject(object.GameInfo))
                    throw TypeError(".RockPaperScissors.RespRockPaperScissorsGameInfo.GameInfo: object expected");
                message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.fromObject(object.GameInfo, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespRockPaperScissorsGameInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {RockPaperScissors.RespRockPaperScissorsGameInfo} message RespRockPaperScissorsGameInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespRockPaperScissorsGameInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.GameInfo = null;
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                object.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.toObject(message.GameInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this RespRockPaperScissorsGameInfo to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespRockPaperScissorsGameInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespRockPaperScissorsGameInfo
         * @function getTypeUrl
         * @memberof RockPaperScissors.RespRockPaperScissorsGameInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespRockPaperScissorsGameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.RespRockPaperScissorsGameInfo";
        };

        return RespRockPaperScissorsGameInfo;
    })();

    RockPaperScissors.ReqSubmitRockPaperScissorsGesture = (function() {

        /**
         * Properties of a ReqSubmitRockPaperScissorsGesture.
         * @memberof RockPaperScissors
         * @interface IReqSubmitRockPaperScissorsGesture
         * @property {number|Long|null} [RoomId] ReqSubmitRockPaperScissorsGesture RoomId
         * @property {RockPaperScissors.RockPaperScissorsGesture|null} [Gesture] ReqSubmitRockPaperScissorsGesture Gesture
         */

        /**
         * Constructs a new ReqSubmitRockPaperScissorsGesture.
         * @memberof RockPaperScissors
         * @classdesc Represents a ReqSubmitRockPaperScissorsGesture.
         * @implements IReqSubmitRockPaperScissorsGesture
         * @constructor
         * @param {RockPaperScissors.IReqSubmitRockPaperScissorsGesture=} [properties] Properties to set
         */
        function ReqSubmitRockPaperScissorsGesture(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqSubmitRockPaperScissorsGesture RoomId.
         * @member {number|Long} RoomId
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @instance
         */
        ReqSubmitRockPaperScissorsGesture.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReqSubmitRockPaperScissorsGesture Gesture.
         * @member {RockPaperScissors.RockPaperScissorsGesture} Gesture
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @instance
         */
        ReqSubmitRockPaperScissorsGesture.prototype.Gesture = 0;

        /**
         * Creates a new ReqSubmitRockPaperScissorsGesture instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.IReqSubmitRockPaperScissorsGesture=} [properties] Properties to set
         * @returns {RockPaperScissors.ReqSubmitRockPaperScissorsGesture} ReqSubmitRockPaperScissorsGesture instance
         */
        ReqSubmitRockPaperScissorsGesture.create = function create(properties) {
            return new ReqSubmitRockPaperScissorsGesture(properties);
        };

        /**
         * Encodes the specified ReqSubmitRockPaperScissorsGesture message. Does not implicitly {@link RockPaperScissors.ReqSubmitRockPaperScissorsGesture.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.IReqSubmitRockPaperScissorsGesture} message ReqSubmitRockPaperScissorsGesture message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqSubmitRockPaperScissorsGesture.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            if (message.Gesture != null && Object.hasOwnProperty.call(message, "Gesture"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Gesture);
            return writer;
        };

        /**
         * Encodes the specified ReqSubmitRockPaperScissorsGesture message, length delimited. Does not implicitly {@link RockPaperScissors.ReqSubmitRockPaperScissorsGesture.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.IReqSubmitRockPaperScissorsGesture} message ReqSubmitRockPaperScissorsGesture message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqSubmitRockPaperScissorsGesture.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqSubmitRockPaperScissorsGesture message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.ReqSubmitRockPaperScissorsGesture} ReqSubmitRockPaperScissorsGesture
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqSubmitRockPaperScissorsGesture.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.ReqSubmitRockPaperScissorsGesture();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                case 2: {
                        message.Gesture = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqSubmitRockPaperScissorsGesture message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.ReqSubmitRockPaperScissorsGesture} ReqSubmitRockPaperScissorsGesture
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqSubmitRockPaperScissorsGesture.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqSubmitRockPaperScissorsGesture message.
         * @function verify
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqSubmitRockPaperScissorsGesture.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            if (message.Gesture != null && Object.hasOwnProperty.call(message, "Gesture"))
                switch (message.Gesture) {
                default:
                    return "Gesture: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            return null;
        };

        /**
         * Creates a ReqSubmitRockPaperScissorsGesture message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.ReqSubmitRockPaperScissorsGesture} ReqSubmitRockPaperScissorsGesture
         */
        ReqSubmitRockPaperScissorsGesture.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.ReqSubmitRockPaperScissorsGesture)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.ReqSubmitRockPaperScissorsGesture: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.ReqSubmitRockPaperScissorsGesture();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            switch (object.Gesture) {
            default:
                if (typeof object.Gesture === "number") {
                    message.Gesture = object.Gesture;
                    break;
                }
                break;
            case "None":
            case 0:
                message.Gesture = 0;
                break;
            case "Rock":
            case 1:
                message.Gesture = 1;
                break;
            case "Scissors":
            case 2:
                message.Gesture = 2;
                break;
            case "Paper":
            case 3:
                message.Gesture = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a ReqSubmitRockPaperScissorsGesture message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.ReqSubmitRockPaperScissorsGesture} message ReqSubmitRockPaperScissorsGesture
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqSubmitRockPaperScissorsGesture.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Gesture = options.enums === String ? "None" : 0;
            }
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            if (message.Gesture != null && Object.hasOwnProperty.call(message, "Gesture"))
                object.Gesture = options.enums === String ? $root.RockPaperScissors.RockPaperScissorsGesture[message.Gesture] === undefined ? message.Gesture : $root.RockPaperScissors.RockPaperScissorsGesture[message.Gesture] : message.Gesture;
            return object;
        };

        /**
         * Converts this ReqSubmitRockPaperScissorsGesture to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqSubmitRockPaperScissorsGesture.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqSubmitRockPaperScissorsGesture
         * @function getTypeUrl
         * @memberof RockPaperScissors.ReqSubmitRockPaperScissorsGesture
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqSubmitRockPaperScissorsGesture.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.ReqSubmitRockPaperScissorsGesture";
        };

        return ReqSubmitRockPaperScissorsGesture;
    })();

    RockPaperScissors.RespSubmitRockPaperScissorsGesture = (function() {

        /**
         * Properties of a RespSubmitRockPaperScissorsGesture.
         * @memberof RockPaperScissors
         * @interface IRespSubmitRockPaperScissorsGesture
         * @property {RockPaperScissors.IRockPaperScissorsGameInfo|null} [GameInfo] RespSubmitRockPaperScissorsGesture GameInfo
         */

        /**
         * Constructs a new RespSubmitRockPaperScissorsGesture.
         * @memberof RockPaperScissors
         * @classdesc Represents a RespSubmitRockPaperScissorsGesture.
         * @implements IRespSubmitRockPaperScissorsGesture
         * @constructor
         * @param {RockPaperScissors.IRespSubmitRockPaperScissorsGesture=} [properties] Properties to set
         */
        function RespSubmitRockPaperScissorsGesture(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespSubmitRockPaperScissorsGesture GameInfo.
         * @member {RockPaperScissors.IRockPaperScissorsGameInfo|null|undefined} GameInfo
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @instance
         */
        RespSubmitRockPaperScissorsGesture.prototype.GameInfo = null;

        /**
         * Creates a new RespSubmitRockPaperScissorsGesture instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.IRespSubmitRockPaperScissorsGesture=} [properties] Properties to set
         * @returns {RockPaperScissors.RespSubmitRockPaperScissorsGesture} RespSubmitRockPaperScissorsGesture instance
         */
        RespSubmitRockPaperScissorsGesture.create = function create(properties) {
            return new RespSubmitRockPaperScissorsGesture(properties);
        };

        /**
         * Encodes the specified RespSubmitRockPaperScissorsGesture message. Does not implicitly {@link RockPaperScissors.RespSubmitRockPaperScissorsGesture.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.IRespSubmitRockPaperScissorsGesture} message RespSubmitRockPaperScissorsGesture message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespSubmitRockPaperScissorsGesture.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                $root.RockPaperScissors.RockPaperScissorsGameInfo.encode(message.GameInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespSubmitRockPaperScissorsGesture message, length delimited. Does not implicitly {@link RockPaperScissors.RespSubmitRockPaperScissorsGesture.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.IRespSubmitRockPaperScissorsGesture} message RespSubmitRockPaperScissorsGesture message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespSubmitRockPaperScissorsGesture.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespSubmitRockPaperScissorsGesture message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.RespSubmitRockPaperScissorsGesture} RespSubmitRockPaperScissorsGesture
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespSubmitRockPaperScissorsGesture.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.RespSubmitRockPaperScissorsGesture();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespSubmitRockPaperScissorsGesture message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.RespSubmitRockPaperScissorsGesture} RespSubmitRockPaperScissorsGesture
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespSubmitRockPaperScissorsGesture.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespSubmitRockPaperScissorsGesture message.
         * @function verify
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespSubmitRockPaperScissorsGesture.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo")) {
                var error = $root.RockPaperScissors.RockPaperScissorsGameInfo.verify(message.GameInfo, long + 1);
                if (error)
                    return "GameInfo." + error;
            }
            return null;
        };

        /**
         * Creates a RespSubmitRockPaperScissorsGesture message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.RespSubmitRockPaperScissorsGesture} RespSubmitRockPaperScissorsGesture
         */
        RespSubmitRockPaperScissorsGesture.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.RespSubmitRockPaperScissorsGesture)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.RespSubmitRockPaperScissorsGesture: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.RespSubmitRockPaperScissorsGesture();
            if (object.GameInfo != null) {
                if (!$util.isObject(object.GameInfo))
                    throw TypeError(".RockPaperScissors.RespSubmitRockPaperScissorsGesture.GameInfo: object expected");
                message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.fromObject(object.GameInfo, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespSubmitRockPaperScissorsGesture message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {RockPaperScissors.RespSubmitRockPaperScissorsGesture} message RespSubmitRockPaperScissorsGesture
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespSubmitRockPaperScissorsGesture.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.GameInfo = null;
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                object.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.toObject(message.GameInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this RespSubmitRockPaperScissorsGesture to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespSubmitRockPaperScissorsGesture.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespSubmitRockPaperScissorsGesture
         * @function getTypeUrl
         * @memberof RockPaperScissors.RespSubmitRockPaperScissorsGesture
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespSubmitRockPaperScissorsGesture.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.RespSubmitRockPaperScissorsGesture";
        };

        return RespSubmitRockPaperScissorsGesture;
    })();

    RockPaperScissors.ReqRestartRockPaperScissorsGame = (function() {

        /**
         * Properties of a ReqRestartRockPaperScissorsGame.
         * @memberof RockPaperScissors
         * @interface IReqRestartRockPaperScissorsGame
         * @property {number|Long|null} [RoomId] ReqRestartRockPaperScissorsGame RoomId
         */

        /**
         * Constructs a new ReqRestartRockPaperScissorsGame.
         * @memberof RockPaperScissors
         * @classdesc Represents a ReqRestartRockPaperScissorsGame.
         * @implements IReqRestartRockPaperScissorsGame
         * @constructor
         * @param {RockPaperScissors.IReqRestartRockPaperScissorsGame=} [properties] Properties to set
         */
        function ReqRestartRockPaperScissorsGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqRestartRockPaperScissorsGame RoomId.
         * @member {number|Long} RoomId
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @instance
         */
        ReqRestartRockPaperScissorsGame.prototype.RoomId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqRestartRockPaperScissorsGame instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.IReqRestartRockPaperScissorsGame=} [properties] Properties to set
         * @returns {RockPaperScissors.ReqRestartRockPaperScissorsGame} ReqRestartRockPaperScissorsGame instance
         */
        ReqRestartRockPaperScissorsGame.create = function create(properties) {
            return new ReqRestartRockPaperScissorsGame(properties);
        };

        /**
         * Encodes the specified ReqRestartRockPaperScissorsGame message. Does not implicitly {@link RockPaperScissors.ReqRestartRockPaperScissorsGame.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.IReqRestartRockPaperScissorsGame} message ReqRestartRockPaperScissorsGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRestartRockPaperScissorsGame.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoomId);
            return writer;
        };

        /**
         * Encodes the specified ReqRestartRockPaperScissorsGame message, length delimited. Does not implicitly {@link RockPaperScissors.ReqRestartRockPaperScissorsGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.IReqRestartRockPaperScissorsGame} message ReqRestartRockPaperScissorsGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqRestartRockPaperScissorsGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqRestartRockPaperScissorsGame message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.ReqRestartRockPaperScissorsGame} ReqRestartRockPaperScissorsGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRestartRockPaperScissorsGame.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.ReqRestartRockPaperScissorsGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoomId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqRestartRockPaperScissorsGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.ReqRestartRockPaperScissorsGame} ReqRestartRockPaperScissorsGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqRestartRockPaperScissorsGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqRestartRockPaperScissorsGame message.
         * @function verify
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqRestartRockPaperScissorsGame.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (!$util.isInteger(message.RoomId) && !(message.RoomId && $util.isInteger(message.RoomId.low) && $util.isInteger(message.RoomId.high)))
                    return "RoomId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqRestartRockPaperScissorsGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.ReqRestartRockPaperScissorsGame} ReqRestartRockPaperScissorsGame
         */
        ReqRestartRockPaperScissorsGame.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.ReqRestartRockPaperScissorsGame)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.ReqRestartRockPaperScissorsGame: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.ReqRestartRockPaperScissorsGame();
            if (object.RoomId != null)
                if ($util.Long)
                    message.RoomId = $util.Long.fromValue(object.RoomId, false);
                else if (typeof object.RoomId === "string")
                    message.RoomId = parseInt(object.RoomId, 10);
                else if (typeof object.RoomId === "number")
                    message.RoomId = object.RoomId;
                else if (typeof object.RoomId === "object")
                    message.RoomId = new $util.LongBits(object.RoomId.low >>> 0, object.RoomId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqRestartRockPaperScissorsGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.ReqRestartRockPaperScissorsGame} message ReqRestartRockPaperScissorsGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqRestartRockPaperScissorsGame.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.RoomId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.RoomId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.RoomId != null && Object.hasOwnProperty.call(message, "RoomId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.RoomId = typeof message.RoomId === "number" ? BigInt(message.RoomId) : $util.Long.fromBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0, false).toBigInt();
                else if (typeof message.RoomId === "number")
                    object.RoomId = options.longs === String ? String(message.RoomId) : message.RoomId;
                else
                    object.RoomId = options.longs === String ? $util.Long.prototype.toString.call(message.RoomId) : options.longs === Number ? new $util.LongBits(message.RoomId.low >>> 0, message.RoomId.high >>> 0).toNumber() : message.RoomId;
            return object;
        };

        /**
         * Converts this ReqRestartRockPaperScissorsGame to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqRestartRockPaperScissorsGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqRestartRockPaperScissorsGame
         * @function getTypeUrl
         * @memberof RockPaperScissors.ReqRestartRockPaperScissorsGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqRestartRockPaperScissorsGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.ReqRestartRockPaperScissorsGame";
        };

        return ReqRestartRockPaperScissorsGame;
    })();

    RockPaperScissors.RespRestartRockPaperScissorsGame = (function() {

        /**
         * Properties of a RespRestartRockPaperScissorsGame.
         * @memberof RockPaperScissors
         * @interface IRespRestartRockPaperScissorsGame
         * @property {RockPaperScissors.IRockPaperScissorsGameInfo|null} [GameInfo] RespRestartRockPaperScissorsGame GameInfo
         */

        /**
         * Constructs a new RespRestartRockPaperScissorsGame.
         * @memberof RockPaperScissors
         * @classdesc Represents a RespRestartRockPaperScissorsGame.
         * @implements IRespRestartRockPaperScissorsGame
         * @constructor
         * @param {RockPaperScissors.IRespRestartRockPaperScissorsGame=} [properties] Properties to set
         */
        function RespRestartRockPaperScissorsGame(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespRestartRockPaperScissorsGame GameInfo.
         * @member {RockPaperScissors.IRockPaperScissorsGameInfo|null|undefined} GameInfo
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @instance
         */
        RespRestartRockPaperScissorsGame.prototype.GameInfo = null;

        /**
         * Creates a new RespRestartRockPaperScissorsGame instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.IRespRestartRockPaperScissorsGame=} [properties] Properties to set
         * @returns {RockPaperScissors.RespRestartRockPaperScissorsGame} RespRestartRockPaperScissorsGame instance
         */
        RespRestartRockPaperScissorsGame.create = function create(properties) {
            return new RespRestartRockPaperScissorsGame(properties);
        };

        /**
         * Encodes the specified RespRestartRockPaperScissorsGame message. Does not implicitly {@link RockPaperScissors.RespRestartRockPaperScissorsGame.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.IRespRestartRockPaperScissorsGame} message RespRestartRockPaperScissorsGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRestartRockPaperScissorsGame.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                $root.RockPaperScissors.RockPaperScissorsGameInfo.encode(message.GameInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespRestartRockPaperScissorsGame message, length delimited. Does not implicitly {@link RockPaperScissors.RespRestartRockPaperScissorsGame.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.IRespRestartRockPaperScissorsGame} message RespRestartRockPaperScissorsGame message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespRestartRockPaperScissorsGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespRestartRockPaperScissorsGame message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.RespRestartRockPaperScissorsGame} RespRestartRockPaperScissorsGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRestartRockPaperScissorsGame.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.RespRestartRockPaperScissorsGame();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespRestartRockPaperScissorsGame message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.RespRestartRockPaperScissorsGame} RespRestartRockPaperScissorsGame
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespRestartRockPaperScissorsGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespRestartRockPaperScissorsGame message.
         * @function verify
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespRestartRockPaperScissorsGame.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo")) {
                var error = $root.RockPaperScissors.RockPaperScissorsGameInfo.verify(message.GameInfo, long + 1);
                if (error)
                    return "GameInfo." + error;
            }
            return null;
        };

        /**
         * Creates a RespRestartRockPaperScissorsGame message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.RespRestartRockPaperScissorsGame} RespRestartRockPaperScissorsGame
         */
        RespRestartRockPaperScissorsGame.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.RespRestartRockPaperScissorsGame)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.RespRestartRockPaperScissorsGame: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.RespRestartRockPaperScissorsGame();
            if (object.GameInfo != null) {
                if (!$util.isObject(object.GameInfo))
                    throw TypeError(".RockPaperScissors.RespRestartRockPaperScissorsGame.GameInfo: object expected");
                message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.fromObject(object.GameInfo, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RespRestartRockPaperScissorsGame message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {RockPaperScissors.RespRestartRockPaperScissorsGame} message RespRestartRockPaperScissorsGame
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespRestartRockPaperScissorsGame.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.GameInfo = null;
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                object.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.toObject(message.GameInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this RespRestartRockPaperScissorsGame to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespRestartRockPaperScissorsGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespRestartRockPaperScissorsGame
         * @function getTypeUrl
         * @memberof RockPaperScissors.RespRestartRockPaperScissorsGame
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespRestartRockPaperScissorsGame.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.RespRestartRockPaperScissorsGame";
        };

        return RespRestartRockPaperScissorsGame;
    })();

    RockPaperScissors.NotifyRockPaperScissorsGameChanged = (function() {

        /**
         * Properties of a NotifyRockPaperScissorsGameChanged.
         * @memberof RockPaperScissors
         * @interface INotifyRockPaperScissorsGameChanged
         * @property {RockPaperScissors.IRockPaperScissorsGameInfo|null} [GameInfo] NotifyRockPaperScissorsGameChanged GameInfo
         */

        /**
         * Constructs a new NotifyRockPaperScissorsGameChanged.
         * @memberof RockPaperScissors
         * @classdesc Represents a NotifyRockPaperScissorsGameChanged.
         * @implements INotifyRockPaperScissorsGameChanged
         * @constructor
         * @param {RockPaperScissors.INotifyRockPaperScissorsGameChanged=} [properties] Properties to set
         */
        function NotifyRockPaperScissorsGameChanged(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyRockPaperScissorsGameChanged GameInfo.
         * @member {RockPaperScissors.IRockPaperScissorsGameInfo|null|undefined} GameInfo
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @instance
         */
        NotifyRockPaperScissorsGameChanged.prototype.GameInfo = null;

        /**
         * Creates a new NotifyRockPaperScissorsGameChanged instance using the specified properties.
         * @function create
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {RockPaperScissors.INotifyRockPaperScissorsGameChanged=} [properties] Properties to set
         * @returns {RockPaperScissors.NotifyRockPaperScissorsGameChanged} NotifyRockPaperScissorsGameChanged instance
         */
        NotifyRockPaperScissorsGameChanged.create = function create(properties) {
            return new NotifyRockPaperScissorsGameChanged(properties);
        };

        /**
         * Encodes the specified NotifyRockPaperScissorsGameChanged message. Does not implicitly {@link RockPaperScissors.NotifyRockPaperScissorsGameChanged.verify|verify} messages.
         * @function encode
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {RockPaperScissors.INotifyRockPaperScissorsGameChanged} message NotifyRockPaperScissorsGameChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyRockPaperScissorsGameChanged.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                $root.RockPaperScissors.RockPaperScissorsGameInfo.encode(message.GameInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NotifyRockPaperScissorsGameChanged message, length delimited. Does not implicitly {@link RockPaperScissors.NotifyRockPaperScissorsGameChanged.verify|verify} messages.
         * @function encodeDelimited
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {RockPaperScissors.INotifyRockPaperScissorsGameChanged} message NotifyRockPaperScissorsGameChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyRockPaperScissorsGameChanged.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyRockPaperScissorsGameChanged message from the specified reader or buffer.
         * @function decode
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {RockPaperScissors.NotifyRockPaperScissorsGameChanged} NotifyRockPaperScissorsGameChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyRockPaperScissorsGameChanged.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.RockPaperScissors.NotifyRockPaperScissorsGameChanged();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyRockPaperScissorsGameChanged message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {RockPaperScissors.NotifyRockPaperScissorsGameChanged} NotifyRockPaperScissorsGameChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyRockPaperScissorsGameChanged.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyRockPaperScissorsGameChanged message.
         * @function verify
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyRockPaperScissorsGameChanged.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo")) {
                var error = $root.RockPaperScissors.RockPaperScissorsGameInfo.verify(message.GameInfo, long + 1);
                if (error)
                    return "GameInfo." + error;
            }
            return null;
        };

        /**
         * Creates a NotifyRockPaperScissorsGameChanged message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {RockPaperScissors.NotifyRockPaperScissorsGameChanged} NotifyRockPaperScissorsGameChanged
         */
        NotifyRockPaperScissorsGameChanged.fromObject = function fromObject(object, long) {
            if (object instanceof $root.RockPaperScissors.NotifyRockPaperScissorsGameChanged)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".RockPaperScissors.NotifyRockPaperScissorsGameChanged: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.RockPaperScissors.NotifyRockPaperScissorsGameChanged();
            if (object.GameInfo != null) {
                if (!$util.isObject(object.GameInfo))
                    throw TypeError(".RockPaperScissors.NotifyRockPaperScissorsGameChanged.GameInfo: object expected");
                message.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.fromObject(object.GameInfo, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NotifyRockPaperScissorsGameChanged message. Also converts values to other types if specified.
         * @function toObject
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {RockPaperScissors.NotifyRockPaperScissorsGameChanged} message NotifyRockPaperScissorsGameChanged
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyRockPaperScissorsGameChanged.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                object.GameInfo = null;
            if (message.GameInfo != null && Object.hasOwnProperty.call(message, "GameInfo"))
                object.GameInfo = $root.RockPaperScissors.RockPaperScissorsGameInfo.toObject(message.GameInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this NotifyRockPaperScissorsGameChanged to JSON.
         * @function toJSON
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyRockPaperScissorsGameChanged.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyRockPaperScissorsGameChanged
         * @function getTypeUrl
         * @memberof RockPaperScissors.NotifyRockPaperScissorsGameChanged
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyRockPaperScissorsGameChanged.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/RockPaperScissors.NotifyRockPaperScissorsGameChanged";
        };

        return NotifyRockPaperScissorsGameChanged;
    })();

    return RockPaperScissors;
})();

$root.Mail = (function() {

    /**
     * Namespace Mail.
     * @exports Mail
     * @namespace
     */
    var Mail = {};

    /**
     * MailErrorCode enum.
     * @name Mail.MailErrorCode
     * @enum {number}
     * @property {number} MailNotFound=500001 MailNotFound value
     * @property {number} MailAlreadyDeleted=500002 MailAlreadyDeleted value
     * @property {number} UnclaimedAttachment=500003 UnclaimedAttachment value
     * @property {number} AttachmentNotFound=500004 AttachmentNotFound value
     * @property {number} AttachmentAlreadyClaimed=500005 AttachmentAlreadyClaimed value
     * @property {number} UnclaimableAttachment=500006 UnclaimableAttachment value
     */
    Mail.MailErrorCode = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[500001] = "MailNotFound"] = 500001;
        values[valuesById[500002] = "MailAlreadyDeleted"] = 500002;
        values[valuesById[500003] = "UnclaimedAttachment"] = 500003;
        values[valuesById[500004] = "AttachmentNotFound"] = 500004;
        values[valuesById[500005] = "AttachmentAlreadyClaimed"] = 500005;
        values[valuesById[500006] = "UnclaimableAttachment"] = 500006;
        return values;
    })();

    Mail.MailAttachmentInfo = (function() {

        /**
         * Properties of a MailAttachmentInfo.
         * @memberof Mail
         * @interface IMailAttachmentInfo
         * @property {number|null} [SlotId] MailAttachmentInfo SlotId
         * @property {number|null} [RewardType] MailAttachmentInfo RewardType
         * @property {number|null} [ItemId] MailAttachmentInfo ItemId
         * @property {number|Long|null} [Count] MailAttachmentInfo Count
         * @property {number|null} [ClaimStatus] MailAttachmentInfo ClaimStatus
         */

        /**
         * Constructs a new MailAttachmentInfo.
         * @memberof Mail
         * @classdesc Represents a MailAttachmentInfo.
         * @implements IMailAttachmentInfo
         * @constructor
         * @param {Mail.IMailAttachmentInfo=} [properties] Properties to set
         */
        function MailAttachmentInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MailAttachmentInfo SlotId.
         * @member {number} SlotId
         * @memberof Mail.MailAttachmentInfo
         * @instance
         */
        MailAttachmentInfo.prototype.SlotId = 0;

        /**
         * MailAttachmentInfo RewardType.
         * @member {number} RewardType
         * @memberof Mail.MailAttachmentInfo
         * @instance
         */
        MailAttachmentInfo.prototype.RewardType = 0;

        /**
         * MailAttachmentInfo ItemId.
         * @member {number} ItemId
         * @memberof Mail.MailAttachmentInfo
         * @instance
         */
        MailAttachmentInfo.prototype.ItemId = 0;

        /**
         * MailAttachmentInfo Count.
         * @member {number|Long} Count
         * @memberof Mail.MailAttachmentInfo
         * @instance
         */
        MailAttachmentInfo.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailAttachmentInfo ClaimStatus.
         * @member {number} ClaimStatus
         * @memberof Mail.MailAttachmentInfo
         * @instance
         */
        MailAttachmentInfo.prototype.ClaimStatus = 0;

        /**
         * Creates a new MailAttachmentInfo instance using the specified properties.
         * @function create
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {Mail.IMailAttachmentInfo=} [properties] Properties to set
         * @returns {Mail.MailAttachmentInfo} MailAttachmentInfo instance
         */
        MailAttachmentInfo.create = function create(properties) {
            return new MailAttachmentInfo(properties);
        };

        /**
         * Encodes the specified MailAttachmentInfo message. Does not implicitly {@link Mail.MailAttachmentInfo.verify|verify} messages.
         * @function encode
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {Mail.IMailAttachmentInfo} message MailAttachmentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MailAttachmentInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.SlotId);
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.RewardType);
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.Count);
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.ClaimStatus);
            return writer;
        };

        /**
         * Encodes the specified MailAttachmentInfo message, length delimited. Does not implicitly {@link Mail.MailAttachmentInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {Mail.IMailAttachmentInfo} message MailAttachmentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MailAttachmentInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MailAttachmentInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.MailAttachmentInfo} MailAttachmentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MailAttachmentInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.MailAttachmentInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.SlotId = reader.int32();
                        break;
                    }
                case 2: {
                        message.RewardType = reader.int32();
                        break;
                    }
                case 3: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 4: {
                        message.Count = reader.int64();
                        break;
                    }
                case 5: {
                        message.ClaimStatus = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a MailAttachmentInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.MailAttachmentInfo} MailAttachmentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MailAttachmentInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MailAttachmentInfo message.
         * @function verify
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MailAttachmentInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                if (!$util.isInteger(message.SlotId))
                    return "SlotId: integer expected";
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                if (!$util.isInteger(message.RewardType))
                    return "RewardType: integer expected";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                if (!$util.isInteger(message.ClaimStatus))
                    return "ClaimStatus: integer expected";
            return null;
        };

        /**
         * Creates a MailAttachmentInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.MailAttachmentInfo} MailAttachmentInfo
         */
        MailAttachmentInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.MailAttachmentInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.MailAttachmentInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.MailAttachmentInfo();
            if (object.SlotId != null)
                message.SlotId = object.SlotId | 0;
            if (object.RewardType != null)
                message.RewardType = object.RewardType | 0;
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            if (object.ClaimStatus != null)
                message.ClaimStatus = object.ClaimStatus | 0;
            return message;
        };

        /**
         * Creates a plain object from a MailAttachmentInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {Mail.MailAttachmentInfo} message MailAttachmentInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MailAttachmentInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.SlotId = 0;
                object.RewardType = 0;
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.ClaimStatus = 0;
            }
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                object.SlotId = message.SlotId;
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                object.RewardType = message.RewardType;
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                object.ClaimStatus = message.ClaimStatus;
            return object;
        };

        /**
         * Converts this MailAttachmentInfo to JSON.
         * @function toJSON
         * @memberof Mail.MailAttachmentInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MailAttachmentInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MailAttachmentInfo
         * @function getTypeUrl
         * @memberof Mail.MailAttachmentInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MailAttachmentInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.MailAttachmentInfo";
        };

        return MailAttachmentInfo;
    })();

    Mail.MailInfo = (function() {

        /**
         * Properties of a MailInfo.
         * @memberof Mail
         * @interface IMailInfo
         * @property {number|Long|null} [MailId] MailInfo MailId
         * @property {number|Long|null} [CampaignId] MailInfo CampaignId
         * @property {number|Long|null} [CampaignVersion] MailInfo CampaignVersion
         * @property {number|null} [MailType] MailInfo MailType
         * @property {string|null} [Title] MailInfo Title
         * @property {number|null} [ReadStatus] MailInfo ReadStatus
         * @property {number|null} [AttachmentStatus] MailInfo AttachmentStatus
         * @property {number|null} [MailStatus] MailInfo MailStatus
         * @property {number|Long|null} [CreateTime] MailInfo CreateTime
         * @property {number|Long|null} [ExpireTime] MailInfo ExpireTime
         * @property {boolean|null} [HasAttachment] MailInfo HasAttachment
         */

        /**
         * Constructs a new MailInfo.
         * @memberof Mail
         * @classdesc Represents a MailInfo.
         * @implements IMailInfo
         * @constructor
         * @param {Mail.IMailInfo=} [properties] Properties to set
         */
        function MailInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MailInfo MailId.
         * @member {number|Long} MailId
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailInfo CampaignId.
         * @member {number|Long} CampaignId
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.CampaignId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailInfo CampaignVersion.
         * @member {number|Long} CampaignVersion
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.CampaignVersion = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailInfo MailType.
         * @member {number} MailType
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.MailType = 0;

        /**
         * MailInfo Title.
         * @member {string} Title
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.Title = "";

        /**
         * MailInfo ReadStatus.
         * @member {number} ReadStatus
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.ReadStatus = 0;

        /**
         * MailInfo AttachmentStatus.
         * @member {number} AttachmentStatus
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.AttachmentStatus = 0;

        /**
         * MailInfo MailStatus.
         * @member {number} MailStatus
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.MailStatus = 0;

        /**
         * MailInfo CreateTime.
         * @member {number|Long} CreateTime
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.CreateTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailInfo ExpireTime.
         * @member {number|Long} ExpireTime
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.ExpireTime = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailInfo HasAttachment.
         * @member {boolean} HasAttachment
         * @memberof Mail.MailInfo
         * @instance
         */
        MailInfo.prototype.HasAttachment = false;

        /**
         * Creates a new MailInfo instance using the specified properties.
         * @function create
         * @memberof Mail.MailInfo
         * @static
         * @param {Mail.IMailInfo=} [properties] Properties to set
         * @returns {Mail.MailInfo} MailInfo instance
         */
        MailInfo.create = function create(properties) {
            return new MailInfo(properties);
        };

        /**
         * Encodes the specified MailInfo message. Does not implicitly {@link Mail.MailInfo.verify|verify} messages.
         * @function encode
         * @memberof Mail.MailInfo
         * @static
         * @param {Mail.IMailInfo} message MailInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MailInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            if (message.CampaignId != null && Object.hasOwnProperty.call(message, "CampaignId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.CampaignId);
            if (message.CampaignVersion != null && Object.hasOwnProperty.call(message, "CampaignVersion"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.CampaignVersion);
            if (message.MailType != null && Object.hasOwnProperty.call(message, "MailType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.MailType);
            if (message.Title != null && Object.hasOwnProperty.call(message, "Title"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.Title);
            if (message.ReadStatus != null && Object.hasOwnProperty.call(message, "ReadStatus"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ReadStatus);
            if (message.AttachmentStatus != null && Object.hasOwnProperty.call(message, "AttachmentStatus"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AttachmentStatus);
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.MailStatus);
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.CreateTime);
            if (message.ExpireTime != null && Object.hasOwnProperty.call(message, "ExpireTime"))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.ExpireTime);
            if (message.HasAttachment != null && Object.hasOwnProperty.call(message, "HasAttachment"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.HasAttachment);
            return writer;
        };

        /**
         * Encodes the specified MailInfo message, length delimited. Does not implicitly {@link Mail.MailInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.MailInfo
         * @static
         * @param {Mail.IMailInfo} message MailInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MailInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MailInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.MailInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.MailInfo} MailInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MailInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.MailInfo();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                case 2: {
                        message.CampaignId = reader.int64();
                        break;
                    }
                case 3: {
                        message.CampaignVersion = reader.int64();
                        break;
                    }
                case 4: {
                        message.MailType = reader.int32();
                        break;
                    }
                case 5: {
                        message.Title = reader.string();
                        break;
                    }
                case 6: {
                        message.ReadStatus = reader.int32();
                        break;
                    }
                case 7: {
                        message.AttachmentStatus = reader.int32();
                        break;
                    }
                case 8: {
                        message.MailStatus = reader.int32();
                        break;
                    }
                case 9: {
                        message.CreateTime = reader.int64();
                        break;
                    }
                case 10: {
                        message.ExpireTime = reader.int64();
                        break;
                    }
                case 11: {
                        message.HasAttachment = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a MailInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.MailInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.MailInfo} MailInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MailInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MailInfo message.
         * @function verify
         * @memberof Mail.MailInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MailInfo.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            if (message.CampaignId != null && Object.hasOwnProperty.call(message, "CampaignId"))
                if (!$util.isInteger(message.CampaignId) && !(message.CampaignId && $util.isInteger(message.CampaignId.low) && $util.isInteger(message.CampaignId.high)))
                    return "CampaignId: integer|Long expected";
            if (message.CampaignVersion != null && Object.hasOwnProperty.call(message, "CampaignVersion"))
                if (!$util.isInteger(message.CampaignVersion) && !(message.CampaignVersion && $util.isInteger(message.CampaignVersion.low) && $util.isInteger(message.CampaignVersion.high)))
                    return "CampaignVersion: integer|Long expected";
            if (message.MailType != null && Object.hasOwnProperty.call(message, "MailType"))
                if (!$util.isInteger(message.MailType))
                    return "MailType: integer expected";
            if (message.Title != null && Object.hasOwnProperty.call(message, "Title"))
                if (!$util.isString(message.Title))
                    return "Title: string expected";
            if (message.ReadStatus != null && Object.hasOwnProperty.call(message, "ReadStatus"))
                if (!$util.isInteger(message.ReadStatus))
                    return "ReadStatus: integer expected";
            if (message.AttachmentStatus != null && Object.hasOwnProperty.call(message, "AttachmentStatus"))
                if (!$util.isInteger(message.AttachmentStatus))
                    return "AttachmentStatus: integer expected";
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                if (!$util.isInteger(message.MailStatus))
                    return "MailStatus: integer expected";
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                if (!$util.isInteger(message.CreateTime) && !(message.CreateTime && $util.isInteger(message.CreateTime.low) && $util.isInteger(message.CreateTime.high)))
                    return "CreateTime: integer|Long expected";
            if (message.ExpireTime != null && Object.hasOwnProperty.call(message, "ExpireTime"))
                if (!$util.isInteger(message.ExpireTime) && !(message.ExpireTime && $util.isInteger(message.ExpireTime.low) && $util.isInteger(message.ExpireTime.high)))
                    return "ExpireTime: integer|Long expected";
            if (message.HasAttachment != null && Object.hasOwnProperty.call(message, "HasAttachment"))
                if (typeof message.HasAttachment !== "boolean")
                    return "HasAttachment: boolean expected";
            return null;
        };

        /**
         * Creates a MailInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.MailInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.MailInfo} MailInfo
         */
        MailInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.MailInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.MailInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.MailInfo();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            if (object.CampaignId != null)
                if ($util.Long)
                    message.CampaignId = $util.Long.fromValue(object.CampaignId, false);
                else if (typeof object.CampaignId === "string")
                    message.CampaignId = parseInt(object.CampaignId, 10);
                else if (typeof object.CampaignId === "number")
                    message.CampaignId = object.CampaignId;
                else if (typeof object.CampaignId === "object")
                    message.CampaignId = new $util.LongBits(object.CampaignId.low >>> 0, object.CampaignId.high >>> 0).toNumber();
            if (object.CampaignVersion != null)
                if ($util.Long)
                    message.CampaignVersion = $util.Long.fromValue(object.CampaignVersion, false);
                else if (typeof object.CampaignVersion === "string")
                    message.CampaignVersion = parseInt(object.CampaignVersion, 10);
                else if (typeof object.CampaignVersion === "number")
                    message.CampaignVersion = object.CampaignVersion;
                else if (typeof object.CampaignVersion === "object")
                    message.CampaignVersion = new $util.LongBits(object.CampaignVersion.low >>> 0, object.CampaignVersion.high >>> 0).toNumber();
            if (object.MailType != null)
                message.MailType = object.MailType | 0;
            if (object.Title != null)
                message.Title = String(object.Title);
            if (object.ReadStatus != null)
                message.ReadStatus = object.ReadStatus | 0;
            if (object.AttachmentStatus != null)
                message.AttachmentStatus = object.AttachmentStatus | 0;
            if (object.MailStatus != null)
                message.MailStatus = object.MailStatus | 0;
            if (object.CreateTime != null)
                if ($util.Long)
                    message.CreateTime = $util.Long.fromValue(object.CreateTime, false);
                else if (typeof object.CreateTime === "string")
                    message.CreateTime = parseInt(object.CreateTime, 10);
                else if (typeof object.CreateTime === "number")
                    message.CreateTime = object.CreateTime;
                else if (typeof object.CreateTime === "object")
                    message.CreateTime = new $util.LongBits(object.CreateTime.low >>> 0, object.CreateTime.high >>> 0).toNumber();
            if (object.ExpireTime != null)
                if ($util.Long)
                    message.ExpireTime = $util.Long.fromValue(object.ExpireTime, false);
                else if (typeof object.ExpireTime === "string")
                    message.ExpireTime = parseInt(object.ExpireTime, 10);
                else if (typeof object.ExpireTime === "number")
                    message.ExpireTime = object.ExpireTime;
                else if (typeof object.ExpireTime === "object")
                    message.ExpireTime = new $util.LongBits(object.ExpireTime.low >>> 0, object.ExpireTime.high >>> 0).toNumber();
            if (object.HasAttachment != null)
                message.HasAttachment = Boolean(object.HasAttachment);
            return message;
        };

        /**
         * Creates a plain object from a MailInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.MailInfo
         * @static
         * @param {Mail.MailInfo} message MailInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MailInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.CampaignId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.CampaignId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.CampaignVersion = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.CampaignVersion = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.MailType = 0;
                object.Title = "";
                object.ReadStatus = 0;
                object.AttachmentStatus = 0;
                object.MailStatus = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.CreateTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.CreateTime = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.ExpireTime = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.ExpireTime = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.HasAttachment = false;
            }
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            if (message.CampaignId != null && Object.hasOwnProperty.call(message, "CampaignId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.CampaignId = typeof message.CampaignId === "number" ? BigInt(message.CampaignId) : $util.Long.fromBits(message.CampaignId.low >>> 0, message.CampaignId.high >>> 0, false).toBigInt();
                else if (typeof message.CampaignId === "number")
                    object.CampaignId = options.longs === String ? String(message.CampaignId) : message.CampaignId;
                else
                    object.CampaignId = options.longs === String ? $util.Long.prototype.toString.call(message.CampaignId) : options.longs === Number ? new $util.LongBits(message.CampaignId.low >>> 0, message.CampaignId.high >>> 0).toNumber() : message.CampaignId;
            if (message.CampaignVersion != null && Object.hasOwnProperty.call(message, "CampaignVersion"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.CampaignVersion = typeof message.CampaignVersion === "number" ? BigInt(message.CampaignVersion) : $util.Long.fromBits(message.CampaignVersion.low >>> 0, message.CampaignVersion.high >>> 0, false).toBigInt();
                else if (typeof message.CampaignVersion === "number")
                    object.CampaignVersion = options.longs === String ? String(message.CampaignVersion) : message.CampaignVersion;
                else
                    object.CampaignVersion = options.longs === String ? $util.Long.prototype.toString.call(message.CampaignVersion) : options.longs === Number ? new $util.LongBits(message.CampaignVersion.low >>> 0, message.CampaignVersion.high >>> 0).toNumber() : message.CampaignVersion;
            if (message.MailType != null && Object.hasOwnProperty.call(message, "MailType"))
                object.MailType = message.MailType;
            if (message.Title != null && Object.hasOwnProperty.call(message, "Title"))
                object.Title = message.Title;
            if (message.ReadStatus != null && Object.hasOwnProperty.call(message, "ReadStatus"))
                object.ReadStatus = message.ReadStatus;
            if (message.AttachmentStatus != null && Object.hasOwnProperty.call(message, "AttachmentStatus"))
                object.AttachmentStatus = message.AttachmentStatus;
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                object.MailStatus = message.MailStatus;
            if (message.CreateTime != null && Object.hasOwnProperty.call(message, "CreateTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.CreateTime = typeof message.CreateTime === "number" ? BigInt(message.CreateTime) : $util.Long.fromBits(message.CreateTime.low >>> 0, message.CreateTime.high >>> 0, false).toBigInt();
                else if (typeof message.CreateTime === "number")
                    object.CreateTime = options.longs === String ? String(message.CreateTime) : message.CreateTime;
                else
                    object.CreateTime = options.longs === String ? $util.Long.prototype.toString.call(message.CreateTime) : options.longs === Number ? new $util.LongBits(message.CreateTime.low >>> 0, message.CreateTime.high >>> 0).toNumber() : message.CreateTime;
            if (message.ExpireTime != null && Object.hasOwnProperty.call(message, "ExpireTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ExpireTime = typeof message.ExpireTime === "number" ? BigInt(message.ExpireTime) : $util.Long.fromBits(message.ExpireTime.low >>> 0, message.ExpireTime.high >>> 0, false).toBigInt();
                else if (typeof message.ExpireTime === "number")
                    object.ExpireTime = options.longs === String ? String(message.ExpireTime) : message.ExpireTime;
                else
                    object.ExpireTime = options.longs === String ? $util.Long.prototype.toString.call(message.ExpireTime) : options.longs === Number ? new $util.LongBits(message.ExpireTime.low >>> 0, message.ExpireTime.high >>> 0).toNumber() : message.ExpireTime;
            if (message.HasAttachment != null && Object.hasOwnProperty.call(message, "HasAttachment"))
                object.HasAttachment = message.HasAttachment;
            return object;
        };

        /**
         * Converts this MailInfo to JSON.
         * @function toJSON
         * @memberof Mail.MailInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MailInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MailInfo
         * @function getTypeUrl
         * @memberof Mail.MailInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MailInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.MailInfo";
        };

        return MailInfo;
    })();

    Mail.MailClaimedSlot = (function() {

        /**
         * Properties of a MailClaimedSlot.
         * @memberof Mail
         * @interface IMailClaimedSlot
         * @property {number|Long|null} [MailId] MailClaimedSlot MailId
         * @property {number|null} [SlotId] MailClaimedSlot SlotId
         * @property {number|null} [RewardType] MailClaimedSlot RewardType
         * @property {number|null} [ItemId] MailClaimedSlot ItemId
         * @property {number|Long|null} [Count] MailClaimedSlot Count
         * @property {number|null} [ClaimStatus] MailClaimedSlot ClaimStatus
         * @property {boolean|null} [Success] MailClaimedSlot Success
         */

        /**
         * Constructs a new MailClaimedSlot.
         * @memberof Mail
         * @classdesc Represents a MailClaimedSlot.
         * @implements IMailClaimedSlot
         * @constructor
         * @param {Mail.IMailClaimedSlot=} [properties] Properties to set
         */
        function MailClaimedSlot(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MailClaimedSlot MailId.
         * @member {number|Long} MailId
         * @memberof Mail.MailClaimedSlot
         * @instance
         */
        MailClaimedSlot.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailClaimedSlot SlotId.
         * @member {number} SlotId
         * @memberof Mail.MailClaimedSlot
         * @instance
         */
        MailClaimedSlot.prototype.SlotId = 0;

        /**
         * MailClaimedSlot RewardType.
         * @member {number} RewardType
         * @memberof Mail.MailClaimedSlot
         * @instance
         */
        MailClaimedSlot.prototype.RewardType = 0;

        /**
         * MailClaimedSlot ItemId.
         * @member {number} ItemId
         * @memberof Mail.MailClaimedSlot
         * @instance
         */
        MailClaimedSlot.prototype.ItemId = 0;

        /**
         * MailClaimedSlot Count.
         * @member {number|Long} Count
         * @memberof Mail.MailClaimedSlot
         * @instance
         */
        MailClaimedSlot.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * MailClaimedSlot ClaimStatus.
         * @member {number} ClaimStatus
         * @memberof Mail.MailClaimedSlot
         * @instance
         */
        MailClaimedSlot.prototype.ClaimStatus = 0;

        /**
         * MailClaimedSlot Success.
         * @member {boolean} Success
         * @memberof Mail.MailClaimedSlot
         * @instance
         */
        MailClaimedSlot.prototype.Success = false;

        /**
         * Creates a new MailClaimedSlot instance using the specified properties.
         * @function create
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {Mail.IMailClaimedSlot=} [properties] Properties to set
         * @returns {Mail.MailClaimedSlot} MailClaimedSlot instance
         */
        MailClaimedSlot.create = function create(properties) {
            return new MailClaimedSlot(properties);
        };

        /**
         * Encodes the specified MailClaimedSlot message. Does not implicitly {@link Mail.MailClaimedSlot.verify|verify} messages.
         * @function encode
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {Mail.IMailClaimedSlot} message MailClaimedSlot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MailClaimedSlot.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.SlotId);
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.RewardType);
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.Count);
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ClaimStatus);
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.Success);
            return writer;
        };

        /**
         * Encodes the specified MailClaimedSlot message, length delimited. Does not implicitly {@link Mail.MailClaimedSlot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {Mail.IMailClaimedSlot} message MailClaimedSlot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MailClaimedSlot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MailClaimedSlot message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.MailClaimedSlot} MailClaimedSlot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MailClaimedSlot.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.MailClaimedSlot();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                case 2: {
                        message.SlotId = reader.int32();
                        break;
                    }
                case 3: {
                        message.RewardType = reader.int32();
                        break;
                    }
                case 4: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 5: {
                        message.Count = reader.int64();
                        break;
                    }
                case 6: {
                        message.ClaimStatus = reader.int32();
                        break;
                    }
                case 7: {
                        message.Success = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a MailClaimedSlot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.MailClaimedSlot} MailClaimedSlot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MailClaimedSlot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MailClaimedSlot message.
         * @function verify
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MailClaimedSlot.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                if (!$util.isInteger(message.SlotId))
                    return "SlotId: integer expected";
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                if (!$util.isInteger(message.RewardType))
                    return "RewardType: integer expected";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                if (!$util.isInteger(message.ClaimStatus))
                    return "ClaimStatus: integer expected";
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                if (typeof message.Success !== "boolean")
                    return "Success: boolean expected";
            return null;
        };

        /**
         * Creates a MailClaimedSlot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.MailClaimedSlot} MailClaimedSlot
         */
        MailClaimedSlot.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.MailClaimedSlot)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.MailClaimedSlot: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.MailClaimedSlot();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            if (object.SlotId != null)
                message.SlotId = object.SlotId | 0;
            if (object.RewardType != null)
                message.RewardType = object.RewardType | 0;
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            if (object.ClaimStatus != null)
                message.ClaimStatus = object.ClaimStatus | 0;
            if (object.Success != null)
                message.Success = Boolean(object.Success);
            return message;
        };

        /**
         * Creates a plain object from a MailClaimedSlot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {Mail.MailClaimedSlot} message MailClaimedSlot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MailClaimedSlot.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.SlotId = 0;
                object.RewardType = 0;
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.ClaimStatus = 0;
                object.Success = false;
            }
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                object.SlotId = message.SlotId;
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                object.RewardType = message.RewardType;
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                object.ClaimStatus = message.ClaimStatus;
            if (message.Success != null && Object.hasOwnProperty.call(message, "Success"))
                object.Success = message.Success;
            return object;
        };

        /**
         * Converts this MailClaimedSlot to JSON.
         * @function toJSON
         * @memberof Mail.MailClaimedSlot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MailClaimedSlot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MailClaimedSlot
         * @function getTypeUrl
         * @memberof Mail.MailClaimedSlot
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MailClaimedSlot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.MailClaimedSlot";
        };

        return MailClaimedSlot;
    })();

    Mail.ReqMailList = (function() {

        /**
         * Properties of a ReqMailList.
         * @memberof Mail
         * @interface IReqMailList
         * @property {number|null} [Cursor] ReqMailList Cursor
         * @property {number|null} [PageSize] ReqMailList PageSize
         */

        /**
         * Constructs a new ReqMailList.
         * @memberof Mail
         * @classdesc Represents a ReqMailList.
         * @implements IReqMailList
         * @constructor
         * @param {Mail.IReqMailList=} [properties] Properties to set
         */
        function ReqMailList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqMailList Cursor.
         * @member {number} Cursor
         * @memberof Mail.ReqMailList
         * @instance
         */
        ReqMailList.prototype.Cursor = 0;

        /**
         * ReqMailList PageSize.
         * @member {number} PageSize
         * @memberof Mail.ReqMailList
         * @instance
         */
        ReqMailList.prototype.PageSize = 0;

        /**
         * Creates a new ReqMailList instance using the specified properties.
         * @function create
         * @memberof Mail.ReqMailList
         * @static
         * @param {Mail.IReqMailList=} [properties] Properties to set
         * @returns {Mail.ReqMailList} ReqMailList instance
         */
        ReqMailList.create = function create(properties) {
            return new ReqMailList(properties);
        };

        /**
         * Encodes the specified ReqMailList message. Does not implicitly {@link Mail.ReqMailList.verify|verify} messages.
         * @function encode
         * @memberof Mail.ReqMailList
         * @static
         * @param {Mail.IReqMailList} message ReqMailList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Cursor != null && Object.hasOwnProperty.call(message, "Cursor"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Cursor);
            if (message.PageSize != null && Object.hasOwnProperty.call(message, "PageSize"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.PageSize);
            return writer;
        };

        /**
         * Encodes the specified ReqMailList message, length delimited. Does not implicitly {@link Mail.ReqMailList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.ReqMailList
         * @static
         * @param {Mail.IReqMailList} message ReqMailList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqMailList message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.ReqMailList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.ReqMailList} ReqMailList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.ReqMailList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Cursor = reader.int32();
                        break;
                    }
                case 2: {
                        message.PageSize = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqMailList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.ReqMailList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.ReqMailList} ReqMailList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqMailList message.
         * @function verify
         * @memberof Mail.ReqMailList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqMailList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Cursor != null && Object.hasOwnProperty.call(message, "Cursor"))
                if (!$util.isInteger(message.Cursor))
                    return "Cursor: integer expected";
            if (message.PageSize != null && Object.hasOwnProperty.call(message, "PageSize"))
                if (!$util.isInteger(message.PageSize))
                    return "PageSize: integer expected";
            return null;
        };

        /**
         * Creates a ReqMailList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.ReqMailList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.ReqMailList} ReqMailList
         */
        ReqMailList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.ReqMailList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.ReqMailList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.ReqMailList();
            if (object.Cursor != null)
                message.Cursor = object.Cursor | 0;
            if (object.PageSize != null)
                message.PageSize = object.PageSize | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReqMailList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.ReqMailList
         * @static
         * @param {Mail.ReqMailList} message ReqMailList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqMailList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.Cursor = 0;
                object.PageSize = 0;
            }
            if (message.Cursor != null && Object.hasOwnProperty.call(message, "Cursor"))
                object.Cursor = message.Cursor;
            if (message.PageSize != null && Object.hasOwnProperty.call(message, "PageSize"))
                object.PageSize = message.PageSize;
            return object;
        };

        /**
         * Converts this ReqMailList to JSON.
         * @function toJSON
         * @memberof Mail.ReqMailList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqMailList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqMailList
         * @function getTypeUrl
         * @memberof Mail.ReqMailList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqMailList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.ReqMailList";
        };

        return ReqMailList;
    })();

    Mail.RespMailList = (function() {

        /**
         * Properties of a RespMailList.
         * @memberof Mail
         * @interface IRespMailList
         * @property {Array.<Mail.IMailInfo>|null} [Mails] RespMailList Mails
         * @property {number|null} [UnreadCount] RespMailList UnreadCount
         * @property {boolean|null} [HasMore] RespMailList HasMore
         */

        /**
         * Constructs a new RespMailList.
         * @memberof Mail
         * @classdesc Represents a RespMailList.
         * @implements IRespMailList
         * @constructor
         * @param {Mail.IRespMailList=} [properties] Properties to set
         */
        function RespMailList(properties) {
            this.Mails = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespMailList Mails.
         * @member {Array.<Mail.IMailInfo>} Mails
         * @memberof Mail.RespMailList
         * @instance
         */
        RespMailList.prototype.Mails = $util.emptyArray;

        /**
         * RespMailList UnreadCount.
         * @member {number} UnreadCount
         * @memberof Mail.RespMailList
         * @instance
         */
        RespMailList.prototype.UnreadCount = 0;

        /**
         * RespMailList HasMore.
         * @member {boolean} HasMore
         * @memberof Mail.RespMailList
         * @instance
         */
        RespMailList.prototype.HasMore = false;

        /**
         * Creates a new RespMailList instance using the specified properties.
         * @function create
         * @memberof Mail.RespMailList
         * @static
         * @param {Mail.IRespMailList=} [properties] Properties to set
         * @returns {Mail.RespMailList} RespMailList instance
         */
        RespMailList.create = function create(properties) {
            return new RespMailList(properties);
        };

        /**
         * Encodes the specified RespMailList message. Does not implicitly {@link Mail.RespMailList.verify|verify} messages.
         * @function encode
         * @memberof Mail.RespMailList
         * @static
         * @param {Mail.IRespMailList} message RespMailList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Mails != null && message.Mails.length)
                for (var i = 0; i < message.Mails.length; ++i)
                    $root.Mail.MailInfo.encode(message.Mails[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.UnreadCount != null && Object.hasOwnProperty.call(message, "UnreadCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.UnreadCount);
            if (message.HasMore != null && Object.hasOwnProperty.call(message, "HasMore"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.HasMore);
            return writer;
        };

        /**
         * Encodes the specified RespMailList message, length delimited. Does not implicitly {@link Mail.RespMailList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.RespMailList
         * @static
         * @param {Mail.IRespMailList} message RespMailList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespMailList message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.RespMailList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.RespMailList} RespMailList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.RespMailList();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mails && message.Mails.length))
                            message.Mails = [];
                        message.Mails.push($root.Mail.MailInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        message.UnreadCount = reader.int32();
                        break;
                    }
                case 3: {
                        message.HasMore = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespMailList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.RespMailList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.RespMailList} RespMailList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespMailList message.
         * @function verify
         * @memberof Mail.RespMailList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespMailList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Mails != null && Object.hasOwnProperty.call(message, "Mails")) {
                if (!Array.isArray(message.Mails))
                    return "Mails: array expected";
                for (var i = 0; i < message.Mails.length; ++i) {
                    var error = $root.Mail.MailInfo.verify(message.Mails[i], long + 1);
                    if (error)
                        return "Mails." + error;
                }
            }
            if (message.UnreadCount != null && Object.hasOwnProperty.call(message, "UnreadCount"))
                if (!$util.isInteger(message.UnreadCount))
                    return "UnreadCount: integer expected";
            if (message.HasMore != null && Object.hasOwnProperty.call(message, "HasMore"))
                if (typeof message.HasMore !== "boolean")
                    return "HasMore: boolean expected";
            return null;
        };

        /**
         * Creates a RespMailList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.RespMailList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.RespMailList} RespMailList
         */
        RespMailList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.RespMailList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.RespMailList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.RespMailList();
            if (object.Mails) {
                if (!Array.isArray(object.Mails))
                    throw TypeError(".Mail.RespMailList.Mails: array expected");
                message.Mails = [];
                for (var i = 0; i < object.Mails.length; ++i) {
                    if (!$util.isObject(object.Mails[i]))
                        throw TypeError(".Mail.RespMailList.Mails: object expected");
                    message.Mails[i] = $root.Mail.MailInfo.fromObject(object.Mails[i], long + 1);
                }
            }
            if (object.UnreadCount != null)
                message.UnreadCount = object.UnreadCount | 0;
            if (object.HasMore != null)
                message.HasMore = Boolean(object.HasMore);
            return message;
        };

        /**
         * Creates a plain object from a RespMailList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.RespMailList
         * @static
         * @param {Mail.RespMailList} message RespMailList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespMailList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Mails = [];
            if (options.defaults) {
                object.UnreadCount = 0;
                object.HasMore = false;
            }
            if (message.Mails && message.Mails.length) {
                object.Mails = [];
                for (var j = 0; j < message.Mails.length; ++j)
                    object.Mails[j] = $root.Mail.MailInfo.toObject(message.Mails[j], options, q + 1);
            }
            if (message.UnreadCount != null && Object.hasOwnProperty.call(message, "UnreadCount"))
                object.UnreadCount = message.UnreadCount;
            if (message.HasMore != null && Object.hasOwnProperty.call(message, "HasMore"))
                object.HasMore = message.HasMore;
            return object;
        };

        /**
         * Converts this RespMailList to JSON.
         * @function toJSON
         * @memberof Mail.RespMailList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespMailList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespMailList
         * @function getTypeUrl
         * @memberof Mail.RespMailList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespMailList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.RespMailList";
        };

        return RespMailList;
    })();

    Mail.ReqMailRead = (function() {

        /**
         * Properties of a ReqMailRead.
         * @memberof Mail
         * @interface IReqMailRead
         * @property {number|Long|null} [MailId] ReqMailRead MailId
         */

        /**
         * Constructs a new ReqMailRead.
         * @memberof Mail
         * @classdesc Represents a ReqMailRead.
         * @implements IReqMailRead
         * @constructor
         * @param {Mail.IReqMailRead=} [properties] Properties to set
         */
        function ReqMailRead(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqMailRead MailId.
         * @member {number|Long} MailId
         * @memberof Mail.ReqMailRead
         * @instance
         */
        ReqMailRead.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqMailRead instance using the specified properties.
         * @function create
         * @memberof Mail.ReqMailRead
         * @static
         * @param {Mail.IReqMailRead=} [properties] Properties to set
         * @returns {Mail.ReqMailRead} ReqMailRead instance
         */
        ReqMailRead.create = function create(properties) {
            return new ReqMailRead(properties);
        };

        /**
         * Encodes the specified ReqMailRead message. Does not implicitly {@link Mail.ReqMailRead.verify|verify} messages.
         * @function encode
         * @memberof Mail.ReqMailRead
         * @static
         * @param {Mail.IReqMailRead} message ReqMailRead message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailRead.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            return writer;
        };

        /**
         * Encodes the specified ReqMailRead message, length delimited. Does not implicitly {@link Mail.ReqMailRead.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.ReqMailRead
         * @static
         * @param {Mail.IReqMailRead} message ReqMailRead message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailRead.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqMailRead message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.ReqMailRead
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.ReqMailRead} ReqMailRead
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailRead.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.ReqMailRead();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqMailRead message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.ReqMailRead
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.ReqMailRead} ReqMailRead
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailRead.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqMailRead message.
         * @function verify
         * @memberof Mail.ReqMailRead
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqMailRead.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqMailRead message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.ReqMailRead
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.ReqMailRead} ReqMailRead
         */
        ReqMailRead.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.ReqMailRead)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.ReqMailRead: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.ReqMailRead();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqMailRead message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.ReqMailRead
         * @static
         * @param {Mail.ReqMailRead} message ReqMailRead
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqMailRead.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            return object;
        };

        /**
         * Converts this ReqMailRead to JSON.
         * @function toJSON
         * @memberof Mail.ReqMailRead
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqMailRead.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqMailRead
         * @function getTypeUrl
         * @memberof Mail.ReqMailRead
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqMailRead.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.ReqMailRead";
        };

        return ReqMailRead;
    })();

    Mail.RespMailRead = (function() {

        /**
         * Properties of a RespMailRead.
         * @memberof Mail
         * @interface IRespMailRead
         * @property {number|Long|null} [MailId] RespMailRead MailId
         * @property {string|null} [Title] RespMailRead Title
         * @property {string|null} [Content] RespMailRead Content
         * @property {number|null} [TemplateId] RespMailRead TemplateId
         * @property {number|Long|null} [TemplateVersion] RespMailRead TemplateVersion
         * @property {number|null} [ReadStatus] RespMailRead ReadStatus
         * @property {number|null} [MailStatus] RespMailRead MailStatus
         * @property {Array.<Mail.IMailAttachmentInfo>|null} [Attachments] RespMailRead Attachments
         */

        /**
         * Constructs a new RespMailRead.
         * @memberof Mail
         * @classdesc Represents a RespMailRead.
         * @implements IRespMailRead
         * @constructor
         * @param {Mail.IRespMailRead=} [properties] Properties to set
         */
        function RespMailRead(properties) {
            this.Attachments = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespMailRead MailId.
         * @member {number|Long} MailId
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RespMailRead Title.
         * @member {string} Title
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.Title = "";

        /**
         * RespMailRead Content.
         * @member {string} Content
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.Content = "";

        /**
         * RespMailRead TemplateId.
         * @member {number} TemplateId
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.TemplateId = 0;

        /**
         * RespMailRead TemplateVersion.
         * @member {number|Long} TemplateVersion
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.TemplateVersion = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RespMailRead ReadStatus.
         * @member {number} ReadStatus
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.ReadStatus = 0;

        /**
         * RespMailRead MailStatus.
         * @member {number} MailStatus
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.MailStatus = 0;

        /**
         * RespMailRead Attachments.
         * @member {Array.<Mail.IMailAttachmentInfo>} Attachments
         * @memberof Mail.RespMailRead
         * @instance
         */
        RespMailRead.prototype.Attachments = $util.emptyArray;

        /**
         * Creates a new RespMailRead instance using the specified properties.
         * @function create
         * @memberof Mail.RespMailRead
         * @static
         * @param {Mail.IRespMailRead=} [properties] Properties to set
         * @returns {Mail.RespMailRead} RespMailRead instance
         */
        RespMailRead.create = function create(properties) {
            return new RespMailRead(properties);
        };

        /**
         * Encodes the specified RespMailRead message. Does not implicitly {@link Mail.RespMailRead.verify|verify} messages.
         * @function encode
         * @memberof Mail.RespMailRead
         * @static
         * @param {Mail.IRespMailRead} message RespMailRead message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailRead.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            if (message.Title != null && Object.hasOwnProperty.call(message, "Title"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.Title);
            if (message.Content != null && Object.hasOwnProperty.call(message, "Content"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.Content);
            if (message.TemplateId != null && Object.hasOwnProperty.call(message, "TemplateId"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.TemplateId);
            if (message.TemplateVersion != null && Object.hasOwnProperty.call(message, "TemplateVersion"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.TemplateVersion);
            if (message.ReadStatus != null && Object.hasOwnProperty.call(message, "ReadStatus"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ReadStatus);
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.MailStatus);
            if (message.Attachments != null && message.Attachments.length)
                for (var i = 0; i < message.Attachments.length; ++i)
                    $root.Mail.MailAttachmentInfo.encode(message.Attachments[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RespMailRead message, length delimited. Does not implicitly {@link Mail.RespMailRead.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.RespMailRead
         * @static
         * @param {Mail.IRespMailRead} message RespMailRead message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailRead.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespMailRead message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.RespMailRead
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.RespMailRead} RespMailRead
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailRead.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.RespMailRead();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                case 2: {
                        message.Title = reader.string();
                        break;
                    }
                case 3: {
                        message.Content = reader.string();
                        break;
                    }
                case 4: {
                        message.TemplateId = reader.int32();
                        break;
                    }
                case 5: {
                        message.TemplateVersion = reader.int64();
                        break;
                    }
                case 6: {
                        message.ReadStatus = reader.int32();
                        break;
                    }
                case 7: {
                        message.MailStatus = reader.int32();
                        break;
                    }
                case 8: {
                        if (!(message.Attachments && message.Attachments.length))
                            message.Attachments = [];
                        message.Attachments.push($root.Mail.MailAttachmentInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespMailRead message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.RespMailRead
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.RespMailRead} RespMailRead
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailRead.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespMailRead message.
         * @function verify
         * @memberof Mail.RespMailRead
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespMailRead.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            if (message.Title != null && Object.hasOwnProperty.call(message, "Title"))
                if (!$util.isString(message.Title))
                    return "Title: string expected";
            if (message.Content != null && Object.hasOwnProperty.call(message, "Content"))
                if (!$util.isString(message.Content))
                    return "Content: string expected";
            if (message.TemplateId != null && Object.hasOwnProperty.call(message, "TemplateId"))
                if (!$util.isInteger(message.TemplateId))
                    return "TemplateId: integer expected";
            if (message.TemplateVersion != null && Object.hasOwnProperty.call(message, "TemplateVersion"))
                if (!$util.isInteger(message.TemplateVersion) && !(message.TemplateVersion && $util.isInteger(message.TemplateVersion.low) && $util.isInteger(message.TemplateVersion.high)))
                    return "TemplateVersion: integer|Long expected";
            if (message.ReadStatus != null && Object.hasOwnProperty.call(message, "ReadStatus"))
                if (!$util.isInteger(message.ReadStatus))
                    return "ReadStatus: integer expected";
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                if (!$util.isInteger(message.MailStatus))
                    return "MailStatus: integer expected";
            if (message.Attachments != null && Object.hasOwnProperty.call(message, "Attachments")) {
                if (!Array.isArray(message.Attachments))
                    return "Attachments: array expected";
                for (var i = 0; i < message.Attachments.length; ++i) {
                    var error = $root.Mail.MailAttachmentInfo.verify(message.Attachments[i], long + 1);
                    if (error)
                        return "Attachments." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RespMailRead message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.RespMailRead
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.RespMailRead} RespMailRead
         */
        RespMailRead.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.RespMailRead)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.RespMailRead: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.RespMailRead();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            if (object.Title != null)
                message.Title = String(object.Title);
            if (object.Content != null)
                message.Content = String(object.Content);
            if (object.TemplateId != null)
                message.TemplateId = object.TemplateId | 0;
            if (object.TemplateVersion != null)
                if ($util.Long)
                    message.TemplateVersion = $util.Long.fromValue(object.TemplateVersion, false);
                else if (typeof object.TemplateVersion === "string")
                    message.TemplateVersion = parseInt(object.TemplateVersion, 10);
                else if (typeof object.TemplateVersion === "number")
                    message.TemplateVersion = object.TemplateVersion;
                else if (typeof object.TemplateVersion === "object")
                    message.TemplateVersion = new $util.LongBits(object.TemplateVersion.low >>> 0, object.TemplateVersion.high >>> 0).toNumber();
            if (object.ReadStatus != null)
                message.ReadStatus = object.ReadStatus | 0;
            if (object.MailStatus != null)
                message.MailStatus = object.MailStatus | 0;
            if (object.Attachments) {
                if (!Array.isArray(object.Attachments))
                    throw TypeError(".Mail.RespMailRead.Attachments: array expected");
                message.Attachments = [];
                for (var i = 0; i < object.Attachments.length; ++i) {
                    if (!$util.isObject(object.Attachments[i]))
                        throw TypeError(".Mail.RespMailRead.Attachments: object expected");
                    message.Attachments[i] = $root.Mail.MailAttachmentInfo.fromObject(object.Attachments[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RespMailRead message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.RespMailRead
         * @static
         * @param {Mail.RespMailRead} message RespMailRead
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespMailRead.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Attachments = [];
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.Title = "";
                object.Content = "";
                object.TemplateId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.TemplateVersion = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.TemplateVersion = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.ReadStatus = 0;
                object.MailStatus = 0;
            }
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            if (message.Title != null && Object.hasOwnProperty.call(message, "Title"))
                object.Title = message.Title;
            if (message.Content != null && Object.hasOwnProperty.call(message, "Content"))
                object.Content = message.Content;
            if (message.TemplateId != null && Object.hasOwnProperty.call(message, "TemplateId"))
                object.TemplateId = message.TemplateId;
            if (message.TemplateVersion != null && Object.hasOwnProperty.call(message, "TemplateVersion"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.TemplateVersion = typeof message.TemplateVersion === "number" ? BigInt(message.TemplateVersion) : $util.Long.fromBits(message.TemplateVersion.low >>> 0, message.TemplateVersion.high >>> 0, false).toBigInt();
                else if (typeof message.TemplateVersion === "number")
                    object.TemplateVersion = options.longs === String ? String(message.TemplateVersion) : message.TemplateVersion;
                else
                    object.TemplateVersion = options.longs === String ? $util.Long.prototype.toString.call(message.TemplateVersion) : options.longs === Number ? new $util.LongBits(message.TemplateVersion.low >>> 0, message.TemplateVersion.high >>> 0).toNumber() : message.TemplateVersion;
            if (message.ReadStatus != null && Object.hasOwnProperty.call(message, "ReadStatus"))
                object.ReadStatus = message.ReadStatus;
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                object.MailStatus = message.MailStatus;
            if (message.Attachments && message.Attachments.length) {
                object.Attachments = [];
                for (var j = 0; j < message.Attachments.length; ++j)
                    object.Attachments[j] = $root.Mail.MailAttachmentInfo.toObject(message.Attachments[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this RespMailRead to JSON.
         * @function toJSON
         * @memberof Mail.RespMailRead
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespMailRead.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespMailRead
         * @function getTypeUrl
         * @memberof Mail.RespMailRead
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespMailRead.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.RespMailRead";
        };

        return RespMailRead;
    })();

    Mail.ReqMailDelete = (function() {

        /**
         * Properties of a ReqMailDelete.
         * @memberof Mail
         * @interface IReqMailDelete
         * @property {number|Long|null} [MailId] ReqMailDelete MailId
         */

        /**
         * Constructs a new ReqMailDelete.
         * @memberof Mail
         * @classdesc Represents a ReqMailDelete.
         * @implements IReqMailDelete
         * @constructor
         * @param {Mail.IReqMailDelete=} [properties] Properties to set
         */
        function ReqMailDelete(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqMailDelete MailId.
         * @member {number|Long} MailId
         * @memberof Mail.ReqMailDelete
         * @instance
         */
        ReqMailDelete.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new ReqMailDelete instance using the specified properties.
         * @function create
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {Mail.IReqMailDelete=} [properties] Properties to set
         * @returns {Mail.ReqMailDelete} ReqMailDelete instance
         */
        ReqMailDelete.create = function create(properties) {
            return new ReqMailDelete(properties);
        };

        /**
         * Encodes the specified ReqMailDelete message. Does not implicitly {@link Mail.ReqMailDelete.verify|verify} messages.
         * @function encode
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {Mail.IReqMailDelete} message ReqMailDelete message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailDelete.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            return writer;
        };

        /**
         * Encodes the specified ReqMailDelete message, length delimited. Does not implicitly {@link Mail.ReqMailDelete.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {Mail.IReqMailDelete} message ReqMailDelete message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailDelete.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqMailDelete message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.ReqMailDelete} ReqMailDelete
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailDelete.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.ReqMailDelete();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqMailDelete message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.ReqMailDelete} ReqMailDelete
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailDelete.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqMailDelete message.
         * @function verify
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqMailDelete.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            return null;
        };

        /**
         * Creates a ReqMailDelete message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.ReqMailDelete} ReqMailDelete
         */
        ReqMailDelete.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.ReqMailDelete)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.ReqMailDelete: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.ReqMailDelete();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a ReqMailDelete message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {Mail.ReqMailDelete} message ReqMailDelete
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqMailDelete.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            return object;
        };

        /**
         * Converts this ReqMailDelete to JSON.
         * @function toJSON
         * @memberof Mail.ReqMailDelete
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqMailDelete.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqMailDelete
         * @function getTypeUrl
         * @memberof Mail.ReqMailDelete
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqMailDelete.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.ReqMailDelete";
        };

        return ReqMailDelete;
    })();

    Mail.RespMailDelete = (function() {

        /**
         * Properties of a RespMailDelete.
         * @memberof Mail
         * @interface IRespMailDelete
         * @property {number|Long|null} [MailId] RespMailDelete MailId
         */

        /**
         * Constructs a new RespMailDelete.
         * @memberof Mail
         * @classdesc Represents a RespMailDelete.
         * @implements IRespMailDelete
         * @constructor
         * @param {Mail.IRespMailDelete=} [properties] Properties to set
         */
        function RespMailDelete(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespMailDelete MailId.
         * @member {number|Long} MailId
         * @memberof Mail.RespMailDelete
         * @instance
         */
        RespMailDelete.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RespMailDelete instance using the specified properties.
         * @function create
         * @memberof Mail.RespMailDelete
         * @static
         * @param {Mail.IRespMailDelete=} [properties] Properties to set
         * @returns {Mail.RespMailDelete} RespMailDelete instance
         */
        RespMailDelete.create = function create(properties) {
            return new RespMailDelete(properties);
        };

        /**
         * Encodes the specified RespMailDelete message. Does not implicitly {@link Mail.RespMailDelete.verify|verify} messages.
         * @function encode
         * @memberof Mail.RespMailDelete
         * @static
         * @param {Mail.IRespMailDelete} message RespMailDelete message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailDelete.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            return writer;
        };

        /**
         * Encodes the specified RespMailDelete message, length delimited. Does not implicitly {@link Mail.RespMailDelete.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.RespMailDelete
         * @static
         * @param {Mail.IRespMailDelete} message RespMailDelete message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailDelete.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespMailDelete message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.RespMailDelete
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.RespMailDelete} RespMailDelete
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailDelete.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.RespMailDelete();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespMailDelete message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.RespMailDelete
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.RespMailDelete} RespMailDelete
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailDelete.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespMailDelete message.
         * @function verify
         * @memberof Mail.RespMailDelete
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespMailDelete.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            return null;
        };

        /**
         * Creates a RespMailDelete message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.RespMailDelete
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.RespMailDelete} RespMailDelete
         */
        RespMailDelete.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.RespMailDelete)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.RespMailDelete: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.RespMailDelete();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RespMailDelete message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.RespMailDelete
         * @static
         * @param {Mail.RespMailDelete} message RespMailDelete
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespMailDelete.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults)
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            return object;
        };

        /**
         * Converts this RespMailDelete to JSON.
         * @function toJSON
         * @memberof Mail.RespMailDelete
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespMailDelete.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespMailDelete
         * @function getTypeUrl
         * @memberof Mail.RespMailDelete
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespMailDelete.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.RespMailDelete";
        };

        return RespMailDelete;
    })();

    Mail.NotifyMailChanged = (function() {

        /**
         * Properties of a NotifyMailChanged.
         * @memberof Mail
         * @interface INotifyMailChanged
         * @property {Array.<number|Long>|null} [ChangedMailIds] NotifyMailChanged ChangedMailIds
         * @property {number|null} [UnreadCount] NotifyMailChanged UnreadCount
         */

        /**
         * Constructs a new NotifyMailChanged.
         * @memberof Mail
         * @classdesc Represents a NotifyMailChanged.
         * @implements INotifyMailChanged
         * @constructor
         * @param {Mail.INotifyMailChanged=} [properties] Properties to set
         */
        function NotifyMailChanged(properties) {
            this.ChangedMailIds = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotifyMailChanged ChangedMailIds.
         * @member {Array.<number|Long>} ChangedMailIds
         * @memberof Mail.NotifyMailChanged
         * @instance
         */
        NotifyMailChanged.prototype.ChangedMailIds = $util.emptyArray;

        /**
         * NotifyMailChanged UnreadCount.
         * @member {number} UnreadCount
         * @memberof Mail.NotifyMailChanged
         * @instance
         */
        NotifyMailChanged.prototype.UnreadCount = 0;

        /**
         * Creates a new NotifyMailChanged instance using the specified properties.
         * @function create
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {Mail.INotifyMailChanged=} [properties] Properties to set
         * @returns {Mail.NotifyMailChanged} NotifyMailChanged instance
         */
        NotifyMailChanged.create = function create(properties) {
            return new NotifyMailChanged(properties);
        };

        /**
         * Encodes the specified NotifyMailChanged message. Does not implicitly {@link Mail.NotifyMailChanged.verify|verify} messages.
         * @function encode
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {Mail.INotifyMailChanged} message NotifyMailChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyMailChanged.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ChangedMailIds != null && message.ChangedMailIds.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.ChangedMailIds.length; ++i)
                    writer.int64(message.ChangedMailIds[i]);
                writer.ldelim();
            }
            if (message.UnreadCount != null && Object.hasOwnProperty.call(message, "UnreadCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.UnreadCount);
            return writer;
        };

        /**
         * Encodes the specified NotifyMailChanged message, length delimited. Does not implicitly {@link Mail.NotifyMailChanged.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {Mail.INotifyMailChanged} message NotifyMailChanged message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotifyMailChanged.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotifyMailChanged message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.NotifyMailChanged} NotifyMailChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyMailChanged.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.NotifyMailChanged();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ChangedMailIds && message.ChangedMailIds.length))
                            message.ChangedMailIds = [];
                        if ((tag & 7) === 2) {
                            var end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw RangeError("index out of range");
                            reader.len = end2;
                            while (reader.pos < end2)
                                message.ChangedMailIds.push(reader.int64());
                            if (reader.pos !== end2)
                                throw RangeError("index out of range");
                            reader.len = end;
                        } else
                            message.ChangedMailIds.push(reader.int64());
                        break;
                    }
                case 2: {
                        message.UnreadCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a NotifyMailChanged message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.NotifyMailChanged} NotifyMailChanged
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotifyMailChanged.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotifyMailChanged message.
         * @function verify
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotifyMailChanged.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ChangedMailIds != null && Object.hasOwnProperty.call(message, "ChangedMailIds")) {
                if (!Array.isArray(message.ChangedMailIds))
                    return "ChangedMailIds: array expected";
                for (var i = 0; i < message.ChangedMailIds.length; ++i)
                    if (!$util.isInteger(message.ChangedMailIds[i]) && !(message.ChangedMailIds[i] && $util.isInteger(message.ChangedMailIds[i].low) && $util.isInteger(message.ChangedMailIds[i].high)))
                        return "ChangedMailIds: integer|Long[] expected";
            }
            if (message.UnreadCount != null && Object.hasOwnProperty.call(message, "UnreadCount"))
                if (!$util.isInteger(message.UnreadCount))
                    return "UnreadCount: integer expected";
            return null;
        };

        /**
         * Creates a NotifyMailChanged message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.NotifyMailChanged} NotifyMailChanged
         */
        NotifyMailChanged.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.NotifyMailChanged)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.NotifyMailChanged: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.NotifyMailChanged();
            if (object.ChangedMailIds) {
                if (!Array.isArray(object.ChangedMailIds))
                    throw TypeError(".Mail.NotifyMailChanged.ChangedMailIds: array expected");
                message.ChangedMailIds = [];
                for (var i = 0; i < object.ChangedMailIds.length; ++i)
                    if ($util.Long)
                        message.ChangedMailIds[i] = $util.Long.fromValue(object.ChangedMailIds[i], false);
                    else if (typeof object.ChangedMailIds[i] === "string")
                        message.ChangedMailIds[i] = parseInt(object.ChangedMailIds[i], 10);
                    else if (typeof object.ChangedMailIds[i] === "number")
                        message.ChangedMailIds[i] = object.ChangedMailIds[i];
                    else if (typeof object.ChangedMailIds[i] === "object")
                        message.ChangedMailIds[i] = new $util.LongBits(object.ChangedMailIds[i].low >>> 0, object.ChangedMailIds[i].high >>> 0).toNumber();
            }
            if (object.UnreadCount != null)
                message.UnreadCount = object.UnreadCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a NotifyMailChanged message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {Mail.NotifyMailChanged} message NotifyMailChanged
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotifyMailChanged.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.ChangedMailIds = [];
            if (options.defaults)
                object.UnreadCount = 0;
            if (message.ChangedMailIds && message.ChangedMailIds.length) {
                object.ChangedMailIds = [];
                for (var j = 0; j < message.ChangedMailIds.length; ++j)
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ChangedMailIds[j] = typeof message.ChangedMailIds[j] === "number" ? BigInt(message.ChangedMailIds[j]) : $util.Long.fromBits(message.ChangedMailIds[j].low >>> 0, message.ChangedMailIds[j].high >>> 0, false).toBigInt();
                    else if (typeof message.ChangedMailIds[j] === "number")
                        object.ChangedMailIds[j] = options.longs === String ? String(message.ChangedMailIds[j]) : message.ChangedMailIds[j];
                    else
                        object.ChangedMailIds[j] = options.longs === String ? $util.Long.prototype.toString.call(message.ChangedMailIds[j]) : options.longs === Number ? new $util.LongBits(message.ChangedMailIds[j].low >>> 0, message.ChangedMailIds[j].high >>> 0).toNumber() : message.ChangedMailIds[j];
            }
            if (message.UnreadCount != null && Object.hasOwnProperty.call(message, "UnreadCount"))
                object.UnreadCount = message.UnreadCount;
            return object;
        };

        /**
         * Converts this NotifyMailChanged to JSON.
         * @function toJSON
         * @memberof Mail.NotifyMailChanged
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotifyMailChanged.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotifyMailChanged
         * @function getTypeUrl
         * @memberof Mail.NotifyMailChanged
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotifyMailChanged.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.NotifyMailChanged";
        };

        return NotifyMailChanged;
    })();

    Mail.ReqMailClaimAttachment = (function() {

        /**
         * Properties of a ReqMailClaimAttachment.
         * @memberof Mail
         * @interface IReqMailClaimAttachment
         * @property {number|Long|null} [MailId] ReqMailClaimAttachment MailId
         * @property {number|null} [SlotId] ReqMailClaimAttachment SlotId
         */

        /**
         * Constructs a new ReqMailClaimAttachment.
         * @memberof Mail
         * @classdesc Represents a ReqMailClaimAttachment.
         * @implements IReqMailClaimAttachment
         * @constructor
         * @param {Mail.IReqMailClaimAttachment=} [properties] Properties to set
         */
        function ReqMailClaimAttachment(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReqMailClaimAttachment MailId.
         * @member {number|Long} MailId
         * @memberof Mail.ReqMailClaimAttachment
         * @instance
         */
        ReqMailClaimAttachment.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReqMailClaimAttachment SlotId.
         * @member {number} SlotId
         * @memberof Mail.ReqMailClaimAttachment
         * @instance
         */
        ReqMailClaimAttachment.prototype.SlotId = 0;

        /**
         * Creates a new ReqMailClaimAttachment instance using the specified properties.
         * @function create
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {Mail.IReqMailClaimAttachment=} [properties] Properties to set
         * @returns {Mail.ReqMailClaimAttachment} ReqMailClaimAttachment instance
         */
        ReqMailClaimAttachment.create = function create(properties) {
            return new ReqMailClaimAttachment(properties);
        };

        /**
         * Encodes the specified ReqMailClaimAttachment message. Does not implicitly {@link Mail.ReqMailClaimAttachment.verify|verify} messages.
         * @function encode
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {Mail.IReqMailClaimAttachment} message ReqMailClaimAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailClaimAttachment.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.SlotId);
            return writer;
        };

        /**
         * Encodes the specified ReqMailClaimAttachment message, length delimited. Does not implicitly {@link Mail.ReqMailClaimAttachment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {Mail.IReqMailClaimAttachment} message ReqMailClaimAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailClaimAttachment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqMailClaimAttachment message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.ReqMailClaimAttachment} ReqMailClaimAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailClaimAttachment.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.ReqMailClaimAttachment();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                case 2: {
                        message.SlotId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqMailClaimAttachment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.ReqMailClaimAttachment} ReqMailClaimAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailClaimAttachment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqMailClaimAttachment message.
         * @function verify
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqMailClaimAttachment.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                if (!$util.isInteger(message.SlotId))
                    return "SlotId: integer expected";
            return null;
        };

        /**
         * Creates a ReqMailClaimAttachment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.ReqMailClaimAttachment} ReqMailClaimAttachment
         */
        ReqMailClaimAttachment.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.ReqMailClaimAttachment)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.ReqMailClaimAttachment: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.ReqMailClaimAttachment();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            if (object.SlotId != null)
                message.SlotId = object.SlotId | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReqMailClaimAttachment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {Mail.ReqMailClaimAttachment} message ReqMailClaimAttachment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqMailClaimAttachment.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.SlotId = 0;
            }
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                object.SlotId = message.SlotId;
            return object;
        };

        /**
         * Converts this ReqMailClaimAttachment to JSON.
         * @function toJSON
         * @memberof Mail.ReqMailClaimAttachment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqMailClaimAttachment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqMailClaimAttachment
         * @function getTypeUrl
         * @memberof Mail.ReqMailClaimAttachment
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqMailClaimAttachment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.ReqMailClaimAttachment";
        };

        return ReqMailClaimAttachment;
    })();

    Mail.RespMailClaimAttachment = (function() {

        /**
         * Properties of a RespMailClaimAttachment.
         * @memberof Mail
         * @interface IRespMailClaimAttachment
         * @property {number|Long|null} [MailId] RespMailClaimAttachment MailId
         * @property {number|null} [SlotId] RespMailClaimAttachment SlotId
         * @property {number|null} [RewardType] RespMailClaimAttachment RewardType
         * @property {number|null} [ItemId] RespMailClaimAttachment ItemId
         * @property {number|Long|null} [Count] RespMailClaimAttachment Count
         * @property {number|null} [ClaimStatus] RespMailClaimAttachment ClaimStatus
         * @property {number|null} [MailStatus] RespMailClaimAttachment MailStatus
         * @property {number|null} [AttachmentStatus] RespMailClaimAttachment AttachmentStatus
         */

        /**
         * Constructs a new RespMailClaimAttachment.
         * @memberof Mail
         * @classdesc Represents a RespMailClaimAttachment.
         * @implements IRespMailClaimAttachment
         * @constructor
         * @param {Mail.IRespMailClaimAttachment=} [properties] Properties to set
         */
        function RespMailClaimAttachment(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespMailClaimAttachment MailId.
         * @member {number|Long} MailId
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.MailId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RespMailClaimAttachment SlotId.
         * @member {number} SlotId
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.SlotId = 0;

        /**
         * RespMailClaimAttachment RewardType.
         * @member {number} RewardType
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.RewardType = 0;

        /**
         * RespMailClaimAttachment ItemId.
         * @member {number} ItemId
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.ItemId = 0;

        /**
         * RespMailClaimAttachment Count.
         * @member {number|Long} Count
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.Count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RespMailClaimAttachment ClaimStatus.
         * @member {number} ClaimStatus
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.ClaimStatus = 0;

        /**
         * RespMailClaimAttachment MailStatus.
         * @member {number} MailStatus
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.MailStatus = 0;

        /**
         * RespMailClaimAttachment AttachmentStatus.
         * @member {number} AttachmentStatus
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         */
        RespMailClaimAttachment.prototype.AttachmentStatus = 0;

        /**
         * Creates a new RespMailClaimAttachment instance using the specified properties.
         * @function create
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {Mail.IRespMailClaimAttachment=} [properties] Properties to set
         * @returns {Mail.RespMailClaimAttachment} RespMailClaimAttachment instance
         */
        RespMailClaimAttachment.create = function create(properties) {
            return new RespMailClaimAttachment(properties);
        };

        /**
         * Encodes the specified RespMailClaimAttachment message. Does not implicitly {@link Mail.RespMailClaimAttachment.verify|verify} messages.
         * @function encode
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {Mail.IRespMailClaimAttachment} message RespMailClaimAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailClaimAttachment.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.MailId);
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.SlotId);
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.RewardType);
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.ItemId);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.Count);
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ClaimStatus);
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.MailStatus);
            if (message.AttachmentStatus != null && Object.hasOwnProperty.call(message, "AttachmentStatus"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AttachmentStatus);
            return writer;
        };

        /**
         * Encodes the specified RespMailClaimAttachment message, length delimited. Does not implicitly {@link Mail.RespMailClaimAttachment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {Mail.IRespMailClaimAttachment} message RespMailClaimAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailClaimAttachment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespMailClaimAttachment message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.RespMailClaimAttachment} RespMailClaimAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailClaimAttachment.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.RespMailClaimAttachment();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MailId = reader.int64();
                        break;
                    }
                case 2: {
                        message.SlotId = reader.int32();
                        break;
                    }
                case 3: {
                        message.RewardType = reader.int32();
                        break;
                    }
                case 4: {
                        message.ItemId = reader.int32();
                        break;
                    }
                case 5: {
                        message.Count = reader.int64();
                        break;
                    }
                case 6: {
                        message.ClaimStatus = reader.int32();
                        break;
                    }
                case 7: {
                        message.MailStatus = reader.int32();
                        break;
                    }
                case 8: {
                        message.AttachmentStatus = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespMailClaimAttachment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.RespMailClaimAttachment} RespMailClaimAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailClaimAttachment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespMailClaimAttachment message.
         * @function verify
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespMailClaimAttachment.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (!$util.isInteger(message.MailId) && !(message.MailId && $util.isInteger(message.MailId.low) && $util.isInteger(message.MailId.high)))
                    return "MailId: integer|Long expected";
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                if (!$util.isInteger(message.SlotId))
                    return "SlotId: integer expected";
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                if (!$util.isInteger(message.RewardType))
                    return "RewardType: integer expected";
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                if (!$util.isInteger(message.ItemId))
                    return "ItemId: integer expected";
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (!$util.isInteger(message.Count) && !(message.Count && $util.isInteger(message.Count.low) && $util.isInteger(message.Count.high)))
                    return "Count: integer|Long expected";
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                if (!$util.isInteger(message.ClaimStatus))
                    return "ClaimStatus: integer expected";
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                if (!$util.isInteger(message.MailStatus))
                    return "MailStatus: integer expected";
            if (message.AttachmentStatus != null && Object.hasOwnProperty.call(message, "AttachmentStatus"))
                if (!$util.isInteger(message.AttachmentStatus))
                    return "AttachmentStatus: integer expected";
            return null;
        };

        /**
         * Creates a RespMailClaimAttachment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.RespMailClaimAttachment} RespMailClaimAttachment
         */
        RespMailClaimAttachment.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.RespMailClaimAttachment)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.RespMailClaimAttachment: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.RespMailClaimAttachment();
            if (object.MailId != null)
                if ($util.Long)
                    message.MailId = $util.Long.fromValue(object.MailId, false);
                else if (typeof object.MailId === "string")
                    message.MailId = parseInt(object.MailId, 10);
                else if (typeof object.MailId === "number")
                    message.MailId = object.MailId;
                else if (typeof object.MailId === "object")
                    message.MailId = new $util.LongBits(object.MailId.low >>> 0, object.MailId.high >>> 0).toNumber();
            if (object.SlotId != null)
                message.SlotId = object.SlotId | 0;
            if (object.RewardType != null)
                message.RewardType = object.RewardType | 0;
            if (object.ItemId != null)
                message.ItemId = object.ItemId | 0;
            if (object.Count != null)
                if ($util.Long)
                    message.Count = $util.Long.fromValue(object.Count, false);
                else if (typeof object.Count === "string")
                    message.Count = parseInt(object.Count, 10);
                else if (typeof object.Count === "number")
                    message.Count = object.Count;
                else if (typeof object.Count === "object")
                    message.Count = new $util.LongBits(object.Count.low >>> 0, object.Count.high >>> 0).toNumber();
            if (object.ClaimStatus != null)
                message.ClaimStatus = object.ClaimStatus | 0;
            if (object.MailStatus != null)
                message.MailStatus = object.MailStatus | 0;
            if (object.AttachmentStatus != null)
                message.AttachmentStatus = object.AttachmentStatus | 0;
            return message;
        };

        /**
         * Creates a plain object from a RespMailClaimAttachment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {Mail.RespMailClaimAttachment} message RespMailClaimAttachment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespMailClaimAttachment.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.MailId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.MailId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.SlotId = 0;
                object.RewardType = 0;
                object.ItemId = 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, false);
                    object.Count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.Count = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.ClaimStatus = 0;
                object.MailStatus = 0;
                object.AttachmentStatus = 0;
            }
            if (message.MailId != null && Object.hasOwnProperty.call(message, "MailId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.MailId = typeof message.MailId === "number" ? BigInt(message.MailId) : $util.Long.fromBits(message.MailId.low >>> 0, message.MailId.high >>> 0, false).toBigInt();
                else if (typeof message.MailId === "number")
                    object.MailId = options.longs === String ? String(message.MailId) : message.MailId;
                else
                    object.MailId = options.longs === String ? $util.Long.prototype.toString.call(message.MailId) : options.longs === Number ? new $util.LongBits(message.MailId.low >>> 0, message.MailId.high >>> 0).toNumber() : message.MailId;
            if (message.SlotId != null && Object.hasOwnProperty.call(message, "SlotId"))
                object.SlotId = message.SlotId;
            if (message.RewardType != null && Object.hasOwnProperty.call(message, "RewardType"))
                object.RewardType = message.RewardType;
            if (message.ItemId != null && Object.hasOwnProperty.call(message, "ItemId"))
                object.ItemId = message.ItemId;
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.Count = typeof message.Count === "number" ? BigInt(message.Count) : $util.Long.fromBits(message.Count.low >>> 0, message.Count.high >>> 0, false).toBigInt();
                else if (typeof message.Count === "number")
                    object.Count = options.longs === String ? String(message.Count) : message.Count;
                else
                    object.Count = options.longs === String ? $util.Long.prototype.toString.call(message.Count) : options.longs === Number ? new $util.LongBits(message.Count.low >>> 0, message.Count.high >>> 0).toNumber() : message.Count;
            if (message.ClaimStatus != null && Object.hasOwnProperty.call(message, "ClaimStatus"))
                object.ClaimStatus = message.ClaimStatus;
            if (message.MailStatus != null && Object.hasOwnProperty.call(message, "MailStatus"))
                object.MailStatus = message.MailStatus;
            if (message.AttachmentStatus != null && Object.hasOwnProperty.call(message, "AttachmentStatus"))
                object.AttachmentStatus = message.AttachmentStatus;
            return object;
        };

        /**
         * Converts this RespMailClaimAttachment to JSON.
         * @function toJSON
         * @memberof Mail.RespMailClaimAttachment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespMailClaimAttachment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespMailClaimAttachment
         * @function getTypeUrl
         * @memberof Mail.RespMailClaimAttachment
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespMailClaimAttachment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.RespMailClaimAttachment";
        };

        return RespMailClaimAttachment;
    })();

    Mail.ReqMailClaimAllAttachment = (function() {

        /**
         * Properties of a ReqMailClaimAllAttachment.
         * @memberof Mail
         * @interface IReqMailClaimAllAttachment
         */

        /**
         * Constructs a new ReqMailClaimAllAttachment.
         * @memberof Mail
         * @classdesc Represents a ReqMailClaimAllAttachment.
         * @implements IReqMailClaimAllAttachment
         * @constructor
         * @param {Mail.IReqMailClaimAllAttachment=} [properties] Properties to set
         */
        function ReqMailClaimAllAttachment(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new ReqMailClaimAllAttachment instance using the specified properties.
         * @function create
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {Mail.IReqMailClaimAllAttachment=} [properties] Properties to set
         * @returns {Mail.ReqMailClaimAllAttachment} ReqMailClaimAllAttachment instance
         */
        ReqMailClaimAllAttachment.create = function create(properties) {
            return new ReqMailClaimAllAttachment(properties);
        };

        /**
         * Encodes the specified ReqMailClaimAllAttachment message. Does not implicitly {@link Mail.ReqMailClaimAllAttachment.verify|verify} messages.
         * @function encode
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {Mail.IReqMailClaimAllAttachment} message ReqMailClaimAllAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailClaimAllAttachment.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            return writer;
        };

        /**
         * Encodes the specified ReqMailClaimAllAttachment message, length delimited. Does not implicitly {@link Mail.ReqMailClaimAllAttachment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {Mail.IReqMailClaimAllAttachment} message ReqMailClaimAllAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReqMailClaimAllAttachment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReqMailClaimAllAttachment message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.ReqMailClaimAllAttachment} ReqMailClaimAllAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailClaimAllAttachment.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.ReqMailClaimAllAttachment();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a ReqMailClaimAllAttachment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.ReqMailClaimAllAttachment} ReqMailClaimAllAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReqMailClaimAllAttachment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReqMailClaimAllAttachment message.
         * @function verify
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReqMailClaimAllAttachment.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            return null;
        };

        /**
         * Creates a ReqMailClaimAllAttachment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.ReqMailClaimAllAttachment} ReqMailClaimAllAttachment
         */
        ReqMailClaimAllAttachment.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.ReqMailClaimAllAttachment)
                return object;
            return new $root.Mail.ReqMailClaimAllAttachment();
        };

        /**
         * Creates a plain object from a ReqMailClaimAllAttachment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {Mail.ReqMailClaimAllAttachment} message ReqMailClaimAllAttachment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReqMailClaimAllAttachment.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this ReqMailClaimAllAttachment to JSON.
         * @function toJSON
         * @memberof Mail.ReqMailClaimAllAttachment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReqMailClaimAllAttachment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReqMailClaimAllAttachment
         * @function getTypeUrl
         * @memberof Mail.ReqMailClaimAllAttachment
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReqMailClaimAllAttachment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.ReqMailClaimAllAttachment";
        };

        return ReqMailClaimAllAttachment;
    })();

    Mail.RespMailClaimAllAttachment = (function() {

        /**
         * Properties of a RespMailClaimAllAttachment.
         * @memberof Mail
         * @interface IRespMailClaimAllAttachment
         * @property {Array.<Mail.IMailClaimedSlot>|null} [Slots] RespMailClaimAllAttachment Slots
         * @property {number|null} [ClaimedCount] RespMailClaimAllAttachment ClaimedCount
         */

        /**
         * Constructs a new RespMailClaimAllAttachment.
         * @memberof Mail
         * @classdesc Represents a RespMailClaimAllAttachment.
         * @implements IRespMailClaimAllAttachment
         * @constructor
         * @param {Mail.IRespMailClaimAllAttachment=} [properties] Properties to set
         */
        function RespMailClaimAllAttachment(properties) {
            this.Slots = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RespMailClaimAllAttachment Slots.
         * @member {Array.<Mail.IMailClaimedSlot>} Slots
         * @memberof Mail.RespMailClaimAllAttachment
         * @instance
         */
        RespMailClaimAllAttachment.prototype.Slots = $util.emptyArray;

        /**
         * RespMailClaimAllAttachment ClaimedCount.
         * @member {number} ClaimedCount
         * @memberof Mail.RespMailClaimAllAttachment
         * @instance
         */
        RespMailClaimAllAttachment.prototype.ClaimedCount = 0;

        /**
         * Creates a new RespMailClaimAllAttachment instance using the specified properties.
         * @function create
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {Mail.IRespMailClaimAllAttachment=} [properties] Properties to set
         * @returns {Mail.RespMailClaimAllAttachment} RespMailClaimAllAttachment instance
         */
        RespMailClaimAllAttachment.create = function create(properties) {
            return new RespMailClaimAllAttachment(properties);
        };

        /**
         * Encodes the specified RespMailClaimAllAttachment message. Does not implicitly {@link Mail.RespMailClaimAllAttachment.verify|verify} messages.
         * @function encode
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {Mail.IRespMailClaimAllAttachment} message RespMailClaimAllAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailClaimAllAttachment.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.Slots != null && message.Slots.length)
                for (var i = 0; i < message.Slots.length; ++i)
                    $root.Mail.MailClaimedSlot.encode(message.Slots[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.ClaimedCount != null && Object.hasOwnProperty.call(message, "ClaimedCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ClaimedCount);
            return writer;
        };

        /**
         * Encodes the specified RespMailClaimAllAttachment message, length delimited. Does not implicitly {@link Mail.RespMailClaimAllAttachment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {Mail.IRespMailClaimAllAttachment} message RespMailClaimAllAttachment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RespMailClaimAllAttachment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RespMailClaimAllAttachment message from the specified reader or buffer.
         * @function decode
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Mail.RespMailClaimAllAttachment} RespMailClaimAllAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailClaimAllAttachment.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var end, message;
            if (length === undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = new $root.Mail.RespMailClaimAllAttachment();
            while (reader.pos < end) {
                var tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Slots && message.Slots.length))
                            message.Slots = [];
                        message.Slots.push($root.Mail.MailClaimedSlot.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        message.ClaimedCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            if (length !== undefined) {
                if (reader.pos !== end)
                    throw RangeError("index out of range");
                reader.len = length;
            }
            return message;
        };

        /**
         * Decodes a RespMailClaimAllAttachment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Mail.RespMailClaimAllAttachment} RespMailClaimAllAttachment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RespMailClaimAllAttachment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RespMailClaimAllAttachment message.
         * @function verify
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RespMailClaimAllAttachment.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.Slots != null && Object.hasOwnProperty.call(message, "Slots")) {
                if (!Array.isArray(message.Slots))
                    return "Slots: array expected";
                for (var i = 0; i < message.Slots.length; ++i) {
                    var error = $root.Mail.MailClaimedSlot.verify(message.Slots[i], long + 1);
                    if (error)
                        return "Slots." + error;
                }
            }
            if (message.ClaimedCount != null && Object.hasOwnProperty.call(message, "ClaimedCount"))
                if (!$util.isInteger(message.ClaimedCount))
                    return "ClaimedCount: integer expected";
            return null;
        };

        /**
         * Creates a RespMailClaimAllAttachment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Mail.RespMailClaimAllAttachment} RespMailClaimAllAttachment
         */
        RespMailClaimAllAttachment.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Mail.RespMailClaimAllAttachment)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Mail.RespMailClaimAllAttachment: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var message = new $root.Mail.RespMailClaimAllAttachment();
            if (object.Slots) {
                if (!Array.isArray(object.Slots))
                    throw TypeError(".Mail.RespMailClaimAllAttachment.Slots: array expected");
                message.Slots = [];
                for (var i = 0; i < object.Slots.length; ++i) {
                    if (!$util.isObject(object.Slots[i]))
                        throw TypeError(".Mail.RespMailClaimAllAttachment.Slots: object expected");
                    message.Slots[i] = $root.Mail.MailClaimedSlot.fromObject(object.Slots[i], long + 1);
                }
            }
            if (object.ClaimedCount != null)
                message.ClaimedCount = object.ClaimedCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a RespMailClaimAllAttachment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {Mail.RespMailClaimAllAttachment} message RespMailClaimAllAttachment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RespMailClaimAllAttachment.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.Slots = [];
            if (options.defaults)
                object.ClaimedCount = 0;
            if (message.Slots && message.Slots.length) {
                object.Slots = [];
                for (var j = 0; j < message.Slots.length; ++j)
                    object.Slots[j] = $root.Mail.MailClaimedSlot.toObject(message.Slots[j], options, q + 1);
            }
            if (message.ClaimedCount != null && Object.hasOwnProperty.call(message, "ClaimedCount"))
                object.ClaimedCount = message.ClaimedCount;
            return object;
        };

        /**
         * Converts this RespMailClaimAllAttachment to JSON.
         * @function toJSON
         * @memberof Mail.RespMailClaimAllAttachment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RespMailClaimAllAttachment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RespMailClaimAllAttachment
         * @function getTypeUrl
         * @memberof Mail.RespMailClaimAllAttachment
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RespMailClaimAllAttachment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Mail.RespMailClaimAllAttachment";
        };

        return RespMailClaimAllAttachment;
    })();

    return Mail;
})();

module.exports = $root;
